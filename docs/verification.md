# Verification

Current camera-facing sprite, privacy and combined-load evidence: [Mixed spatial review](verification/mixed-spatial-review.md). Earlier results below describe their own revisions.

## Recast and world-presentation integration

See the [current integration record](verification/recast-integration.md) for the pinned worker/capsule implementation, sampled clearance calibration, running application, performance probes and browser evidence/limitations. Historical native lattice, schema-9 and earlier automated results below are not passing qualification for the new backend. No new unit, integration or browser test suites were run for this request.

This file records current reproducible evidence and acceptance gaps. Fixture evidence does not establish live model quality, provider cost, hosted security, capacity or balance.

## Macrofold worker reuse

Manual local recovery execution used a disposable SQLite database: reserve a $0.01 synthetic allocation, close/reopen before writing its operational marker, recover through the real backend allocation method, then repeat recovery. The ledger retained one $0.01 accounting entry; changing the amount or using ordinary duplicate reservation was rejected. This was local accounting evidence, not provider spend. Production build passed. The Macrofold endpoint remained unreachable, so workspace retry and zero-rate worker resume have no new live acceptance evidence. No automated tests were written or run.

The worker-ownership change passed the production TypeScript/client build. A manual invocation started the real backend against disposable SQLite state with a $0.01 compute allocation and $0.02 total cap, but workspace provisioning could not connect to the configured `http://localhost:3210` endpoint. A separate connection check also failed. No sandbox was created, no model request was dispatched, and live reuse across runs/restart was not verified. Automated tests were neither written nor run; remaining coverage is tracked in [TODO](maintainers/TODO.md#macrofold-worker-reuse--deferred-validation).

## Foundation review verification

The September 22 review corrected the native response envelope passed to action interpretation when NPC invention is enabled, lost withdrawal handles in relevance fallback, missing generated gathering-target knowledge checks, and premature removal of child dispatch from tracked work. These boundaries have deferred automated coverage in [TODO](maintainers/TODO.md#invention-foundation--deferred-automated-coverage).

Manual execution in a fresh disposable SQLite world admitted a previously recorded rake proposal, crafted it, gathered four fibers with exactly four resource units debited, and reopened with its recipe and completed request retained. Native action interpretation accepted an explicitly constructed demonstration response after authoring metadata was separated; this is native fixture evidence, not a new live NPC decision. Wilderness, reservoir and touch presets each advanced 120 simulation seconds and passed saved-world validation. Provider calls, spending and outstanding reservations were zero. Production build and formatting passed; no automated tests were written or run. Browser, live-provider and dispatch-failure/concurrency qualification were not repeated.

## Invention foundation expansion

Manual execution on September 22, 2026 used isolated SQLite worlds, real configured inference, native transitions and the production HTTP server. No automated tests were written or run.

- The live provider admitted a branch bow, stone-tipped arrow and bound wooden fiber rake. The bow feasibility rubric used the existing 0.55 confidence threshold. This is one successful clear-request observation, not broad routing calibration.
- After reopening the saved world, native crafting produced bow `item-19` and arrow `item-45`. Equipping and hunting consumed the arrow; the shot missed under native rules. The crafted rake `item-57` gathered four fibers and debited exactly four units from its finite resource. Restart retained definitions, equipment identity and completed requests.
- Supplying the saved rake proposal directly admitted without provider calls. An HTTP-supplied variant with null unused components was normalized and admitted under a zero-dollar cap; repeating the request returned its saved completion. Owner HTTP controls locked and unlocked NPC invention, and player history/status omitted NPC requests.
- Native NPC execution rejected a locked request, persisted invalid-method feedback, admitted an explicit linked revision and suppressed the unchanged method on resubmission. Discovery events addressed only their inventor; NPC ownership named the world creator and did not teach the player. Admission consumed no crafting resources. These native observations do not themselves establish model quality.
- In a fresh synthetic world containing repository-authored fictional characters, live NPC decisions exposed and helped resolve an envelope-integration error and null-component mismatch. A later complete proposal was correctly rejected because raw fiber cannot fill the body role. After private feedback was delivered, a fresh live decision revised its own materials and submitted an admitted sling through a linked child request, without a second authoring call. The NPC chose to speak during that later decision; the engine's validation/discovery events remained private. This demonstrates one live revision, not reliable autonomous invention across all situations.

Browser control timed out twice, so no fresh visual walkthrough is claimed. The production page returned HTTP 200; actual session/origin-protected routes verified settings, supplied proposals, durable results and duplicate delivery. Full browser-originated crafting/use and broader automated/model-quality coverage remain in [TODO](maintainers/TODO.md#invention-foundation--deferred-automated-coverage).

Final native execution reloaded five recipes through the gathering-definition integrity checks, rejected a decision stamped before an NPC lock/reopen, delivered an invalid-method result only to its actor, and projected correct gathering-tool labels without NPC jobs. The temporary HTTP server was stopped; disposable saves were retained. Production build, formatting and the 104-file Markdown link/anchor scan passed. Existing PlayCanvas externalization and bundle-size warnings remain.

Conservative additional ledger usage was **$0.047265**, including **$0.030000** retained for the initial sandbox-blocked network attempts; no reservations remained. Including earlier work in this task, the recorded total is **$0.374684**, below the authorized $10. These are application estimates, not a provider invoice. Live NPC calls used only synthetic demo worlds after approval review rejected use of the earlier demo context.

Agency implementation and new operation/goal/plan acceptance remain open in the [AG tracker](maintainers/agent-agency.md). Documentation integration is not gameplay, fixture or live-provider evidence.

## Agency documentation integration checks

Documentation-only integration checked local Markdown paths/heading anchors, existing checkbox counts/states, source-comment references and formatting. Research-source identity checks retained the supplied review’s access limitations; this was not a repeated full-text study. This changed no executable code. Runtime tests, browser gameplay and paid inference were not run. AG/INV/CR/NC acceptance remains open.

### Retained memory-perspective evidence

The prior memory specification recorded isolated native execution of gathering perspectives, attributed speech, repeated conversion and inspector source tags. This is relocated evidence, not a new execution during agency integration. Running-save/restart verification, comprehensive automated coverage and live model acceptance remain pending in [CR’s perspective checks](maintainers/cognition-redesign.md#individual-memory-perspective).

## Current automated evidence

| Area                                               | Current result                                                                                                             | Scope and limitation                                                                                                                                                                                                            |
| -------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Formatting, strict TypeScript and production build | Pass on the latest recorded September 20 run                                                                               | Build emits the existing large-chunk warning.                                                                                                                                                                                   |
| Chromium UI scenarios                              | 8/8 pass                                                                                                                   | Covers native gameplay, actions, camera, vision, time/presence, React layout, continuous work/themes/scale and private diagnostics with no credentials or paid calls. Device and assistive-technology coverage remains bounded. |
| Focused cognition/cancellation                     | 49/49 pass                                                                                                                 | Covers awareness-backed evidence, context refresh after attention, no autonomous dispatch without complete credentials and abort-as-cancellation. No paid calls.                                                                |
| Non-HTTP test run                                  | 159 pass / 8 fail                                                                                                          | Remaining failures concern catalogue, context ordering, schema-version and journal assertions.                                                                                                                                  |
| Full UI-change baseline comparison                 | 140 pass / 37 fail after UI changes versus 136 pass / the same 37 failures at unchanged HEAD                               | Four new native trait/description/fact scenarios pass; the UI work introduced no new failing test name in that comparison.                                                                                                      |
| Later shared-worktree run                          | 139 pass / 38 fail                                                                                                         | Concurrent recall/storage changes added the no-credentials/paused fixture failure. This does not alter the earlier UI comparison.                                                                                               |
| PostgreSQL pgvector sample                         | Migration/isolation/deletion/top-24 path pass; 2,000 vectors at 512 dimensions returned 24 IDs/scores in about 15 ms       | Synthetic local data, one sample, exact search; no semantic-quality or production-capacity claim.                                                                                                                               |
| God editors                                        | Focused domain, same-origin HTTP and Chromium flows pass                                                                   | Covers person creation, trait validation, paused edits, finite spawn catalogue and person revival. Broad conflict, revocation and mature-data scaling remain open.                                                              |
| Persistence and public patches                     | Native journal recovery, compact backup, bootstrap/SSE, retained-revision replay and editor deletion persistence exercised | Small seed world only; one routine tick produced a 151-byte patch versus a 15,368-byte full view. Crash windows, PostgreSQL recovery, slow readers and mature saves remain open.                                                |

A sandboxed full run also encountered ten loopback `listen EPERM` setup failures; that run is not evidence about the HTTP assertions.

## Performance investigation

### Review runtime observations

A disposable native SQLite world ran through the production HTTP server with no provider credentials and zero AI budget. It accepted movement, returned the same result on duplicate and paused retries, reached `(12,13)`, delivered native speech, and exposed that speech in the first direct public projection while optional status was unresolved. The public view reported saved persistence and 11 visible entities. The history revision advanced after native narration fallback; explicit regeneration succeeded and advanced it again. The server closed cleanly. This verifies the no-provider fallback path, not generated prose, browser rendering, PostgreSQL or scale. The production build passes; no automated tests were written or run. The native run was repeated after the cancellation and Talk opening-signal fixes with the same successful command/speech outcomes and clean shutdown. Browser opening-scroll behavior and pending-read cancellation/edit races remain in [deferred coverage](maintainers/TODO.md#deferred-performance-validation).

### Continued implementation runtime observations

The production build passes. No automated tests were written or run. Native verification used Node 22.23.2 on an Apple M1 Pro, local SQLite, isolated saves, no provider credentials and zero AI budget. [Raw samples](verification/performance-continued.json) retain successful outcomes and timing data. These are small runtime observations, not browser, PostgreSQL, soak, AI-quality or population acceptance.

Twelve walks from cloned small-world snapshots compared the 250 ms and 50 ms cadences. Both contained 1,599 synthetic historical rows in addition to native events. First changed position received over SSE had a median of approximately 157 ms / 58 ms and a maximum of 164 ms / 63 ms, respectively. HTTP acknowledgement medians were approximately 9 ms / 13 ms. The clean candidate's native-step p95 was 4.81 ms and command-durable p95 was 11.70 ms across 24 move/cancel commands. These tiny samples do not establish tail percentiles. The first candidate run overlapped backup/restore work in another process and recorded a 339 ms movement outlier; it is retained in the raw evidence. No run measured browser paint. Each paused one-second sample observed zero SQL statements.

The application replayed a committed gameplay result before and after restart, rejected a conflicting body and an unknown command epoch, and served/deleted a cold event through the owner API. A synthetic history fixture produced 1,087 archived rows outside a roughly 550-event working set while preserving SQL history. The backup script exported it, the restore script loaded it into an empty target, and an existing-world restore removed a post-backup fixture event while preserving current receipts. Source authenticity and live cognition were not evaluated. Exact expiry/pruning boundaries, corrupt archives, concurrency/failure matrices, naturally aged worlds and PostgreSQL recovery remain in [deferred coverage](maintainers/TODO.md#deferred-performance-validation).

### Original query-amplification baseline

A direct execution fixture before these optimizations, with seed 73, an in-memory SQLite store, explicit zero spending and no credentials, admitted one movement intention for the controlled actor. Instrumenting repository statement execution after initialization/presence admission counted 16 SQL statements for its one `action-started` event with two audience members. The PostgreSQL adapter additionally sends `BEGIN` and `COMMIT`: static path inspection predicted 18 serial database operations for that ordinary non-checkpoint case. Of the 16 statements, 12 belonged to history projection, including the existing-row check, revocation lookup and five deletions even for a new event. This is baseline query-amplification evidence, not current statement counts or measured PostgreSQL response time.

Configuration inspection exposed only a loopback/non-loopback classification; the configured PostgreSQL address is loopback. It does not establish the physical backend topology or latency. Earlier reasoning that attributed the delay to a remote database was not verified. Exploratory CPU runs on a copy of the older local SQLite save showed substantial draft-finalization work and sensitivity to retained history, but were not a controlled current-save before/after benchmark. Some initial command samples were rejected because that saved player was incapacitated, so those timings are not successful-walk evidence.

No live browser-to-PostgreSQL command trace was captured, no paid call was made, and no runtime optimization was implemented in this investigation. Reproduce with outcome validation, actual PostgreSQL timing and the same workload through [PF00](maintainers/performance.md#pf00--baseline-and-attribution) before assigning a dominant latency cause or claiming a speedup.

### Performance foundation runtime observations

On 2026-09-21, the production client build and runtime TypeScript build passed on an Apple M1 Pro, Darwin arm64, Node 22.23.2. A disposable seed-1086 SQLite world ran through the actual loopback HTTP application with an empty environment except runtime paths, port, temporary data directory and an explicit zero AI budget. No automated tests or paid/provider calls were written or run. The existing Vite large-chunk and PlayCanvas worker externalization warnings remain.

The final native run recovered position `(12,13)` and paused/saved state from a clean restart, accepted 30 movement requests plus one separate walk, reached `(11,13)`, and reported saved persistence. Its 31 successful durable command samples had p50 2.88 ms, p95 17.75 ms and p99 30.22 ms. These are short-sample descriptive order statistics, not stable tail estimates. An earlier run measured p95 3.93 ms, illustrating run-to-run variation; neither establishes a controlled before/after speedup. The final run's 60 native-step samples included a 45.73 ms maximum, so an eight-millisecond continuous CPU budget is not established merely by yielding between steps. Paused idle observation issued zero statements over one second.

A separate accepted walk from `(11,13)` to `(12,13)` returned its HTTP acknowledgement in 6.46 ms and delivered its first changed position through SSE in 258.77 ms. This is stream receipt, not browser render time; it does not meet the proposed 200 ms first-position target and does not justify adopting a higher cadence before CPU qualification. The preceding combined run had no usable first-position sample. [Raw bounded samples and workload metadata](verification/performance-native.json) preserve both observations.

No PostgreSQL runtime, browser/React paint, mature-world replay, narration-provider execution, crash-in-flight recovery, multiplayer admission or population/crowd qualification was performed. In particular, this does not establish support for 100 agents, 100 independent players or thousands of animals. Deferred regression and load coverage is recorded in [maintainer TODO](maintainers/TODO.md#deferred-performance-validation), with incomplete implementation exits in [PF00–PF11](maintainers/performance.md).

### Mature-world tick profiling

Read-only inspection of the running local PostgreSQL game on Apple M1 Pro / Darwin arm64 / Node 22.23.2 found approximately 93% of one CPU core in use. The bounded live native-step samples had p50 21.85 ms and p95 47.23 ms; mutation wait p95 was 896 ms. World commit p50 was 6.57 ms, public projection p50 0.21 ms, and PostgreSQL statement p50 0.77 ms. These are descriptive samples, not end-to-end browser timings. At 1×, 60 native steps are required per real second; even the median step cost exhausts that CPU budget.

A consistent read-only PostgreSQL snapshot contained 15 entities, 864 hot events, 209 personal memories and 1,585 awareness entries at simulation time 4,259,454. Offline execution of the actual native kernel, with 30 warm-up steps and 180 profiled one-second steps, took 3,752 ms (p50 18.97 ms, p95 29.19 ms). About 96% of sampled baseline CPU was in Immer finalization/traversal helpers. Diagnostic deep freezing of the input snapshot before warm-up reduced the measured loop to 73 ms (p50 0.34 ms, p95 0.71 ms). Both runs produced the same final-world SHA-256 digest. This excludes initial freezing cost and does not establish long-session or editor/migration compatibility. That input-only experiment preceded the server ownership-boundary implementation described below.

Independent disposable SQLite HTTP servers loaded that same snapshot with explicit zero spending and no provider credentials. Over approximately ten seconds, 1× accepted 413 simulated seconds (41.27 per real second); 3× accepted 444 (44.37 per real second), versus required rates of 60 and 180. The 3× run recorded 4.257 real seconds excluded by the existing greater-than-two-second suspension rule and a maximum completed tick of 2,186 ms. CPU gauges approached 100% of one core. In-flight batches were still running at observation; native work counts therefore exceed accepted simulated time. These short runs used SQLite, not PostgreSQL, and shared the machine with the existing live game. They reproduce saturation and clock loss, not stable tail estimates or population qualification. The live game's speed/state was not changed.

The profiler is reproducible without a running database or provider:

```sh
node --import tsx scripts/profile-native.ts /private/path/backup.json /tmp/native.cpuprofile
node --import tsx scripts/profile-native.ts /private/path/backup.json /tmp/native-baseline.cpuprofile 180 --mutable-snapshots
```

Input accepts a materialized SavedWorld or an existing `backup-world.ts` backup; raw checkpoint rows without journal replay are not sufficient. Output paths must be new. Profiles are created with owner-only permissions; keep snapshots and CPU profiles outside version control. The profiler reports aggregate timing, hottest self samples and a final-world digest. By default it freezes its parsed input and every step result, matching the server boundary. `--mutable-snapshots` reproduces the unfrozen baseline. It reports initial freezing separately from the measured loop. Complete outcome equivalence still needs broader qualification.

The authenticated local `GET /api/performance` endpoint adds bounded cumulative tick/persistence timings, requested/accepted clock counters, excluded-gap counters, process CPU/heap and event-loop/GC observations. Compare counter deltas across matching uptime windows; nested stage times overlap. Instrumentation requires running this updated build. [PF03](maintainers/performance.md#pf03--native-cpu-and-incremental-admission) owns snapshot-boundary and overload qualification; [deferred coverage](maintainers/TODO.md#deferred-performance-validation) records the unrun automated checks. No automated tests or paid calls were performed for this investigation.

### Frozen snapshots and bounded tick prefixes

With server-owned snapshot freezing and bounded catch-up batches, final independent ten-second runs on the same copied mature save advanced 601 simulated seconds at 1× (60.10 per real second), 1,802 at 3× (180.19), and 4,797 at 8× (479.67). Small differences from the requested rates reflect timer/control observation boundaries. None recorded excluded gaps; final fractional debt was below one simulated second. Latest process CPU gauges were approximately 4%, 10% and 21% of one core. Native-step recent p50/p95 were 0.45/1.74 ms at 1×, 0.37/1.00 ms at 3× and 0.36/0.82 ms at 8×. Individual outliers still exceeded eight milliseconds (up to 143 ms at 8×); the budget is a cooperative boundary between indivisible steps, not a hard latency guarantee. These remain short SQLite runs, not production PostgreSQL or browser latency acceptance.

An offline run including freezing every result completed 180 steps in 220 ms, with the same final-world digest as the unfrozen baseline. Initial input freezing took about 24 ms separately. A 10,000-step run took 4.35 seconds (p50 0.33 ms, p95 0.84 ms); this is approximately 2.8 simulated hours of native execution, not a real-time provider/history soak. It does not prove all workloads meet the eight-millisecond individual-step budget.

Actual application execution on disposable zero-budget saves accepted movement and duplicate retries, immediate speech, person edits, attributed event edits and event deletion. A continuous three-second interval explicitly marked as observed busy time advanced all 180 due steps across bounded prefixes. A speed-change command interleaved after the first step and completed in 18.76 ms without losing the remaining debt. A three-second interval using the default unobserved-suspension policy advanced zero steps. Clean close/reopen preserved the edited name and simulation time and restored frozen snapshots without storage errors. An initial invalid attributed-text edit was correctly rejected and is not counted as accepted editor evidence. Legacy-schema migration, failure/concurrency matrices and larger populations remain unqualified. Automated tests were neither added nor run.

### Perception and population follow-up

The next native CPU profile attributed substantial work to encounter projection and Immer proxy reads. A synthetic fixture added ten people through the actual spawn transition to the same mature copied save (25 total entities). With 30 warm-up steps and 1,000 measured steps, the previous code took 2,193 ms (p50 1.90 ms); scalar perception reads, prior-membership sets and stable visibility arrays took 858 ms, with a confirmation run of 663 ms (p50 0.56 ms). Both candidate runs matched the baseline final-world digest. This is 61–70% less native processing time for this fixture, not a claim about live cognition, browser rendering or every world. The original 15-entity save's 3,000-step median improved from 0.34 to 0.22 ms, but total duration varied from 1,217 to 1,259 ms in the final comparison; no consistent 40% whole-run gain is established there.

A crowded 100-added-person experiment did not complete warm-up promptly and was deliberately terminated. It provides no completed throughput result or 100-agent acceptance. Code inspection identifies pairwise encounter emission, witness fan-out and linear `experienceEntry` duplicate scans over growing awareness as remaining amplification paths. Separate instrumentation and bounded crowd qualification remain in PF03/PF09.

An actual `AiDirector.considerThought` execution on the ten-added-person fixture used explicit zero budget, an injected client that rejects provider execution, and synthetic hunger to exercise native-protection scheduling. One hundred admission passes took 41.7 ms; thought-signature refresh totaled 10.14 ms (p50 0.065 ms, p95 0.162 ms), and 14 maintenance-refresh samples totaled 0.97 ms. Provider call count was zero. This shows these idle/native-protection checks were cheap in that fixture; it excludes paid model latency, full context construction, embeddings and successful AI execution. The one-world workflow/global interval limitation is established by code inspection, not model-load measurement. The final build passed, and a disposable zero-budget HTTP run accepted movement/retries and speech, published updated state, and paused/closed cleanly after the perception change. No automated tests were written or run.

## Current live-provider evidence

Capped isolated checks exercised native multi-question Jev, embeddings, immediate speech, summary generation and workspace-to-PostgreSQL publication. A need-event case selected a carried-berry memory, routed level 2 and committed eating. A dream became eligible after 7,200 sleeping seconds and published an imagined thought, but the content quality was minimal. These checks establish connectivity and lifecycle paths only. They do not establish broad conversational quality, recall quality, latency or sustainable cost.

No fixture, synthetic vector run or manually authored response counts as live-model quality acceptance. Live evidence must record model identifiers, routes, provider receipts, configured price assumptions, latency, failures and the authorized spending cap.

## Open acceptance

- The [cognition tracker](maintainers/cognition-redesign.md), especially CR12 and its migrated acceptance gaps, owns privacy, recall, routing, consolidation, reflection, sleep/dream, cancellation, recovery and token/latency completion.
- The [narration tracker](maintainers/narration-and-conversations.md) retains response-to-speech/recall integration, durable conversation lifecycle, Narrator privacy, ordering, storage, transcript and live prose-quality acceptance.
- The [actor tracker](maintainers/actor-model.md) owns cross-species migration, lifecycle, body effects, optional minds and compatibility evidence.
- The [invention tracker](maintainers/inventions-and-world-evolution.md) owns the live invent-to-craft/use loop, semantic reuse, workshop, conjuring and mechanics-evolution evidence.
- The [production-data tracker](maintainers/production-data.md) owns migration, restore, normalization, query, hosting and measured-scale gates.
- The [performance tracker](maintainers/performance.md) owns native latency attribution, query/CPU reduction, scheduling and performance regression budgets.
- Cross-cutting UI, editor, journal, patch and documentation checks remain in [Maintainer TODO](maintainers/TODO.md).

## Bounded live playtest protocol

Use a backed-up or isolated save and an explicit nonzero cap. Verify a real conversation and later permitted recall, one resident decision, a generated sling that is crafted and used, finite harvest/cooking/eating, reuse without regeneration, and a separately compatible bow/arrow path. Exercise pause, restart, idempotent resubmission, stale work and uncertain accounting. No timeout may consume resources or silently repeat paid work. Record receipts and keep the relevant gate open when a model misroutes, leaks, refuses or produces an invalid definition.

## Current product limits

The current runtime is one local owner in one personal world. The finite G1 envelope supports three construction families and at most 64 recipes. AI work is bounded; only an importance-and-urgency supersession may automatically refresh an immediate response, and it can do so once. Narrator generation has no automatic paid retry. Recent immediate-response receipts retain the newest 300 entries; this does not cap actor memories, world events, durable jobs or story history. Audio voice, multiplayer, shared-world synchronization, broad workshop mechanics, deep anatomy, full thermal simulation, arbitrary G2 execution and commerce remain outside the implemented slice.

## Three-program implementation verification

Automated tests are deferred at the user's request; prior table results do not validate these changes. The initial foundation verification used formatting, TypeScript/build checks and native runtime execution without credentials or paid calls. Subsequent PostgreSQL and live Narrator evidence is recorded below; broad browser and long-lived-world acceptance remain open. The production build passed, including runtime TypeScript checking. The full typecheck reports errors in test sources expecting synchronous service APIs; tests were not edited or executed. `pnpm run check` was not run because it executes the prohibited test suite.

An isolated SQLite application ran with explicit empty provider configuration and a zero-dollar cap. Native HTTP calls exercised deer damage/death, generic revival, same-request duplicate revival, optional cognition and private history. Browser inspection showed the Journal's committed effect deltas and top-screen Narrator; the animal retained its deer rendering and physical identity. A subsequent native execution exercised movement, self-talk, group engagement/atomic merge, an explicit promise fulfilled by a later gather event, finite harvesting (the earlier unresolved revival rejection is superseded by full god revival). Closing/reopening SQLite preserved schema 3, the same deer, conversation membership and harvested state. Diagnostic capture drained to SQLite. Backup with journal entries and full restore into an empty SQLite target completed with recovery digest validation. These are small-world native/runtime observations, not automated regression or live-model acceptance.

A synthetic schema-2 conversion was also executed: repeated migration was identical and entity IDs, inventory digest, RNG and simulation time were preserved; the ordinary deer remained native. Persisted diagnostic input contained the redacted synthetic API-key marker. The repository-wide Markdown audit found 54 unavailable relative paths (including sibling AgentCloud references), with no newly broken path introduced by the changed documents. Broad documentation-link repair remains outside this change.

## Remaining-program runtime verification

No test files or suites were added or executed. Runtime TypeScript compilation and the production client build passed. Native HTTP calls exercised deer injury/death, full revival, duplicate request identity and saved voice preferences. Browser inspection confirmed the Journal, smaller committed impact values, voice selector and top-screen Narrator. Native execution then harvested a hare and revived it to its maximum 18 health without changing the harvested inventory; a spoken “I promise to gather berries” acquired a native completion binding and a later gather event fulfilled it. SQLite commit took about 7 ms and recovery about 3 ms in that small isolated save; recovered world digests matched. Full backup and empty-target restore succeeded.

A separate local PostgreSQL database exercised world/effect/history commits, full revival, independent per-agent reservations, rejection above one agent's cap, and restart. Recovery took about 12 ms and matched the world digest. These small runs are not production-load or crash-matrix acceptance.

Two explicit live Narrator revisions used a new fictional deer-only sample through configured Macrofold/OpenRouter with a $0.25 call bound and $1 verification allowance. The first exposed a local JSON Schema dialect mismatch and retained fallback. After matching the existing Draft 7 validator, an explicit new revision published “The deer's body changed. Its health decreased as its injury increased.” Source handles and actual −8 health/+8 injury impacts were retained. Recorded combined cost was $0.000496; the successful call took about 12.9 seconds. Reopening preserved the completed revision. Forgetting its source removed private history and job capsules while preserving the shared event and both paid attempts. This is narrow live connectivity, grounding and publication evidence, not broad naturalness, voice differentiation or long-term social quality acceptance.

## Rebase integration verification

The merged runtime compiled and the production client build passed. An isolated SQLite world with zero spending admitted speech and a private thought while rejecting a stale action plan, persisted explicit conversation departure, drained ordered redacted diagnostics to their completed state, returned owner-scoped history and recovered an identical saved-state digest. No automated suites or paid calls were run. Conversation recall now combines durable association with the newest-32 plus semantic-32 policy; broad recall, supersession and privacy acceptance remains deferred.

## Identity contract runtime observations

The production build completed after the identity-binding and response-contract changes. Disposable direct execution admitted ID-addressed speech, a nod and a private thought; the bound parser rejected a display name and the player role string in a newly seeded world. A disposable SQLite application service projected the controlled actor, produced contextual actions, persisted its binding and reopened with the same ID. These were local runtime observations, not automated-suite or live-model acceptance. No test files or suites were written or run. Legacy fixture updates and migration/privacy regression coverage remain in [TODO](maintainers/TODO.md#identity-and-reference-verification).

## Explicit chat retry runtime observation

With a disposable SQLite save, injected no-network client and zero spending allowance, an initial chat failed and an explicit retry created a linked job retaining the same speech event. Speech count remained one. Repeating the retry request ID returned the same job; a distinct request to retry the superseded failure was rejected. The production build passed. No automated test files/suites or live provider requests were used; successful model completion, tooltip interaction and recovery boundaries remain in maintainer TODO.

### 2026-09-20 — persistence/editor runtime follow-up

Production build passed. Isolated in-memory SQLite execution with an explicit zero AI budget exercised composed append proof, incompatible fork rejection, earlier-prefix edit journal replay, Person summary paging/save/reload, importance-only edits preserving a dependent summary, and a routine milestone/event surviving a control flush. The latter scenarios used disposable synthetic records and the routine acceptance method directly; they do not establish browser, live-provider, native action-loop or PostgreSQL acceptance. An initial synthetic event omitted its required sequence and was rejected; rerunning with a complete event passed. No test files were added or edited and no test suite was run; async fixture migration and regression coverage remain in the maintainer TODO.

### 2026-09-21 — live Person editor fields

Production build passed. An isolated in-memory SQLite run with an explicit zero AI budget loaded the default resident through the Person editor, saved description, personality, backstory and two current goals, reloaded durable state, and confirmed that the first goal drove the native planning field, all goals and authored identity fields appeared in the protected identity document, and creation-time initial goals were unchanged. This does not establish browser layout or live-provider behavior. No test files were added and no test suite was run; deferred schema/context regression coverage is listed in the maintainer TODO.

The same production build and an additional isolated SQLite run verified Person-editor stat snapshots and save merging: a simulated fullness change after opening survived an unrelated description save, while an explicit fill after later energy drift persisted health/fullness/energy as 100/100/100. This does not establish browser interaction or visual layout. Automated stat-editor coverage remains deferred in the maintainer TODO.

## Selective story mechanism runtime evidence

An isolated native SQLite execution with no provider requests produced no job/banner for ordinary encounters, admitted a designated hare introduction, and published its permitted fallback. A separate native body-effect transition selected two related effects into one passage under an explicit consequential rule. Replacing the selector with `silent_v1` revoked admitted work: dispatch eligibility and fallback publication returned false and no banner appeared. Owner-service execution rejected an out-of-range field and a stale revision, and reloading the saved snapshot retained the valid importance value.

The production app ran against a temporary save with zero AI budget and no credentials. Manual browser inspection showed ordinary encounter memories without a Narrator banner, and the new World Events story tab loaded the saved policy. HTTP execution exercised policy retrieval, saving and stale edits; its initial generic invalid-value error was corrected to an actionable admission response and rerun through the owner service. This is native/manual evidence, not complete privacy, migration, concurrency or live prose-quality acceptance.

Production build passes with existing PlayCanvas browser-externalization and bundle-size warnings. Full typecheck has 132 diagnostics, identical to unchanged HEAD in an isolated checkout with the same dependencies. No automated tests were written or run, and `pnpm run check` was intentionally not run because it invokes the test suite. Deferred cases are in [maintainer TODO](maintainers/TODO.md#story-selection-deferred-validation).

## World Agent rejection and retry runtime observations

Disposable, injected no-network executions preserved a typed `execution_disabled` rejection, admitted a subsequent successful fixture operation under a fresh idempotency key, and blocked a second dispatch after a simulated lost connection. A disposable SQLite World Agent message failed with execution-disabled feedback, completed through a linked retry, replayed that retry without another dispatch, and rejected a second distinct retry of the superseded message. These are runtime fixture observations, not live Macrofold/browser acceptance. No automated test files or suites were written/run. Older ambiguous journals and actual provider execution enablement remain unverified.

## Intelligence diagnostic projection runtime observation

The production build passed. The application then ran against a disposable SQLite save with god mode enabled, a zero-dollar AI budget and blank provider credentials. A local World Agent message failed before provider dispatch and its owner-only detail endpoint returned the submitted request ID, `Player world-agent message` trigger type, concise player message, `full-harness` route, failed status, `unavailable` disposition and actionable configuration error. This validates the root-only failure capture path without a paid or external request. No automated test files or suites were written or run; typed-trigger, legacy-normalization and browser-layout regression coverage remains in the maintainer TODO.

The production build also passed after separating query-embedding facts from memory-context ranked results and restoring response-linked actions to Talk history. Read-only inspection of trace `11df3bef-e022-4f12-86b9-ea0c54391d41` found accepted speech, the committed expression event “Deer smiles toward Mike.” and an accepted private thought. Its Jev reflection answer selected yes at 0.72 probability but had 0.44 confidence, below the runtime's 0.55 reflection gate, so no reflection job was enqueued. The running owner-authorized history endpoint then returned the smile as an ordered `action` immediately after the corresponding speech. No automated test files or suites were written or run; pagination/restart/browser coverage remains deferred.

## Manual save/load runtime

Paused movement feedback: reproduced “Reconnect to the world” by clicking empty ground in the connected, paused restored world on isolated port 3298. After the correction and a successful production build, the same click displayed “Press Play to resume the world.” Pressing Play and clicking the ground moved the character and advanced the clock. Automated regression coverage remains deferred in the maintainer TODO.

Game menu and filesystem follow-up: the production build passed, and the actual browser on isolated port 3298 showed Game immediately below World agent. With a disposable data directory and zero AI spending, saved “Clearing” at Day 1 08:14, advanced the world, then loaded that file-backed save and observed the earlier time, food 73%, energy 85% and paused state. Saved the loaded game as “Clearing continued”; both manual saves and the pre-load recovery slot appeared in the list. Filesystem inspection confirmed separate `metadata.json` and `world.json` files for both manual saves. `git check-ignore` confirmed `.data/saves/` contents are excluded. No automated tests were written or run; filesystem failure and broader adapter coverage remain in the maintainer TODO.

2026-09-21: production TypeScript/client build passed. An isolated server on loopback port 3298 used disposable SQLite data, no configured live AI and a zero spending cap. Through the actual browser UI, created “Before exploring,” advanced from about Day 1 08:14 to 08:31, then loaded the slot. The clock returned to 08:14, food returned from 70% to 73%, energy returned from 84% to 85%, and the world reopened paused. This is scoped native runtime evidence, not automated, PostgreSQL, provider-cancellation, crash-injection or scale qualification. A save-list clock-label offset found during the walkthrough was corrected to use the existing `EventTime` component.

After rebuilding and restarting the isolated server against the same disposable database, both slots remained listed with matching game-clock labels. Loading “Before last load” restored Day 1 08:38, food 69% and energy 84%, paused. This exercised the final bulk history restore path through the browser.

No automated tests were written or run, as requested. Deferred coverage is recorded in [the maintainer TODO](maintainers/TODO.md#manual-saveload-deferred-validation). A full TypeScript invocation exposed existing test-file async API type errors; the production build excludes those test sources.

### Reusable native stress runner

Executed the committed gem and mixed scenario shapes on Node 22.23.2 with zero warm-up and 180 measured native steps. The 500-gem fixture had 512 total entities, took 2,007 ms, and ended with 1,022 events / 2,044 awareness entries (step p50 4.35 ms, p95 6.10 ms). The mixed fixture added ten people, twenty deer and three hundred objects; it took 29,411 ms and ended with 4,102 events / 49,178 awareness entries (p50 3.96 ms, p95 5.43 ms). Total time includes initial encounter bursts that the percentile samples can obscure. These are synthetic native-only observations, not real-time server or provider capacity claims. A separate one-second deadline terminated a crowded setup with exit 124 and an explicit incomplete-run message. No live saves, provider calls or automated tests were involved. Usage and interpretation belong to the [profiling guide](maintainers/performance-profiling.md).

## Extensible attribute runtime

**Scope:** native implementation/runtime evidence for the attribute and coarse-contact slices, based on checkout `dd40ad2` (the supplied audit used earlier commit `4d808e5`). These observations do not complete EW-R2/EW-R3, AG plans, reusable constructs, conversational authoring, general EPR intake or population-scale acceptance. All runs used disposable local worlds, injected native data and a zero spending cap; no provider was called. No automated tests were written or run, as explicitly requested by the owner. Deferred coverage is in [TODO](maintainers/TODO.md#extensible-attribute-foundation--deferred-automated-validation).

Production TypeScript checking and `pnpm run build` passed. The build retains existing PlayCanvas worker/chunk-size warnings. Full `pnpm run check` was deliberately not run because it invokes test suites. The untouched `dd40ad2` source tree already reports 132 full-typecheck diagnostics, primarily tests calling now-asynchronous APIs synchronously; the current full typecheck reports 135 diagnostics, including three additional untouched fixture assumptions that fullness/energy always exist. Those fixture adaptations are deferred with the tests. Production compilation is a narrower result, not a green full check.

### Native and local service observations

- A seed-73 wilderness world ran 600 one-second native steps after a movement command to `(12,13)`. Compared with the archived checkout, actor position/health/fullness/energy/actions, item state, event type/text/audience/order/time and final RNG matched. Three paired runs in one process measured baseline 419.95/438.58/414.30 ms and current 432.00/424.06/417.98 ms. Earlier isolated timings were not comparable. These short runs are not a performance qualification or a claimed speedup.
- The charge preset supplies a 0–240 reservoir and categorical disposition while omitting fullness, energy and sleep from the two selected constructs. Actual timed replenishment increased charge and reduced the finite capacitor supply; the resident also completed a native replenishment without a model call. Cancellation retained prior transfers. Two actors contending for the final three supply units received two and one respectively; the source reached zero and no quantity was duplicated. Default body/need rules stayed native.
- Private concern onset was visible only to the owner and excluded from neighboring observations. Same-version JSON capture during an active replenishment followed by 25 seconds of native execution matched uninterrupted execution. Duplicate command admission did not start another action. A definition revision during dependent work was rejected; after cancellation, a presentation-only revision succeeded. Referenced-definition removal and unknown host interfaces were rejected.
- Definition-only transfer into a second native world initialized its own default charge/disposition; it did not copy the first world's depleted charge or private state. This is attribute reuse, not a portable construct pack or EX10 acceptance.
- A fresh SQLite `WorldService` accepted a god disposition edit from cautious to curious, retained revision 1 on replay, and rejected a stale restore generation. The check used an isolated data directory and no network.
- In the touch preset, a nearby source produced an unidentified private `felt` contact, with zero visible entities and no source-key map in the actor projection. A stationary second step produced no new contact evidence. Directed speech to that resident was rejected as not heard; aloud speech succeeded for normal listeners but never entered its awareness. A distant movement request was rejected; a short probe was accepted. Same-version capture and two-second continuation matched uninterrupted state; contact separation emitted an end notice. A further run changed a present contact to moving without a new source identity, preserved its episode across same-version continuation, exposed no contact evidence to the neighbor, and rejected missing sense pins. Broader tactile navigation and material/identity recognition are not implemented.

### Browser walkthrough

A disposable SQLite server on port 3218 used `reservoir-demo`, god inspection enabled, and `AI_BUDGET_USD=0`. Through the actual browser UI: the condition panel showed Health, Charge on its real 0–240 range and Disposition, with no Food/Energy meters. Opening **In view → Charged capacitor → Recharge** started timed native work; charge rose while source supply fell. Pausing, saving **Reservoir runtime check**, resuming until charge changed, then **Game → Load → Confirm load** restored the saved charge, simulation time and pause state. The final build was checked again on port 3219 with a fresh directory: **Final manifest runtime** restored charge 234.3, Day 1 08:39 and the paused state after charge fell to 222.1. This establishes native UI behavior, not live-model authoring quality.

To reproduce on a fresh directory without reading `.env`:

```sh
OPEN_LEGEND_DATA_DIR=/tmp/openlegend-reservoir-demo OPEN_LEGEND_WORLD_PRESET=reservoir-demo OPEN_LEGEND_GOD_MODE=true AI_BUDGET_USD=0 PORT=3218 node --import tsx apps/server/src/main.ts
```

Open `http://127.0.0.1:3218`, resume play, follow the browser steps above and compare the restored quantities. Use a new directory if it contains an incompatible development world. For the native touch scenario change the preset to `touch-demo` and select another fresh directory; the player stays sighted, so do not mistake the player's view or god inspection for the resident's knowledge. Sense definitions, contact episodes and pending replenishment are part of the current saved manifest/state; older development schemas are rejected without migration.

## Actor agency runtime

This slice was manually executed against the schema-5 development format using disposable worlds, explicit zero spending and no external provider calls. No automated tests were written or run; the deferred matrix is in [TODO](maintainers/TODO.md#deferred-agency-validation). Production compilation/client build and repository formatting passed. The full `check` command was not run because it invokes tests; older response/goal fixtures also need the intentional contract cutover.

- A native decision admitted a private goal, a three-step gather → prepare fiber → make cord frontier and a separate thought. All three steps completed over actual simulation time without another decision. A JSON round trip during the running gather preserved continuation. Empty output retained the same native action ID.
- Missing materials blocked the first step and left its successor undispatched. Another 100 simulated seconds added no repeated failure memory. Duplicate aliases rejected the envelope with the original world unchanged. Two speeches survived an independently rejected hidden-target gesture. Stale frontier revisions rejected replacement. Goal-parent cycles rejected; subjective completion recorded `actor-declared` without changing physical state.
- Two actors contested a one-unit reed supply. The first received one raw fiber; the second received zero and its frontier blocked. Preparing cord consumed two prepared fibers at work start; cancellation retained the zero remaining balance and the goal. Lethal body effects changed a running frontier to blocked instead of leaving a permanently running receipt. Quadruped manual-work/gesture proposals were rejected by the finite body policy.
- Local schema serialization produced a strict object with an operations array and no `oneOf`. Empty immediate output and reflection with an empty typed goal-change list parsed locally. This does not establish live-provider schema compatibility. Unmatched prose produced a private `needs-interpretation` record, not a physical effect or an invention.
- A hungry actor with no carried food or nonempty berry source had no native-protection block. Once it began gathering available berries, native protection applied. A pending fiber preparation matched perceived reeds without an existing selected-object subscription. The production scheduler retains deferred evidence; paid behavior and fairness were not exercised.
- A disposable SQLite service on port 3221 retained a running gather across both an interrupted process and clean restart. Advancing 600 simulated seconds completed the frontier. Manual save/load restored the earlier running/queued dispositions. Removing the hot response receipt and replaying its completed durable job returned `retired-response` without effects. A private sentinel goal did not appear in the public `GameView`.
- The production browser rendered the restored resident as **Gathering**, not as having completed the plan. The god Person editor displayed the consolidated private goals and saved an explicit replacement, returning **All changes saved**. The temporary tab and server were closed afterward. The first save attempt used an invalid scratch identifier and was rejected; the successful walkthrough used a UUID slot.
- Three paired 600-second native runs against the pre-slice `089eb93` checkout matched physical entities, items, events, RNG, time and native IDs after excluding the replaced goal representation. Baseline/current wall times were 42/43 ms, 37/47 ms and 51/59 ms. These small noisy samples show no speedup and do not qualify larger populations; representative profiling remains deferred before adding caches or scheduling machinery.

The subsequent bounded live observations below qualify only the cases actually exercised. Broad optionality, adaptation, semantic interpretation, actor-led invention and cost at sustained play rates remain unverified. The current implementation and deliberate limitations are described in [Architecture](architecture.md#actor-agency-foundation).

### Live agency contracts

September 22, 2026: the owner's standing authorization in `AGENTS.md` allowed up to $10 total for this task. Each walkthrough used a newly created temporary directory and repository-seeded fictional actors, never an existing user save. Each actor had a $2 allowance, each Macrofold model call a $0.25 cap; reflection compute was separately bounded at $0.25. No automatic simulation, autonomous director loop or embeddings ran. This was explicit manual live execution, not an automated test suite.

The real director's immediate route used Jev `typesafe/jev-1.13-20260917` for attention, routing and optional action suggestions, followed by Macrofold/OpenRouter `openai/gpt-5-mini` at level 2/low. Addressed speech asked for reeds and fiber preparation while talking. The model returned independent speech, thought and a two-step gather/prepare plan using supplied native handles. Job `94ea57f3-50ef-4dab-8d91-1b27853336ba` committed all three components in 8,721 ms. Its three Jev calls and one generation cost $0.001853 in the durable micro-dollar ledger. After closing and reopening the same disposable SQLite save with all provider keys blank and a zero allowance, 600 simulated seconds completed both steps; prepared fiber increased from two to four and call counts/spending stayed unchanged. This establishes one useful model-selected sequence with native continuation, not general autonomous planning quality.

The first walkthrough returned the same valid two-step structure, but its manual driver let browser presence expire before commit. Job `52ca7432-f8a4-4576-b3ec-e51a855593c3` correctly waited for resume and the standalone process exited with an unsettled await. Its $0.001901 remains counted; it is not a completed-response success. The driver was corrected to send presence heartbeats before the separate run above. The failed run's provider generation receipt was `01a0c9cf-a27e-76df-985e-272dc8237d84` (7,395 ms); the failure did not trigger a retry in the application.

A separate reflection call used the actual scoped workspace backend, reflection schema and production instructions on another newly seeded fictional world. Configured model `meta/muse-spark-1.3-contributor` / harness `opencode` failed with provider code `harness_error`, verified persistence and empty output. Run `01a0c9d2-be1d-710d-aa97-d86d057f744b` took 167,445 ms end to end (118,218 ms reported execution, 767 ms queue); its event stream contained no tool-start or model-output evidence. No files, thoughts or goals were published. This does **not** qualify the new reflection schema or PostgreSQL maintenance scheduling. The provider supplied no model usage, so the $0.25 reservation remains conservatively consumed; its Jev attention cost $0.000126. Compute reported an authoritative zero rate/cost, and the disposable sandbox was confirmed destroyed after completion. There was no paid retry. Full reflection acceptance is blocked on the external harness.

This failure exposed an application diagnostic problem: a verified failed run without billing was labeled uncertain and its provider failure code was discarded. The adapter now preserves the known failure and reports the code/run ID while retaining unknown charges. A manual in-process replay of the observed terminal response shape, with an injected no-network transport and disposable SQLite, returned `failed`, `completionUncertain: false`, `harness_error` and the run identity; settlement still consumed its $0.25 **fixture** reservation. That synthetic amount is not provider spending. No automated test file or suite was added or executed.

Combined live accounting for the three walkthroughs is **$0.003880 reported/rounded spend plus $0.250000 unknown model cost reserved**, or **$0.253880 conservatively counted** against the $10 task allowance. Provider invoices remain external. Durable receipts are in the disposable SQLite ledgers; broad quality, no-response optionality, goal revision, adversarial behavior and sustained-rate costs remain AG12 work.

The production TypeScript/client build and formatting check passed after the diagnostic fix; the documentation link scan found zero issues across 104 Markdown files. The full `check` command was skipped because it runs the prohibited automated test suite.

### Native attempt resolution

September 22, 2026: explicit manual execution of the actual director with injected no-network choices/generation admitted a freeform gather → prepare composition while the optional action-suggestion gate was closed. One interpretation was used. SQLite close/reopen during gathering retained the queued preparation; 600 native simulated seconds completed both. An unresolved fictional teleport request was retained privately; repeating it in another response did not interpret it again. Withdrawing its supplied private handle preserved the same running gather action ID. An unavailable interpreter and a forged choice handle each preserved accepted speech and deferred the intent without physical effects. Replaying the completed response ID dispatched no generation. All these injected cases had zero external calls/spending; no automated test file or suite was added or executed.

One separately capped **ordinary inference** call (no Macrofold harness or workspace) exercised four proposals on fresh fictional seed data. Macrofold/OpenRouter `openai/gpt-5-mini`, low effort, returned gather → prepare for one paraphrase and the same gather binding for another. Gold theft and teleportation returned unresolved. The three admitted native steps then completed in 600 simulated seconds; the two unsupported proposals remained private. Run `01a0c9ec-0ba9-749f-a3ad-560c1404cad2` took 6,858 ms and reported $0.00100675 ($0.001007 rounded ledger), under its $0.10 call cap. This is a small semantic-contract sample, not general method-fidelity or autonomous-invention acceptance. No paid continuation or repair occurred.

Subsequent native execution checked the richer batch/time descriptions and duplicate-binding removal: exactly one River reeds binding remained, copying its description required no interpreter and its work completed with zero allowance. The batch size is shared with actual gather execution. Interpretation remains limited to supplied known mechanics; future-item references, neutral clarification, broad paraphrase equivalence and actor-led invention are unqualified.

### Invention policy runtime

September 22, 2026: manual execution on fresh SQLite with an explicit no-network AI fixture confirmed that a player lock rejects a request before judgment/generation (zero calls), stale settings reject, and NPC unlock rejects pending qualification. An admitted fixture request was held during generation; locking then reopening before releasing its result cancelled the job and installed no recipe. The saved job retained its original inventor/origin/revision. SQLite close/reopen retained both settings and the revocation marker; the old origin stamp remained revoked. Domain execution separately confirmed that a player lock/reopen does not revoke an otherwise-permitted agent-origin stamp.

A fresh native fixture recipe admitted while open, new admission was rejected while locked, and ordinary crafting of the existing recipe still completed after native simulation. Owner attribute, cognition and changed story-definition edits rejected under the player lock. Current schema validation passed; a schema-5 development world was refused. A named save restored its captured lock settings after intervening changes, changed the restore generation and rejected an owner edit bound to the prior generation. This is native/fixture evidence, not live autonomous-invention qualification.

A zero-credential, zero-allowance local production browser walkthrough displayed both settings, saved the player lock, left the NPC lock on/unavailable, and preserved the saved player setting after reload. No automated test file or suite was written or run for this slice. No additional paid calls were made; the ongoing task total is $0.004887 reported/rounded plus $0.250000 reserved for the earlier unknown harness cost, or **$0.254887 conservatively counted** against $10.

The production TypeScript/client build and formatting checks passed for native attempt resolution and invention policy. A repository Markdown link/anchor scan found zero issues across 104 files. The full `check` command was skipped because it includes the prohibited automated test suite.

### Invention attribution runtime

September 22, 2026: manual zero-network execution used a disposable schema-7 world with two fictional creator accounts and one separate player account. An NPC invention recorded both creators as owners and `npcCreated: true`; an independent player invention with identical mechanics retained its own receipt, marked player-created and owned by the player plus both creators, while the executable recipe count remained one. Same-request replay reused the receipt; changing its inventor rejected with an idempotency conflict. A player who was also a creator appeared only once in the owner list. Missing player account binding rejected admission.

A second NPC recipe remained unknown to the player until native teaching. Teaching preserved NPC ownership. The normal player view exposed only creation flags over known recipes and contained none of the fictional account IDs. SQLite close/reopen and named save/load preserved the separate attribution records; removing a recipe's original creation receipt caused validation to reject the save. A zero-credential production browser walkthrough displayed both labels and filtered separately to the single NPC-created and player-created recipes. All three world-creation presets preserved supplied creator/player accounts; empty creators rejected and later caller mutation did not alter the saved creator list.

These are explicit native fixtures, not live generation or autonomous-invention qualification. No paid calls, automatic retry, automated test file or test suite were used. Production TypeScript/client build, formatting and documentation-link checks passed. Cumulative conservative spending remains $0.254887 against the $10 task allowance, including the prior $0.25 unknown harness reservation.

## Shared inventions and plan outputs

Manual execution on 2026-09-22 used disposable SQLite worlds and the actual director, repository, domain transitions, HTTP server and production browser client. No automated tests were written or run. Production compilation/client build and formatting checks passed; existing PlayCanvas externalization/chunk-size warnings remain.

- A labeled fixture invention took one routing and one generation call through the extracted pipeline. Re-delivering its request ID returned the saved result with two total calls. Candidate JSON/digest and recipe ID persisted; another actor's history query returned no records.
- A response queued craft followed by equip of the first step's output. The director and store closed with craft running; a fresh service loaded that SQLite state and completed both steps. The completion receipt and equipped item were exactly `item-39`; no further provider calls occurred. A forward reference was rejected. Startup validated schema-8 state.
- An interrupted saved candidate remained inspectable with a stale/interrupted result. A separate reserved provider attempt recovered as uncertain with its full $0.10 fixture reservation retained and no dispatch. Direct admission atomically completed the corresponding durable job.
- Restoring a pre-invention manual save removed the admitted technique while retaining its completed request/candidate. The world had no installed recipe and the saved request's timeline differed; history remained readable. No generation or spend was replayed.
- The browser World agent → Invent view read saved admitted/interrupted records without an existing local conversation transcript. Submitting an explicit request completed the shared workflow and showed the saved proposal and Crafting availability. Draft text survived switching Discuss/Invent modes, and the updated scroll layout kept saved results reachable. This exercised fixture providers, not live model quality.

One capped live request used configured Macrofold ordinary inference, with compute disabled and at most $0.11 reserved across routing/generation. Jev selected the swing family and the model returned **Fiber Pouch Sling**. Local schema traversal initially rejected the otherwise valid response because a data field was named `properties`. The corrected generic walker validated the saved provider response and domain admission reported no errors. Replaying those exact saved provider responses through the director completed admission without another external call. The original live job remains failed; this is live generation plus local saved-response replay evidence, not a fresh end-to-end live pass after the correction. Settled estimated new spend was **$0.000551**, no remaining reservation. The ongoing implementation allowance conservatively totals **$0.255438**, including the earlier $0.25 unpriced harness reservation.

This does not qualify NPC unlock, broad invention quality, the revised plan schema across providers, similarity retrieval, workshop continuation, PostgreSQL or full-harness recovery. Deferred automation and qualification are linked from [TODO](maintainers/TODO.md#deferred-shared-invention-and-output-reference-validation).

## Supported invention loop

Manual execution on 2026-09-22 used an isolated PostgreSQL/pgvector world, the real director and configured Macrofold ordinary inference plus OpenAI embeddings. Autonomous scheduling and paid compute were disabled. All requests shared a $0.75 world-agent allowance within the standing $10 task cap. No automated test file or test suite was written or run.

- A fresh live request admitted **Braided Fiber Stone Sling**. After reopening the store, repeating its request ID returned the saved receipt without another call. Native crafting consumed two prepared fibers and two cords, produced `item-17`, and equipping/hunting hit a hare for 12 damage while consuming one stone.
- A paraphrased pebble/pouch request found that learned recipe with cosine similarity about 0.612. Explicit reuse made no generation call or item. A second child request from the same search result returned `already-continued` with the original child ID.
- A live modification admitted **Light Braided Fiber Stone Sling**, using one prepared fiber and one cord, preserving the original range/damage/accuracy, and retaining the source recipe pin. The original equipped item and recipe remained intact after restart. The first modification attempt exposed an omitted-value context bug (`non_json_input`); that job remains failed/uncertain and its reservation remains conservatively counted. The corrected code succeeded through a separately requested linked revision; no automatic retry occurred.
- The production browser read server-owned history, showed enabled crafting for the light variant and missing-material blockers for the original, and retained a clarification follow-up draft across page reload. Similar inventions could be canceled and reopened without generation. An ambiguous request produced `needs-clarification`; a later clear bow request also abstained because Jev's admissibility confidence was 0.44 despite selecting the flex route. This is an observed provider-quality limitation, not a claim that bows are unsupported or physically impossible. The confidence gate was preserved.

Similarity filtering is a small initial heuristic, not a broad retrieval-quality qualification. These observations do not unlock autonomous invention, certify private NPC method fidelity, qualify full-harness recovery, or complete the full INV-2.6 scenario matrix. Deferred automated coverage stays in [TODO](maintainers/TODO.md#supported-invention-loop--deferred-automated-validation).

The final production browser build reloaded the same saved definitions, item and linked results; a fresh exact-name search returned compact definition pins and explicit Use existing completed with the current missing-cord Craft blocker. No generation or resource consumption occurred on that selection. The walkthrough server and temporary browser tab were closed afterward.

New conservative task spending was **$0.071981**, with no pending reservations/calls. This includes **$0.001981** in priced provider estimates and **$0.070000** retained for successful unpriced embeddings plus the uncertain context-failure attempt. The ongoing implementation allowance totals **$0.327419** when combined with the previous $0.255438, including its earlier unpriced harness reservation. These are application-ledger estimates/reservations, not a provider invoice.

Production TypeScript/client build, formatting and the 104-file Markdown link/anchor check passed. The existing bundle-size warning remains. `pnpm run check` was not run because it includes the prohibited automated suite; existing full-test typing/coverage gaps remain in TODO. No new dependencies, general equipment slots, autonomous invention permission, paid retry loop or full-harness dependency were introduced.

## Spatial world runtime

The spatial branch is based on `12c9cd7318ee3958d5bea006005e1f0aad996547`. Local native verification used Node 22.16.0 and the repository's pinned dependencies, obtained through a read-only source/dependency export. No live AI, embedding, image or model-provider calls were made; new provider spend is $0. The new spatial package adds no third-party dependency.

Twenty-four focused tests cover exact support identities, ramp ascent/descent, conservative swept volume and headroom, same-XZ separate floors, acoustic slab accounting, partial/budget failure, geometry invalidation, finite validation, camera math/preferences, real native gathering on the deck, cross-floor speech privacy, route/flight restoration, dead-flyer landing including rock tops, invalid saved waypoints, unsupported airborne commands and incompatible old worlds. Real HTTP checks cover same-origin authority, strict 3D commands, retry identity and no private flight-route disclosure. A temporary SQLite database and manual save slot preserve mid-ramp progress across service restart and restore, rotate the load epoch, reopen paused and retain native bird state. These are native fixtures, not live autonomous invention evidence.

Production TypeScript compilation and the client Vite build passed. The existing client chunk-size warning remains. The complete legacy test suite is not green: an unchanged baseline run has 125 failed test names and 57 passing tests; the changed run retains those same failing names rather than introducing new failed cases. Many existing tests assume superseded synchronous/domain/provider contracts. The general full-test TypeScript baseline also has 156 existing errors; the production configuration compiles. This branch does not mark those unrelated failures resolved or claim `pnpm run check` passes.

The Playwright scenario in `tests/browser/spatial-world.spec.ts` passed in GitHub Actions run [35803819218](https://github.com/Macrofold/OpenLegend/actions/runs/35803819218) on commit `5b4676c4e612619bd07b36607e225581e5243c90`. The same run passed the production build and all 24 focused tests. The browser clicked the real projected deck surface, observed the native actor ascend to y=3, exercised orbit/projection/floor focus and rotation lock, verified drag-release isolation and persisted projection after reload, and produced mixed sprite/mesh screenshots with no page errors or paid requests. The initial remote attempt failed because the fixture incorrectly tried to resume an already-running fresh world; only that test setup was corrected. Local Chromium navigation remains unavailable under the administrator policy, and no local browser success is claimed. Broader touch/accessibility, context-loss, late generated-model and multi-client cases remain open in SW16. Screenshots are retained in the run's `spatial-browser-evidence` artifact for seven days. The read-only `.github/workflows/spatial-check.yml` preserves this focused gate without replacing the repository's existing full check.

Native-only performance samples on an AMD EPYC 9V74 host used the 28×24 starter map, three authored surfaces and six actors including the bird. Twelve cold route samples had median 30.8 ms and maximum 47.1 ms; 240 warm samples had median 0.26 ms, p95 0.56 ms and maximum 1.25 ms. Twelve 600-step advances had median 57.7 ms and maximum 70.2 ms. These small in-process measurements exclude persistence, network, GPU, rendering and model work; they are not real-time FPS, many-agent capacity or cross-hardware guarantees. They support retaining synchronous cached native navigation for this small initial map rather than introducing workers and navmesh rebuild coordination now.

Two focused code reviews covered authority/persistence/geometry and integration/performance/modularity. Issues found and fixed included the ramp-seam sweep exception, a circular initialization dependency, planar quick-action distance, ground-only candidate planning for a flying target, repeated sprite-alpha readback, mismatched profile checks during spawning, competing airborne physical actions, manual-save format versioning, invalid saved route heights and dead-flyer rock support. Remaining broader limitations are recorded under their existing SW task IDs, not silently labeled complete: dynamic crowd arbitration, arbitrary support loss/flight landing volumes, navmesh/WASM qualification, partial-hearing contacts, validated model loading, hidden-map routing and D51's viewport/multi-view contract.

## Spatial scaling review

Reviewed `feature/spatial-world-3d` at `d12ebc4ae88552cf442906999060499f300dc2e8` and its finite native provider. Two passes traced geometry/navigation/sensory dependencies and renderer/persistence/invalidation behavior. Concrete changes include full-extent static query trees, height-local seam construction, weak connected-component rejection, lazy exact stance checks, range-first hearing, bounded frozen-pose sight reuse, geometry/sense-aware public view invalidation, phase-local landing occupancy, top-only landing contact, private future-route removal and dirty renderer updates. All new automated coverage is deferred to [SR01–SR07](maintainers/TODO.md#spatial-review-regression-todos). No unit, integration or browser test files were added/edited, and no test suites were run during this review. Prior 24-test/browser results above remain evidence of the earlier revision, not automated qualification of these changes.

Production TypeScript (`tsc --noEmit -p tsconfig.build.json`), the production Vite build, changed-file formatting and whitespace review passed. The existing large client-bundle warning remains. This does not claim the full repository check is green. No provider keys were configured; the task made zero AI/embedding/art calls.

Manual runtime observations used Node 22.16.0 on an AMD EPYC 9V74 container with five reported CPUs. Temporary scripts outside the repository invoked the real native modules directly, printed results/timings, and used no test runner. The same workload was run on the source snapshot and the revised tree. The geometry workload retained the 28-by-24 starter map, adding 28 stacked supports (31 total) and enough boxes for 120 blockers including derived terrain rocks, within unchanged admission limits. The boxes were distant from the local query; the stacked planes intentionally stress same-XZ routing and support selection. Numbers are single-process samples, not cross-hardware guarantees.

| Native workload                                                  |    Before |     After | Coverage                                                                                                                                        |
| ---------------------------------------------------------------- | --------: | --------: | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| Cold route onto the existing lookout, median                     |  3,841 ms |    160 ms | Three cold map copies; after maximum 173 ms. Includes complete graph preparation.                                                               |
| Disconnected upper-floor route, median                           |   1.39 ms |   0.20 ms | 100 calls, including one initial cold sample; after warm p95 about 0.30 ms. The series maximum was a 142 ms cold preparation, not a warm query. |
| Local clear ray, median                                          | 0.0148 ms | 0.0032 ms | 3,000 calls; broad-phase pruning of distant geometry, not a worst-case ray crossing every floor.                                                |
| Distant hearing rejection, median                                | 0.0030 ms | 0.0004 ms | 3,000 calls; sound outside hearing range, no need to compute barrier transmission.                                                              |
| 250 added resource entities, later one-second advances, median   |  11.48 ms |   2.94 ms | Five steady-state samples after the first exposure; 264 total entities, not 264 intelligent actors.                                             |
| 180 additional airborne birds, later one-second advances, median |  17.86 ms |   8.17 ms | Five samples after first exposure; 194 total entities. This exercises native motion, not hundreds of LLM controllers or contested landings.     |

The first object-exposure advance still took about **247 ms** and created 524 events; the first airborne-population advance took about **167 ms** and created 384 events. These costs include existing event/awareness fan-out and are explicitly not fixed by a spatial index. The cold query and exposure bursts are recorded in SW06.2a/SW08.4a, with EPR/PF integration retained. Shape/tree caching, connected components and per-observer memoization do not make genuinely dense interactions sublinear. The sight cache caps retained entries at 128 observers × 512 targets per retained map; eviction changes computation, not evidence. These measurements exclude persistence, network, rendering/GPU work and paid cognition. No FPS, unlimited world capacity or long-session memory result is claimed.

The built application was also started on loopback port 3218 with an isolated `/mnt/data/spatial-review-live` SQLite directory and zero AI budget, using the ordinary production entrypoint. Actual authenticated HTTP commands moved the player from ground onto `lookout-deck` at `{x:24,y:3,z:6}`, completed the native action, rejected a destination with that height but the `terrain` support, and routed back underneath to `{x:20,y:0,z:5}`. The served page returned HTTP 200. After a clean shutdown/restart, that lower position/support remained and the world reopened paused. A separate direct runtime inspection reported an occupied landing footprint at a living actor, no overlap ten metres above it, a blocked flight up through the deck underside, and a clear descent onto its top. These are manual observations, not a comprehensive invariant suite. Both application processes were stopped; no existing user save was touched.

Current renderer changes were compiled and reviewed but were **not** re-exercised in Chromium in this pass. Full crowd avoidance, arbitrary support loss, repeated landing fairness, hidden-map navigation, graded auditory contacts, generated-model delivery, viewport authorization and sustained client/server load remain unimplemented or unqualified as individually identified by the SW delivery ledger. Temporary source/publication workflows are removed on the branch; the review commit skips automatic test jobs without permanently disabling the normal CI workflows.

## Spatial scaling review second pass

The second pass began at `0c137e5705844d1a37c2fc724ef8987b0a374f5c` on `feature/spatial-world-3d`. During the review the branch was rebased to `7bba4866819e5623fc4e42d6f8d82f96ca25877d`; the changes were reconciled onto that tree without discarding the parallel invention work. The primary before/after table uses that rebased baseline and its reviewed working tree. Production build, native observations and the HTTP/restart exercise were repeated after reconciliation. It changes six native source files: static bounds/geometry/navigation, experience admission and encounter emission/calling code. No test files were added or edited and no automated test suites were executed. New automated regression work is SR08–SR10 in [maintainer TODO](maintainers/TODO.md#spatial-review-regression-todos), alongside the still-valid SR01–SR07 cases. Earlier unit/browser results above do not qualify this revision.

Two source-review passes covered invalidation/ownership and path/collision/event behavior. Geometry now partitions stacked slabs by center variation and short-circuits boolean stance/sweep work without changing ordered acoustic crossings. Navigation conservatively groups potentially connected supports before building a graph, retains directional checks for tolerance-height seams, reduces exact-coincident zero-cost cliques to stars, reuses reversible same-surface checks and uses generation-stamped bounded search storage. Shared-endpoint support changes validate both native legs before avoiding graph construction. Experience ID membership is scoped to the authoritative mutation owner's draft array and invalidated on non-add changes; raw mutable builders retain live lookup. Audience reuse is confined to the synchronous fixed-position encounter phase. Existing per-event ledger, awareness, commitment and RNG behavior remains in its original mutation path. Neither cache is authoritative or persisted.

Production TypeScript (`tsc --noEmit -p tsconfig.build.json`) and the Vite production build passed. Changed-file formatting and diff whitespace checks passed. The existing client bundle-size warning remains; no full-check or current browser success is claimed. No external providers were configured or called.

Ad-hoc runtime diagnostics outside the repository ran the real native modules using Node 22.16.0 on the same AMD EPYC 9V74 container for the baseline and revised copies. Both reported runs below omitted CPU-profiler instrumentation; a separate preliminary profile was used only to identify hot functions. The stack workload used the 28×24 starter map, 28 additional 22×19 grid-point support patches at distinct heights (31 surfaces total), and 120 combined explicit/terrain blockers. Distant blockers intentionally exercise pruning. Admission limits were not raised. The disconnected upper floors cannot contribute to the lookout route, but the previous provider built their complete grids anyway.

| Native workload                                                                     |           Before |            After | Observation scope                                                                                                  |
| ----------------------------------------------------------------------------------- | ---------------: | ---------------: | ------------------------------------------------------------------------------------------------------------------ |
| Cold lookout route in the large stacked layout, median                              |        629.46 ms |         39.33 ms | Five fresh maps; revised maximum 53.45 ms. Same reached endpoint, 21 path points, 125 expansions and route length. |
| Warm route in that layout, median / p95                                             | 0.324 / 0.649 ms | 0.291 / 0.686 ms | 500 calls; small differences should not be treated as a guaranteed speedup.                                        |
| First one-second advance with 250 extra resource objects                            |        239.01 ms |         58.93 ms | Median of three worlds; 264 entities and 524 emitted events, unchanged counts.                                     |
| First one-second advance with 180 extra airborne birds                              |        163.81 ms |         43.85 ms | Median of three worlds; 194 entities and 384 emitted events; birds use native controllers, not LLMs.               |
| First resource-exposure advance with 2,000 prior awareness entries per memory actor |        865.57 ms |         53.40 ms | Median of three worlds; same 524 event count. The large gain removes repeated history scans, not records.          |

Later one-second advances in those three workloads had baseline/revised medians of 2.64/2.54 ms (resources), 7.07/7.27 ms (birds) and 2.12/2.22 ms (history), over seven later advances in each of three worlds. The main demonstrated improvement is burst/cold work, not a demonstrated steady-state population increase. The reconciled warm-route series contained one 74.17 ms outlier (baseline maximum 3.52 ms); no cause was established. Low medians do not qualify tail latency, and this remains part of sustained-runtime SR07/SW14 work. All measurements include native finalization/freezing and exclude persistence, networking, GPU/rendering and paid cognition. They do not establish sustained memory usage, interactive FPS or maximum actor capacity.

An additional diagnostic on the initial, pre-rebase review pair used 28 exactly co-located large supports at Y=8. The unobstructed cross-support query fell from about 20.83 seconds to 1.02 ms by validating a direct two-leg route instead of baking redundant topology. Its path is intentionally shorter (23.60 rather than 32.41 metres), so this is not a claim about accelerating an identical chosen path. Adding a wall to block the shortcut still took about 2.81 seconds cold, versus 18.47 seconds before; the printed path and cost matched in that blocked example. This remains a concrete cold-build weakness, not a solved scale case. SW06.2a retains preparation/representation qualification before denser authored content or higher limits are enabled. The source-group comparison is bounded quadratic work over admitted surfaces, not an unlimited-world algorithm.

Printed runtime observations also covered forward/reverse ramp routes, a budget-exceeded query followed by a valid query using the same search scratch, the small-body profile and a wrong-floor endpoint. Their outputs matched the baseline for the sampled ordinary routes. A separate 80-object exposure printed identical event and per-actor awareness digests before/after. Direct experience mutations retained duplicate rejection, forgotten-source rejection after deletion, editable-text behavior, mixed-batch rollback and independent draft-fork additions. These ad-hoc observations are not an automated invariant suite or a proof of floating-point equivalence for every shape.

The built application initially ran on loopback port 3219 using an isolated `/mnt/data/spatial-round2-live` SQLite directory, and the reconciled tree repeated the exercise on port 3220 with `/mnt/data/spatial-round2-reconciled-live` and zero provider budget. The ordinary page and authenticated state endpoint returned HTTP 200. Normal HTTP commands moved the actor onto `lookout-deck` at `{x:24,y:3,z:6}`; a resumed-world command with that height and `terrain` support was rejected; the actor then returned underneath to `{x:20,y:0,z:5}` and completed its action. Away-presence pausing was observed and respected rather than bypassed. A clean application restart retained the lower position and reopened paused. All application processes were stopped; no existing user save was touched. This exercises native/application integration, not a new Chromium gameplay run.

Scope remains important: EPR03's actor-private acquisition is pending. The current encounter mechanism can broadcast an observer's acquisition around that observer; this review preserves that behavior and only avoids redundant computation. It must not be relied on to keep an unseen target private. Correcting that scope also reduces inappropriate witness fan-out, but needs the EPR-owned acquisition/living-encounter/narration integration rather than silently changing audience semantics inside a cache. General moving-crowd avoidance, heavy connected navigation preparation, hidden-map routing, graded sound contacts, viewport authorization and sustained full-stack qualification remain open in their existing owners.

## Spatial scaling review third pass

This pass reviewed `feature/spatial-world-3d` at `7291628d7f0b1305218a9eb765748313a5d2db5b`; `ce7d9d1900c7852073dc46582544a2841afc58ea` only exported that source with test jobs skipped. Two source-review passes covered the native participant/draft boundary, observation-copy ownership and lazy navigation/negative reachability. Only `packages/spatial/src/navigation.ts` and `packages/domain/src/kernel.ts` change runtime behavior. No test files were written or edited and no automated unit, integration or browser suites were run. SR11/SR12 and the extended SR05 in [maintainer TODO](maintainers/TODO.md#spatial-review-regression-todos) retain required automated qualification. Earlier automated evidence does not certify this revision.

Exact graph clearance/edges are now evaluated on demand over bounded lattice metadata. A connector-aware admissible cardinal-grid heuristic and deterministic progress tie-break avoid exploring entire equal-cost plateaus. Four bounded closed-region proofs, recorded only after complete failed searches, preserve cheap repeated negative queries without eagerly checking the whole graph. Native stepping reuses sorted active IDs within an advance, refreshing after a nested command changes the draft; inert entities do not take part in each second's physical loop. Actor observations are sanitized before their one final isolated deep copy. No sensory sampling cadence, native time step, event scope, geometry admission limit or saved route was rewritten to obtain a speedup.

Production TypeScript (`tsc --noEmit -p tsconfig.build.json`), Vite production build, changed-file formatting and diff whitespace checks passed. The existing large client-bundle warning remains. No external provider was configured/called. The server was started with an empty environment except PATH/HOME and explicit local configuration, zero AI budget and a fresh isolated SQLite directory. This is not a claim that the full repository check or current browser acceptance is green.

### Matched native diagnostics

Ad-hoc scripts outside the repository invoked the actual native modules with Node 22.16.0 on an AMD EPYC 9V74 container exposing five CPUs. Source-baseline and revised runs used the same seed 73 and identical diagnostics, without a test runner or CPU profiler in the timed series. Timings include native freezing where the workload advances the world; they exclude database commits, network, renderer/GPU and paid cognition. These workloads differ from earlier rounds; do not compare timings across their different maps or blocker counts.

The route layout is the 28-by-24 starter map plus 28 rectangular patches with 24-by-19 lattice points, giving 31 authored surfaces (plus eight derived rock-top supports). In one variant the extra patches occupy distinct heights; the other places them all at Y=8 and adds a detour wall. There are 11 original explicit/terrain blockers, or 12 with that wall, not the earlier round's 120-blocker workload. Three new maps supply cold samples; the last supplies 100 warm samples. Each sampled successful route retains its endpoint and length; the stacked route has 21 points and the detour 35.

| Native workload                                                     |                 Before |             After | Qualification                                                                                              |
| ------------------------------------------------------------------- | ---------------------: | ----------------: | ---------------------------------------------------------------------------------------------------------- |
| Cold lookout route with disconnected upper layers, median / maximum |       22.09 / 33.07 ms |   8.75 / 13.66 ms | Same 20.1224-metre route length; 125 versus 93 expansions.                                                 |
| Cold detour among 28 co-located patches, median / maximum           | 3,075.49 / 3,124.53 ms | 94.91 / 102.12 ms | Same 34.4142-metre length; 5,677 versus 53 expansions.                                                     |
| Warm lookout route, median / p95                                    |       0.291 / 0.504 ms |  0.337 / 0.547 ms | Slightly slower in this series; the new heuristic is not a universal warm-query improvement.               |
| Warm co-located detour, median / p95                                |       3.717 / 6.901 ms |  1.542 / 1.840 ms | Same map after cold preparation.                                                                           |
| 250 added resource objects, one subsequent 60-second native advance |               49.19 ms |           4.95 ms | Median of three worlds; normal memory-capable actors remain enabled.                                       |
| 180 added birds, one subsequent 60-second native advance            |              169.67 ms |         143.50 ms | Native bird controllers, not 180 intelligent/LLM actors.                                                   |
| 5,000 inert resources, one subsequent 60-second native advance      |              716.87 ms |           9.45 ms | Memory disabled for all actors to isolate physical-loop cost; not a normal fully sensing population claim. |
| 3,000 visible objects, actor-observation assembly median            |               30.59 ms |          19.69 ms | Fifteen calls in each series; permitted-output digest and copy isolation matched.                          |

Each population diagnostic creates three worlds, runs one first-exposure second, five later individual seconds, then one 60-second call. Event counts after first exposure remain 524 for the 250-object case, 384 for the 180-bird case and zero for the memory-disabled case; final printed RNG and simulation-time values also match. First-exposure medians are 63.50/60.88 ms (objects), 47.29/49.21 ms (birds) and 36.80/6.46 ms (memory-disabled). Later individual-second medians are 2.943/2.080 ms, 7.278/7.958 ms and 34.171/6.591 ms respectively. The steadily moving/sensing bird case is not demonstrably faster per short call; the main roster gain amortizes inert work within a multi-second call. The application still owns its existing fixed-step commit/exposure boundaries: these numbers do not authorize replacing sixty application transitions with one 60-second transition.

The observation series also used 250 visible objects: medians 3.34/1.95 ms. Printed source/observation digests and deliberate mutations of returned nested resource data and the returned actor's goals showed isolated output with unchanged source state in both variants. This does not replace SR05's candidate-draft, scope and shared-reference coverage.

### Limits and adverse cases

A wall extended across the entire co-located layout exposes the remaining worst case. The baseline's first negative query takes about 2.18 seconds because it checks the whole graph even with an A\* expansion allowance of one. Revised code returns `budget-exceeded` after one expansion in about 32 ms; a subsequent full search takes about 1.09 seconds to prove no route across 5,852 expanded states, and the next identical query uses the closed-region proof in about 1.10 ms. The full search already had metadata and a few checks from the small-budget request, so it is not an entirely cold measurement. A first exhaustive no-route proof can still block the host; SW06.2a remains open before denser content or higher limits. Caching a partial search as impossible would hide this cost incorrectly and is forbidden.

The successful dense-route sample improves by about 97%, and isolated inert multi-step work can improve by more than 98%; neither establishes a 90% reduction in whole-game latency. Physical checks, first acquisition/event storage, observation serialization, commit/projection and rendering remain actual work. Candidate metadata still scales with admitted surface area, coarse surface grouping remains bounded pairwise work, and genuinely dense sensing does not become sublinear. No long-session memory, population maximum, GPU FPS or percentile guarantee follows from these short runs. A proposed `current(world.entities)` snapshot inside every encounter phase made a moving-bird diagnostic slower and was discarded rather than shipped.

### Running application

The production entrypoint ran on loopback port 3223 with a fresh `/mnt/data/spatial-round3-live` SQLite directory. The page and authenticated state endpoint returned HTTP 200. Normal presence/control and command endpoints resumed the world, moved the actor from `{x:11,y:0,z:13}` to `lookout-deck` at `{x:24,y:3,z:6}`, and printed intermediate supported ramp heights before completion. A command with the same elevated point but `terrain` support was rejected; the actor then traveled down the ramp and underneath to `{x:20,y:0,z:5}`, completing normally. A clean shutdown/restart retained that lower position/support and reopened paused. The application process was stopped; no existing user save was changed. Closed-region navigation and observation-copy changes were also exercised through the direct native diagnostics; Chromium was not run in this pass.

EPR03 acquisition-scope privacy, general crowd avoidance/landing fairness, the D51 viewport/lease contract, hidden-map routing and broader model/asset handling remain deferred in their owning trackers. This optimization does not cure the existing external encounter-broadcast leakage described in the preceding review. Normal CI configuration is restored after temporary publication tooling is removed; the final review commits skip test jobs for this request, not for future work.

## Spatial scaling continuation: equivalent patches

The continuation initially inspected `ce7d9d1900c7852073dc46582544a2841afc58ea`, then found the branch had advanced to `cd6c3ef1e9d5828393c832e771722b9561b9b366` with the third-pass changes above. The current tree was exported at `2e8bd07dfd674ecee117f3094c909506a723790f` for reconciliation. Its lazy graph, connector-aware heuristic, four closed-region proofs, finite native roster and observation sanitation were preserved. Only three additional source files change: navigation, geometry and the independent JSON-data copier. No automated test files were added/edited and no test suites were run. SW04.1c/SW05.1d record implementation; SR13–SR15 retain deferred qualification without replacing SR11/SR12.

The added algorithm is a navigation-only quotient of exactly identical public movement patches: bounds, plane, thickness, solid base and movement material match, while all physical/acoustic records retain their own identities and effects. Native zero-length seams preserve the original endpoint support IDs. Frozen lattice points can reuse exact stance results through weak keys with body dimension/capability checks under map/revision geometry. Mutable points still take the complete query path. The JSON copier removes intermediary entry/tuple arrays, not independent mutable evidence. Reconciliation kept the existing closed-region proof cache instead of adding a second attachment-pair failure cache. Future private/directed support families must qualify their own equivalence.

Production TypeScript and the Vite build passed on the reconciled tree; the existing client bundle-size warning remains. Changed-file formatting, relative documentation paths and diff whitespace were reviewed. No unit/integration/browser suites, paid providers or current Chromium qualification were used. A preliminary native CPU profile still located material event-copy and Immer finalization work; the modest clone change is not a claim to eliminate that cost.

### Matched continuation observations

Ad-hoc scripts outside the repository invoked the actual native modules on Node 22.16.0, an AMD EPYC 9V74 container exposing five CPUs. Both columns below use the same current/reconciled source pair and workload; the timed series omit profiler instrumentation. The 28×24 starter map has 28 added 22×19-lattice-point patches (31 authored surfaces total), with 11 default blockers or 12 including the added detour wall. The patched maps increment their geometry revision before queries. This is not the 120-blocker workload from older reviews. Each cold successful series uses three fresh worlds; time includes graph preparation/search but not world construction, SQL, network, GPU or paid cognition.

| Native workload                                                                 | Concurrent branch baseline | Combined continuation | Observation                                                                                   |
| ------------------------------------------------------------------------------- | -------------------------: | --------------------: | --------------------------------------------------------------------------------------------- |
| Cold starter lookout route, median                                              |                    6.17 ms |               6.94 ms | Maximum 11.93 / 13.22 ms; no speedup established.                                             |
| Cold lookout route with disconnected stacked patches, median                    |                    7.03 ms |               5.45 ms | Maximum 9.49 / 8.91 ms; same 20.1224-metre length and 93 expansions.                          |
| Cold blocked route across 28 identical overlapping patches, median              |                  108.44 ms |               7.57 ms | Maximum 130.30 / 15.77 ms; same 33.4142-metre length and 34 path points; expansions 59 / 32.  |
| Fractional-endpoint route across identical patches, median                      |                   96.66 ms |              22.14 ms | Same 33.2494-metre length, valid native legs and intended final support; expansions 86 / 32.  |
| Fractional-endpoint route across slightly different overlapping patches, median |                   89.07 ms |              78.43 ms | Each patch has a distinct minimum X, so equivalence is unavailable; revised maximum 85.22 ms. |
| First one-second advance with 250 additional resource entities, median          |                   52.98 ms |              52.95 ms | Three worlds; 264 entities, same 467 first-exposure events. No material improvement.          |
| First one-second advance with 180 additional airborne birds, median             |                   44.36 ms |              42.21 ms | Three worlds; 194 entities, same 384 events. Native controllers, not 180 LLMs.                |

Seven later advances in each of three worlds had baseline/revised medians of 2.088/2.128 ms for resources and 7.748/7.588 ms for birds. These small differences are not sustained scale qualification. Initial pre-reconciliation observations on the earlier eager source measured the identical-overlap route at about 2.74 seconds, but that is not the baseline in this table. Both the concurrently landed lazy/heuristic work and this continuation's equivalence/memoization contribute to the total improvement. None of these figures establishes a 90% whole-game latency reduction.

A deliberately complete blocking wall provides the adverse case. With identical patches, a one-expansion request returned `budget-exceeded` in 24.40/12.22 ms; the following full no-route proof took 933.70/15.80 ms and expanded 4,788/171 states. The subsequent query reused the bounded closed-region proof in 0.99/0.87 ms. With slightly different patches, the corresponding full proof still took **826.57/392.02 ms**, with 4,275 states in both versions; the repeated negative query took 0.83/2.67 ms. These are single diagnostic observations, not percentile estimates, and the full proofs already had metadata from the small-budget request. The remaining approximately 392 ms host-blocking case keeps SW06.2a open. No content limits, safety checks or sensory cadence were weakened.

Fractional routes retained their endpoint IDs and each returned leg passed the existing native segment predicate in printed inspections. The equivalent-patch case inserts an explicit initial seam to the representative rather than claiming identical intermediate IDs. Overlapping supports with different transmission values retained the same complete sound product (`0.000026612964445810885`); navigation sharing did not erase barriers. Forward/reverse ramps, a small-body route, wrong-floor rejection and a budget interruption followed by success were also printed. The 80-object exposure observation retained 141 events and the same original-source event/awareness digest prefixes (`a7b023bec4327c2db1c4` / `8467b7aaeeb7e777361e`). Separate copy observations preserved sparse arrays, own `__proto__` data, independent nested records and detached draft copies. Frozen stance observations changed correctly for a taller body and a lowered ceiling after revision; mutable point changes did not reuse a frozen result. These are selected manual observations, not an independent automated invariant proof.

### Reconciled running application

The combined application was built and started through its ordinary production entrypoint on loopback port 3225, with fresh isolated `/mnt/data/round3-merged-live` SQLite storage and zero AI budget. Normal authenticated presence/control and command calls moved the actor to `{x:24,y:3,z:6}` on `lookout-deck`, rejected the same point on `terrain`, and completed movement underneath to `{x:20,y:0,z:5}`. Those local command calls returned in 17.98, 4.18 and 6.26 ms respectively; these are individual observations, not a request-latency distribution. The page returned HTTP 200. A clean pause/shutdown/restart retained the lower position/support and reopened paused. Both application processes were stopped; no existing user save was touched.

Current browser/GPU, PostgreSQL, long-session heap, many-client and paid-cognition latency remain unmeasured. EPR03 private acquisition, D51 viewport authorization and broader crowd/flight/model-family gaps remain in their existing owners. Temporary reconciliation/publication files are removed at final publication and normal CI restored; this request skips automated test jobs, not future work. The complete-latency plan remains PF-owned, with spatial preparation and evidence semantics separately owned by SW/EPR.

### Compact camera toolbar verification

The production build passed. Manual inspection in the running local browser confirmed the visible icon cluster, checked follow toggle, recentering, rotation, projection switching, drag cancellation of follow and keyboard-focus shortcut help. Continuous moving-player/elevation tracking, reload persistence and narrow-screen touch qualification remain unverified. The repository-wide check stopped at formatting differences in unrelated concurrent cognition files before reaching tests; those files were left untouched.

## Cognition context and action capacity

September 23 manual runtime qualification used synthetic data and isolated SQLite/temporary PostgreSQL storage; no automated tests were written or run. The failed incident's 43-question request passes local shape/size validation. A synthetic size exercise admitted 300 questions at 47,771 estimated tokens; 1,000 and 10,000 candidates selected 345 under the 55k estimate in approximately 8ms and 56ms respectively. This is admission throughput, not model quality.

A 512-action dense scene retained 20 perceived entities and six possessions after injected optional relevance failure. Unavailable vectors returned an explicitly incomplete deterministic 300-candidate fallback. Fixture all-yes judgments produced 262 handle-bearing action options under request/prompt budgets; action-filter failure preserved 288 fitting options. Twenty-four significant experiences advanced the wakeup watermark together. The subsequent changed interest subscription caused one fresh opportunity; the third unchanged consideration purchased no additional work. Fixture spend was zero. Manual React server rendering exercised speech, thought, action, goal, plan, empty continuation, missing envelope, and undispatched failure summaries; this was not browser interaction acceptance.

Live OpenAI embeddings and temporary PostgreSQL/pgvector indexed 500 synthetic action descriptions and returned 300 in 8.1 seconds, ranking the deer approach option first despite its last source position. The first 300-question Jev request revealed Macrofold's 16,384 output-token maximum; after correcting that request limit, the configured Macrofold route rejected admission with HTTP 413 `model_context_exceeded`. No successful 300-question provider inference is claimed. No direct Jev credential is configured. Large-request acceptance through that route remains blocked on its context admission; no automatic paid retries or silent truncation were added. Admission rejection is now distinguished from uncertain execution; an isolated HTTP 413 fixture returned failed/undispatched/not-uncertain and released its reservation to zero. Local conservative ledger charge across these checks was $0.20 (including retained reserves from rejection receipts before that correction); actual invoice reconciliation remains external.

Sequential native stress runs used Node 22.23.2, frozen snapshots, no warmup, 180 steps at requested 3× speed. Gems (514 entities): 1,482ms total, p50 5.43ms, p95 13.68ms, max 208.68ms, headroom 0.675. Mixed (344 entities): 1,658ms total, p50 3.78ms, p95 5.59ms, max 938.56ms, headroom 0.603. Neither cold workload met 3× capacity (headroom must exceed 1); geometry, encounters and Immer finalization dominate. These native-only profiles do not establish a cognition regression or full multiplayer capacity. Local artifacts: `/tmp/openlegend-cognition-{gems,mixed}-sequential.json` and corresponding CPU profiles.

The production TypeScript/Vite build passed. Repository-wide Markdown link/anchor inspection found zero issues across 109 documents; whitespace checks passed. Automated suites remain deliberately unrun per owner instruction.

## Targeted punch runtime

September 23: manual native execution with a disposable scene admitted Punch by target ID, approached from outside reach, restored the in-progress action from JSON, and completed exactly one hit (100 → 95 health). Replaying the command returned duplicate without another hit; self-target and unknown-definition commands were rejected. Moving the target out of range during wind-up left health unchanged. Public animation projection included the pinned action's progress/direction; no target identity was exposed by that descriptor. A local 1,000-admission exercise took approximately 1.13 seconds; this is a synthetic command measurement, not concurrent pursuit capacity.

An isolated zero-budget server on port 3219 rendered the procedural arm/fist pose in the browser. The target catalogue displayed enabled “Punch Ada”; selecting it dispatched through the player UI. Simulation ticking was disabled for stable visual inspection, so continuous browser animation/impact timing was not qualified by that preview. Native stepping verified approach, impact and completion separately.

Sequential native stress profiles (Node 22.23.2, 180 steps, zero warmup, requested 3×): gems p50 6.39ms / p95 17.87ms / maximum 168.44ms / headroom 0.612; mixed p50 5.35ms / p95 19.34ms / maximum 1,963.32ms / headroom 0.300. Both remain below 3× headroom. These existing cold fixtures do not isolate punching or prove a regression; pursuing actors still need dedicated scale qualification. Local artifacts are `/tmp/openlegend-punch-{gems,mixed}.json` and corresponding CPU profiles.

The production TypeScript/Vite build passed. No automated tests were written or run; deferred coverage is in [TODO](maintainers/TODO.md#targeted-strike-validation). No paid APIs were called.

### Punch animation correction

The initial static mesh preview above did not qualify a working character-arm animation or its cleanup. A user report exposed the visual mismatch and a player-delta bug: undefined completion fields disappeared during JSON serialization, retaining the last animation on the client.

The replacement uses three cached character-sprite poses and a short recovery, with explicit null clearing. An isolated zero-budget server manually advanced one action from wind-up to extension and completion while the browser stayed connected. Browser inspection showed the raised arm, extended arm and return to the normal hanging arm without reload; completion's serialized player delta contained `actionAnimation: null`, and target health changed 100 → 95. The production build passed. No automated tests or paid calls were used.

## Observer-private encounters

A fresh-world manual `advanceWorld(world, 1)` execution produced 24 encounter events. Every encounter had private scope and exactly one audience member, its observing actor. Sample awareness records were owner-only, retained observed modality, and used first-person encounter text. This is runtime evidence for emission isolation, not broader EPR03 recognition or exposure-delta acceptance. The emission correction does not rewrite existing historical memories.

## God-mode character panels

An isolated zero-budget production server showed Revive, Edit Person and Inspect private mind for a dead fixture actor. Clicking Revive returned a success message, changed the description to living and removed Revive without reload. The initial fixture server encountered an error during concurrent sleep changes; restarting against the updated sources allowed revival to complete. The client Vite bundle passed; the full production build remained blocked by two possibly-undefined `actor.energy` errors in `packages/domain/src/sleep.ts`. No automated tests or paid calls were used. Deferred coverage is in [TODO](maintainers/TODO.md#god-mode-character-controls).

## Development startup recovery

The normal `.env` PostgreSQL startup failed because its saved world contained the previous `sleepPolicy` but no `statusEffectPolicy`. Read-only reconstruction of the saved checkpoint/journal reproduced the validation error. Earlier status-effect verification used a fresh temporary database and did not cover this path. The initial reset-based recovery is superseded; see [documentation history](documentation-changelog.md#in-place-development-updates).

Manual isolated SQLite execution of the in-place replacement retained the world and actor IDs, name, energy 37, simulation time 12,345, inventory and original rest action ID. The action became a current effect instance with 456 elapsed dream seconds. Raw checkpoint/journal reconstruction confirmed the new registry was durably stored and obsolete policy/rest fields removed. Accounting was byte-for-byte unchanged and a separate integration record survived. A second startup retained identity and dream progress. Manual-save reading converted the same earlier shape without changing identity or action ownership. Malformed current registry values still rejected startup without replacing the world.

Production build passed. These were zero-network native fixture executions, not automated tests or PostgreSQL migration acceptance. No paid calls were made; deferred adapter and integrity coverage remains in TODO. This change does not restore gameplay discarded by the preceding reset.

## Generic status effects

Manual domain runs exercised the authored default: NPC energy 19 stayed awake during daytime, energy zero activated the effect, nighttime energy 69 activated it, and player-controlled actors remained exempt in both zero-energy and nighttime cases. Explicit player activation succeeded. Physical deactivation emitted `Ada woke up.` with the conversation association and witnessed audience; automatic full-energy completion ended at exactly 100, without expenditure in the same interval. Current-state JSON roundtrip validation passed. Old sleep-policy evidence below describes the superseded implementation.

A separate manual object scenario admitted a definition on an object without an actor component, bound `$actionTarget` to another object, increased that object's registered charge from 20 to 24 over two steps, and ended on its target-value condition. The subject stayed at 10. Stale registry revisions and an unknown target reference were rejected. This demonstrates explicit entity/attribute bindings; it does not certify arbitrary operations or world-agent authoring.

An isolated SQLite server-service run held an explicitly labeled fixture judgment in flight, activated the restricting state, and observed cancellation with zero generator calls and no accepted response receipt. Durable history and public conversation projection contained the player's greeting followed by `Ada fell asleep.` narration. Fixture spent/reserved cost remained zero. No real model requests were made.

A production build ran in a fresh isolated local server with no credentials and a zero AI budget. Browser inspection showed Ada horizontal with floating `zzz`; the target menu offered the authored `Wake up · Ada` action. Clicking it returned successful deactivation, restored her upright pose and removed the particle. The temporary browser/server were closed afterward. This is limited manual visual evidence, not full reduced-motion/floor/multiple-effect coverage.

Sequential Node 24.13.0 stress profiles used frozen snapshots, 180 steps, zero warmup and requested 3× speed. The initial generic implementation measured 3,148ms for 514 entities (gems) and 2,334ms for 344 entities (mixed). Profiles identified repeated condition/rule traversal through draft proxies. Reading immutable admitted definitions directly and avoiding redundant self-binding lookups reduced totals to 1,412ms and 1,290ms respectively. Gems: p50 6.88ms, p95 9.09ms, max 130.04ms, 3× headroom 0.708. Mixed: p50 5.63ms, p95 7.60ms, max 232.87ms, headroom 0.775. These remain below full cold-workload 3× capacity; concurrent unrelated work and changed sensory behavior prevent attribution against historical snapshots. Profiles and JSON are `/tmp/openlegend-status-optimized-{gems,mixed}.*`.

A separate frozen-snapshot synthetic scene contained 106 actors with 100 active restorative effects, ten warmup steps and 180 measured steps: 1,132ms total, p50 5.95ms, p95 7.61ms, max 32.31ms, 3× headroom 0.883. All 100 remained active and saved state validated. Production TypeScript/Vite build passed. Automated tests were neither written nor run; regression cases are in [TODO](maintainers/TODO.md#status-effects--deferred-automated-validation). Paid spend was $0. Dense-scene capacity and broader authoring remain follow-ups, not completed acceptance.

## Declarative actor sleep

An isolated manual simulation confirmed the player stays awake at night with energy 69 and during daytime with energy zero, while Ada sleeps automatically in both cases. Explicit player Sleep succeeded in both scenarios. Production TypeScript checking passed; deferred regression coverage is recorded in TODO, with no automated tests written or run for the player exemption.

The supplied live trace `cefb8591-dec9-4bfd-8138-342ecdfbf960` records accepted speech and a private thought: response admission completed at 2026-09-24 00:42:01 UTC. Ada answered the question about Joseph; this was not a canceled reply falsely classified as speech. A read-only query of persisted event `event-1500` confirmed both Ada and the player in its audience, at simulation time 88966 and conversation `conversation-1491`. That trace does not establish why the conversation panel failed to show it, or a sleep transition before admission.

Manual isolated domain runs exercised night energy 69 and daytime energy 19 triggering sleep, daytime energy 69 staying awake, immediate voluntary sleep, physical wake, wake grace, a sleeping deer remaining stationary, full-energy waking at 100, policy-disable waking, format-10 JSON save/load and explicit format-9 rejection. A separate isolated SQLite server-service run held a fixture judgment in flight, put Ada to sleep through the normal command transition, and observed immediate cancellation: no generator call, no accepted response receipt, and no speech from Ada. Persisted conversation history returned the player's speech followed by an event `Ada falls asleep.`; the public view projected that event as narration. Fixture accounting remained zero; no paid API requests were made for this change. These are executed manual scenarios, not automated regression or model-quality acceptance.

Sequential native stress profiles used Node 24.13.0, frozen snapshots, 180 steps and requested 3× speed. Gems (514 entities): 1,143ms total, p50 5.63ms, p95 8.31ms, max 82.54ms, headroom 0.875. Mixed (344 entities): 1,100ms total, p50 3.82ms, p95 9.89ms, max 253.31ms, headroom 0.909. Both remain below full cold-workload 3× capacity; these runs do not isolate this change from concurrent spatial work. Artifacts are `/tmp/openlegend-sleep-{gems,mixed}.json` and associated CPU profiles. Native simulation and event/recipient work remain the measured follow-up, not a reason to introduce a sleep-specific queue.

A sleep-heavy synthetic runtime run used 106 actors with 100 clustered sleeping deer, ten warmup steps and 180 measured steps: 2,011ms total, p50 11.03ms, p95 12.37ms, max 13.58ms, 3× headroom 0.497. All 100 remained asleep with valid saved state. This does not qualify dense actor scenes for 3× speed. Production TypeScript/Vite build and documentation link/anchor checks passed; automated tests were neither written nor run.

## Disposable base-world startup

The `openlegend_base` development world was initialized once with current sleep-policy defaults and current rest-state fields, retaining its world identity, energy values and revision 3454. Current domain validation, an unpersisted one-second simulation step, and the real PostgreSQL repository load all passed. Per-feature world-version rejection was removed from module validation and named-save admission; structural validation and save checksum checks remain. This was a local state adjustment, not an added legacy reader or automatic migration framework.

### Person editor needs

An isolated zero-budget browser session showed shared Health/Food/Energy meters with editable numbers in Needs and an empty Stats section. Editing player Food from 76 to 42 updated the draft meter; Save updated the main HUD and character meter to 42 and reported all changes saved. The exercise exposed and fixed a read/save eligibility mismatch: saving now accepts the same memory-capable actors as editor reads and the domain transition. Production TypeScript/Vite build passed. No automated tests or paid calls were used.

## Conversation snapshot and handled speech triggers

The September 23 read-only inspection of `cefb8591-dec9-4bfd-8138-342ecdfbf960`, `thought-a836dc2f-379c-49b0-ad70-40606ed44e7f` and related records located the repeated Joseph answer in intervening trace `thought-e19bb6af-ea35-406b-b7a7-6db5317335bf`. That prompt presented the already-answered question as new addressed speech while retaining Ada's prior answer in recent memory. The named later thought instead coalesced bird/hare encounters and initiated new speech; optional relevance failure excluded both earlier question and answers from that later context.

The correction passed two isolated SQLite/provider-fixture tests: all 34 replies after an original trigger remain in chronological conversation context despite relevance failure, and a fresh director uses the durable completed chat record to exclude its handled speech while preserving earlier and newer unanswered speech as triggers. History remains available independently of trigger suppression. Fixtures made no network requests; this is not live behavior acceptance. The production TypeScript/Vite build passed. `pnpm run check` passed formatting but stopped at 156 type errors outside this fix's source/test files, largely stale asynchronous API usage in existing tests. No paid calls were made for the correction.

## Single-cause cognition trigger

The local trigger-context fixture verifies that the newest selected event alone renders as the literal trigger, while earlier coalesced evidence survives optional relevance failure in context. The focused test passed without provider calls. The repository check passed formatting but stopped on existing test type errors (including stale declaration provenance, removed goal fields and asynchronous API fixtures); full-suite acceptance is not claimed.

## Compact Jev relevance requests

Local serialization of the owner's 81-question incident, replacing question definitions alone and retaining all original state/candidates, reduced question characters from 44,693 to 7,514 and total request characters from 58,246 to 21,067 (64% smaller). This is character accounting, not measured provider token savings. Additional runtime changes remove duplicated people labels, omit empty inner-world excerpts and keep the generative response contract out of action-relevance context.

A live synthetic 81-question Noul request through the configured Macrofold/Jev route returned all 81 answers in 566ms (418ms provider latency), reporting 4,050 input/1,452 output tokens and $0.0001701 estimated cost ($0.000171 settled conservatively). For a water-seeking trigger, the remembered spring scored 0.94 and the first incidental pebble memory 0.06. No private incident text was sent. This qualifies the request format and a simple relevance distinction, not broad recall quality or equivalence to the former Choice rubric. Three focused local tests cover 300-question admission, the threshold, missing answers, action-context projection and trigger/context separation; all passed without external calls.

The compact-request production build passed before concurrent status-effect changes landed. The later final build was blocked by type errors in that ongoing domain change; the repository check also stopped on formatting in `default-status-effects.ts` and `status-effects.ts`. Those files were left untouched. Markdown links/anchors and changed-file whitespace checks passed.

### Person editor tabs and inventory

In an isolated zero-budget browser session, Character showed current statuses, editable Needs and empty Stats; Identity showed descriptive fields, traits and goals. Inventory typeahead filtered “meat” to two choices. Replacing a cutting stone with seven cooked meat and saving produced the corresponding inventory change notification and all-changes-saved state. Production TypeScript/Vite build passed. No automated tests or paid calls were used.

A synthetic native profile applied 200 inventory edits to a fixture actor with 1,006 stacks: median 5.84ms, p95 9.85ms, maximum 15.09ms. This measures the domain transition only, not browser rendering or SQLite commits.

## Readable cognition context audit

Manual synthetic runtime execution on Node 24.13.0 used an isolated in-memory SQLite world, no configured provider credentials and zero budget. Captured attention input contained labeled identity/time/body/agency/conversation/surroundings/possessions. The final prompt described the lookout crate's Supple branch contents, horizontal bearing/distance and 3m elevation, and named inventory ownership/properties. No null-action animal was labeled idle. An injected optional-relevance outage retained 20 perceived entities and six possessions. Changing observer position changed description text but preserved the entity embedding revision. Authoritative forgetting during the judgment callback followed by an injected failure discarded the decision with an explicit privacy-boundary error. No live model-quality inference follows from these local callbacks; spend was $0.

Ten projection runs per synthetic observation size measured p50/max 1.24/3.13ms for 500 entities and 12.60/14.19ms for 5,000. Separate sequential native profilers used the existing gems and mixed scenarios, frozen snapshots, no warmup, 180 steps at requested 3×. Gems (514 entities) measured total 3,811ms, p50 12.91ms, p95 41.40ms, max 494.00ms, headroom 0.262. Mixed (344 entities) measured total 1,898ms, p50 8.07ms, p95 14.22ms, max 293.33ms, headroom 0.527. Both remain below 3× capacity. Concurrent workspace activity and a different Node version prevent attributing differences from older profiles to this change; these native profiles do not measure cognition. Raw local artifacts are `/tmp/ol-context-projection-runtime.log`, `/tmp/ol-projection-gems.json`, `/tmp/ol-projection-mixed.json` and their CPU profiles. Native performance follow-up remains PF00/PF03.

Automated tests were not written or run at the owner's request. Production TypeScript/Vite build passed; the existing large-client-bundle warning remains. Markdown link/anchor inspection found zero issues across 115 documents; whitespace checks passed. This review covers the immediate cognition context path, not completion of pending CR/AG/EPR acceptance gates or a full engine audit.

## YAML-defined wake interaction

Three focused no-provider fixtures passed: waking another nearby sleeping entity with the configured action and narration, rejecting distant interaction without changing sleep state, and honoring changed labels/other-target permissions. These execute the generic domain command and shared server action discovery using the generated YAML defaults. They are not browser acceptance or a mutation of the running world. Production TypeScript/Vite build, generated-config drift validation and formatting checks passed. The full repository check stopped at existing test type errors (including stale rest commands and synchronous uses of async APIs); the unrelated fixtures were not rewritten.

Numeric body-context manual execution used a synthetic actor with fullness 18.5 and energy 23.25. Output retained both exact values, percent units, 0–100 ranges and hunger/exhaustion descriptions, plus healthy-state health at 100. Production build passed; no automated tests or provider calls were made for this change.

## Intelligence panel readability and runtime verification

Manual browser inspection of the running world confirmed compact person/animal rows, normalized encounter descriptions, small timestamps, proposed speech/thought/actions, explicit failed-stage text, and separate trace/world-time metadata. Header dragging moved the panel; the native bottom-right handle reduced its height while retaining a scrolling body. A recorded Context relevance stage rendered 93 Noul answers as Yes 2 / No 91 with exact probabilities. On another 80-answer stage, deselecting No reduced the visible results to 2 of 80; Copy input displayed Copied. A live read-only billing fetch displayed a completed BYOK Jev record with 7,570 input / 1,380 output tokens, $0.000318 provider cost and zero platform charge. This inspected existing work; no new inference was requested and direct verification spend was $0.

Synthetic execution of the actual React renderers (20 renders per size) measured 300 Noul answers at p50 7.91ms / p95 43.75ms and 1,000 at p50 20.36ms / p95 25.48ms, with exactly two filters. Rendering 25 recorded history rows took 7.73ms. These are server-render timings, not browser paint or live model-quality measurements. Ten sequential live history reads observed p50 870ms versus 1,838ms before the parent/time index; concurrent simulation prevents a controlled attribution. The specific PostgreSQL parent lookup changed from a sequential scan at 270.53ms to an indexed plan at 0.263ms. Follow-off root-only polling measured p50 7.59ms, max 27.10ms and 130 response bytes. Full history latency remains a performance follow-up.

The existing mixed native stress scenario (344 entities, 180 cold steps, requested 3×) measured p50 9.06ms / p95 18.16ms / max 400.06ms, total 2,240ms and headroom 0.446. It still misses 3× capacity; this is a native simulation limitation, not proof of inspector performance. PF00/PF03 retain that work. Production build passed; no automated test suite was written or run. Regression cases are deferred in TODO. Local artifacts include `/tmp/intelligence-noul-stress.log`, `/tmp/intelligence-peek-stress.log`, `/tmp/intelligence-api-final.log` and `/tmp/intelligence-native.json`.

## Short entity reference runtime

An isolated in-memory, zero-budget runtime produced two distinct anonymous hare references (`3cac`, `41f5`), deer `8100`, explicit species and compact possession entries. A locally constructed response passed the bound provider schema, resolved thought/goal references to `deer-1`, and completed native admission; its goal stored the canonical identity and reprojected to `8100`. Unknown token `zzzz` was rejected. This is a manual synthetic execution, not a paid-model behavioral claim. A 5,000-entity collision exercise returned 5,000 unique references with 341 expanded beyond four characters; observed uncached allocation took 13–59ms across runs under concurrent workspace load. The map is reused for the same entity-map snapshot. No automated tests were written or run; no paid calls were made. Production build passed with the existing bundle warning. Observer-name and knowledge evidence is recorded in [Editable knowledge and observer names](#editable-knowledge-and-observer-names).

### Intelligence hierarchy and rich previews

Manual browser review at 1280×720 confirmed visible autonomous-cognition subtype and Level 2 badges, timestamps beside the bottom line, and a structured response preview on keyboard focus containing separate speech/thought/plan fields. Routing displayed the chosen level and compact probabilities. Accounting and action-selection/routing support moved beneath identity-matched provider stages; raw history remains unchanged. DOM geometry inspection found no unintended horizontal overflow after constraining the detail grid and wrapping probability text. Pointer-hover behavior uses the same React Aria tooltip primitive; the directly exercised preview path was keyboard focus.

A no-provider runtime request constructed 300 action-specific reasonable-now questions in 89,376 serialized characters. This verifies prompt construction and sizing, not judgment quality; older traces retain their originally submitted wording. Synthetic actual-renderer stress measured 1,000 Noul answers at p50 20.64ms / p95 24.50ms and 25 trace rows at 13.95ms. Ten full live history reads measured p50 903ms, maximum 1,713ms. These are distinct from browser paint and model latency. No paid inference or automated test suite was run. Production build passed with the existing bundle-size warning.

## Trace attribution and pacing runtime

Manual execution used isolated synthetic domain worlds and in-memory SQLite, with provider callbacks explicitly marked fixtures, network disabled for scheduler execution and no paid verification calls. Ada and Hare completed immediate decisions in two successive passes at the same injected wall time (about 54ms and 13ms); a third pass admitted neither again. This confirms removal of the global wait while the individual cooldown still applies, not live model quality.

Speech to an unavailable bird preserved `intendedRecipientId` in the event and speaker awareness without admitting a directed target or adding the bird to the actual listener audience. A nearby overhearer lacked the hidden recipient identity; serializing/cloning the saved shape and rerunning cognition initialization preserved that omission. Generic memories involving two hares produced one grouped episode with no hare handles; repeated episodes about named Mike remained separate. The directed-speech prompt contained reply encouragement; the overheard prompt did not.

Rendering the owner's captured trace with the actual React component displayed skipped vector search, 28 hard entries, 87 optional candidates, 87 Jev judgments and 11 selected entries. Live browser inspection also displayed structured trigger facts and separate retrieval counts on a newer trace. That live trace had an optional Jev `non_json_input` failure while routing/generation continued using hard context. Investigation identified local request validation rejecting undefined speaker/recipient fields on non-speech trigger facts, before provider dispatch; JSON diagnostic serialization concealed these omitted values. Non-speech trigger facts now omit inapplicable roles explicitly. This correction is not evidence of repaired live model behavior.

Twenty synthetic recall runs at 500/5,000 stored episodes measured p50 2.49/4.61ms and p95 5.26/5.42ms; the existing source bound limited returned candidates to 270/275. Twenty server renders of 300/1,000 vector result rows measured p50 3.25/9.71ms and p95 15.37/12.82ms. These are bounded projection/server-render measurements, not browser paint times or unbounded recall claims. The existing mixed native scenario (344 entities, 180 cold steps, requested 3×, Node 22.23.2) measured total 1,319ms, p50 5.86ms, p95 7.41ms, max 228.55ms and 136.51 simulated seconds per wall second, below the required 180; PF00/PF03 remain open. Concurrent workspace activity prevents attributing differences to this change.

Production build passed. No automated tests were written or run; regression work is in TODO. Local artifacts: `/tmp/ol-trace-runtime.log`, `/tmp/ol-scheduler-runtime.log`, `/tmp/ol-retrieval-render.log`, `/tmp/ol-trace-native.json` and `/tmp/ol-trace-native.cpuprofile`.

The subsequent `non_json_input` correction was exercised by executing the actual trigger-facts function in isolation with fixture time/label/visibility helpers, then passing its output through the actual attention request builder and AI client. The old non-speech shape reproduced `invalid / non_json_input / dispatched: false`; corrected speech, non-speech and missing-awareness inputs passed serialization and reached `provider_not_configured` with no network calls. Serializing each 300-question shape 1,000 times averaged 0.29–0.31ms per request. Direct module execution was blocked by unrelated in-progress domain import changes in the shared workspace; this isolated evidence does not establish full application or live-provider acceptance.

The follow-up serialization review loaded the real server modules successfully and passed the production TypeScript check. Manual no-network execution covered non-speech, directed speech, overheard speech, self speech and missing awareness through trigger projection, the 300-question attention builder and the AI client; all passed JSON validation and stopped at missing provider configuration. A synthetic 1,000-iteration run combining trigger text/facts lookup, 300-question construction and serialization measured p50/p95 0.69/1.22ms with 300 awareness entries and 0.86/1.19ms with 10,000. Trigger lookup uses a direct scan without allocating a full index; no additional cache or batching was justified. These results supersede the earlier module-import blocker for this path, but remain local runtime evidence rather than live model-quality acceptance.

Skipped-cognition presentation was checked through the running server using retained trace `thought-d1c87e6f-02f8-422e-932f-4ea5bfc18ad6`: the projection returned `Cognition skipped · Sleeping`, reason `Sleeping`, disposition `skipped` and zero children. The client excludes semantic roots from execution-stage cards and counts actual children, while retaining root-only world-agent rendering. Production build passed; no automated tests or paid calls were used.

### Autonomous cognition without actor cooldown

Production build passed after removing the actor admission delay, delayed thought wakeups and cognition-policy field. No-provider manual execution upgraded a synthetic world with a 45-second saved field, preserving world identity and removing that field. The actual `ActorWork` implementation returned two initial actors, zero after inspection without changes, then both after changed inputs at the exact same wall-clock timestamp. A 10,000-iteration two-actor refresh/readiness/inspection stress loop took 46.19ms; this is scheduler-component evidence, not end-to-end model latency or many-actor scaling. Automated regression coverage is deferred in TODO. Background maintenance timing is unchanged.

## Base-world item handling and content separation

Inspected remote `design/action-repertoire-and-capability-grounding` at `0582e86`, then reviewed its advance through `9fb0291` (perception/exposure/intake performance changes), without merging it. Its typed navigation/action-grounding work is not an implementation of inventory transfers. Native item custody and concrete actions were exercised independently; post-merge typed invocation/approval/readiness integration remains BW05.

Manual browser execution used an isolated SQLite world on port 3227, no configured inference providers and zero AI budget. God creation added five Small stones to player inventory (6 → 11); dropping three left eight and created a visible pile. Ground Add something → Items opened the known catalogue and created four Supple branches; native approach/pickup increased inventory from two to six and removed that pile. Restarting the verification server preserved eight stones and six branches. Dropping two branches and one stone at the same position produced one mixed pile; inspection listed both stacks. Right-click → Pick Up displayed Pick Up All and each stack; selecting All restored both quantities and removed the pile. Final client inspection confirmed nested category selection by click, paused God creation, and right-click selection directly on rendered pile artwork. Hover activation was observed when the pointer entered the newly opened category trigger; a dedicated full contents-hover/accessibility exercise remains deferred. The live user world was not used for these mutations. Browser verification used `localhost` to avoid sharing the live world's host-scoped session cookie.

Manual synthetic domain execution covered repeated creation receipts, partial drops, equipped-stack release, nonportable rejection/retention, coincident pile merging, safe-integer overflow rejection, and JSON-restored competing pickup work. Competing actors preserved the total stone quantity (15): one collected the pile and the other stopped when its target disappeared. In-place legacy upgrade retained world identity and populated missing portability. These are local runtime exercises, not automated regression coverage or paid-model evidence.

A 1,000-stack synthetic pile exposed repeated preview work in catalogue construction: the initial 1,002-row menu took 6,105ms. Sharing the common admission preview reduced observed construction to 41–57ms; execution still rechecks the selected contents. The final 15-run pickup exercise measured median 42.28ms and maximum 95.01ms. These small samples are local measurements, not a capacity guarantee. A separate existing mixed native stress scenario (344 entities, 180 cold steps, requested 3×, Node 24.13.0) measured total 1,746ms, p50 6.81ms, p95 14.17ms, maximum 259.79ms and headroom 0.573. It still misses 3× capacity; PF00/PF03 retain native scaling work. Concurrent workspace activity prevents causal comparisons against older profiles.

Production TypeScript/Vite build passed before concurrent knowledge-module work appeared; the final rebuild is currently blocked by new `knowledge.ts` / `worlds/base/knowledge.ts` references to missing WorldState knowledge/perception fields. That unrelated work was left untouched. Item runtime execution, generated-config validation, formatting and the 121-document link/anchor audit passed. No automated tests were written or run; the deferred cases are in TODO. Spend was $0. Local artifacts: `/tmp/base-items-runtime-final.log`, `/tmp/base-items-boundaries.log`, `/tmp/base-items-native.json`, `/tmp/base-items-native.cpuprofile` and `/tmp/base-items-build-final.log`.

## Consolidation capacity decoupling

No-provider synthetic runtime execution constructed a daily batch of 522 distinct routine memories with 300 already retained summaries. Request partitioning produced two calls; domain acceptance preserved all 822 summaries, removing the old 256-group/retention ceiling. An 8,192-source stress execution partitioned into 23 requests and accepted 8,492 total summaries in about 693ms; the 522-source run took about 53ms. These timings include batch preparation, schema parsing and domain publication in a generated local world; they are not live database or provider latency.

A separate manual execution of the actual maintenance method with an explicitly synthetic provider completed two chunks and performed one commit, publishing 522 summaries. Injecting a provider interruption in the second chunk produced zero commits and retained all 522 originals. No persisted world was changed by these fixtures, no automated tests were authored/run and spend was $0. Historical failed maintenance jobs were not replayed.

Focused formatting and repository Markdown link inspection passed (121 documents). The production build remains blocked by concurrent knowledge/response work: the latest run reports an `ActorResponse` versus generated response-schema mismatch for the optional `note` field in `ai-director.ts`. Earlier attempts encountered incomplete knowledge fields on `WorldState`. These unrelated changes were left untouched; full-build and live model-quality acceptance remain outstanding.

### Item interaction review

Production TypeScript/Vite build passed after the shared knowledge changes settled, superseding the earlier item-build blocker above; the existing large-bundle warning remains. Manual no-provider domain execution verified preview/execution outcome parity, unchanged preview source state and no returned preview events, fresh mutable custody reads, immutable cache invalidation after transfer, capability/policy loss canceling pending pickup without transfer, immobile in-reach collection, distant immobile rejection and JSON-restored module validation. These executions are not automated regression coverage.

The isolated zero-budget browser world on port 3227 verified arrow-key navigation and selection in the nested God item catalogue, adding a Bone fragment to an existing pile, Pick Up All before alphabetically earlier Bone fragment, and Escape closing the pickup pullout while retaining the root menu. Pile ghost cleanup was code-reviewed; comprehensive renderer/occlusion and large-hover viewport checks remain deferred. No live user-world mutations or paid calls were made.

A five-run synthetic frozen-world exercise with one portable stack per visible pile measured pickup-option construction at 10/100/300 piles. Before: median 6.22/65.61/539.34ms. After shared immutable custody lookup and scheduled-preview admission without action-start finalization: 0.53/5.24/30.98ms (300-pile maximum 41.35ms). This isolates option construction, not browser paint or end-to-end projection. The existing mixed native scenario (344 entities, 180 cold steps, requested 3×) measured total 1,671.90ms, p50 7.03ms, p95 12.50ms and maximum 277.04ms. The one-second 3× budget is still missed; native scale work remains PF00/PF03. Concurrent workspace activity prevents causal attribution of native timings. Local evidence: `/tmp/base-items-review-runtime.log`, `/tmp/base-items-review-after.log`, `/tmp/base-items-review-native.log`, `/tmp/base-items-review-native.cpuprofile` and `/tmp/base-items-review-build.log`. Generated-config validation, focused formatting and whitespace checks passed. The 123-document link audit found two unrelated references to the removed `open-decisions.md#observer-known-names` anchor in concurrent cognition/knowledge documentation; item and base-world links passed. Spend: $0. No automated tests were written or run.

## Editable knowledge and observer names

An isolated native world exercised revisioned general/subject edits, observer-only preferred names, preserved global identity, rejected overflow and stale revisions, 5,000-code-point emoji general text, required subject-note context, SQLite close/reopen, and out-of-sight/re-entry without automatic re-identification. Owner service execution while paused saved both pads and authored acquaintance, restored a named save with prior text/name, rejected an edit from the abandoned restore generation, accepted exactly 1,000 emoji in a subject pad and retained them when 1,001 were rejected. Restart after restore preserved both documents. Further native execution verified edits through remembered-pad references in immediate responses and reflection without re-identifying a newly visible stranger; document references were rejected as speech/name targets. It also verified combined reflection publication, atomic rollback when a later pad exceeded its limit, unchanged revisions for identical edits, independently accepted speech beside a rejected note, stale-encounter rejection, clear/recreate fencing, lossless legacy-file migration and explicit oversized-migration refusal. These are manual runtime exercises, not automated tests.

A 10,000-subject-pad synthetic SQLite exercise measured 30 batches of 10,000 direct lookups: median 2.84 ms, p95 9.12 ms. Initial projection/commit was 1,177 ms; one changed-document commit was 29.07 ms; unchanged-document commit was 0.54 ms. Cold handle allocation for approximately 10,000 entities was 31.07 ms. These local measurements include concurrent workspace activity and establish neither production throughput nor large-history semantic retrieval qualification. Full recall-candidate projection over 10,000 pads (20 runs) measured median 76.88 ms, p95 105.53 ms and maximum 118.62 ms; this includes candidate formatting and excludes vector/provider execution. Changing only a stored preferred name changed its semantic revision while retaining the document revision. Cold document loading/aggregate admission remains CR13.

One live direct model call (`gpt-5.6-luna`) consumed 4,040 input and 299 output tokens (79 reported reasoning-output tokens), estimated cost $0.00136865, with no uncertain completion. Given an introduction and an unverified flooding report in an isolated world, it emitted speech, assigned observer givenName Tomas, and saved the report with explicit uncertainty. Native admission accepted all three operations. This establishes one immediate-decision example, not broad model quality or remote reflection/workspace acceptance. No automatic paid retries were made.

Production TypeScript/Vite build, generated-config checks and focused formatting passed, with the existing large-client-bundle warning. The repository documentation audit found no current broken local links/anchors across 139 Markdown files and no new duplicate task bodies.

A browser walkthrough exposed the paused-owner-edit bug, subsequently corrected by using the existing god transaction lane; native owner/save/restore execution verified the corrected path. Full browser reopen verification encountered a shared localhost session-cookie collision with another running task. SQL/state reopen checks passed independently. No automated tests were written or run; cases are retained in [TODO](maintainers/TODO.md#knowledge-documents--deferred-automated-validation). Local exercise outputs: `/tmp/openlegend-knowledge-exercise.log`, `/tmp/openlegend-knowledge-owner.log`, `/tmp/openlegend-knowledge-boundaries.log`, `/tmp/openlegend-knowledge-recalled-edit.log`, `/tmp/openlegend-knowledge-recall-scale.log`, `/tmp/openlegend-knowledge-provider.log`.

## Simulation and cognition audit

September 24, 2026 observations used Apple M1 Pro / Darwin arm64 / Node 22.23.2, the running PostgreSQL development game, a consistent read-only copy of its mature world, and disposable in-memory runtime scenarios. The user's world was neither reset nor populated with profiling fixtures. Other checkout edits and host load occurred during this work; only the matched snapshot comparisons below establish a local before/after result. [Bounded measurements](verification/simulation-cognition-audit.json) retain scenario counts and timings; private world snapshots, complete prompts and CPU profiles remain outside the repository.

The initial live sample showed native-step p50 about 1 ms but outliers over 500 ms, with long tick/database/GC tails. A later 15-second CPU profile spent about 80% idle, about 562 ms in sampled garbage collection, and substantial active time in draft traversal and status/perception work. These sampled times overlap application spans and are not additive causal percentages. The profile does not establish that inference network latency itself blocks native simulation.

| Workload                                                   | Before                                       | After                                | Scope                                                              |
| ---------------------------------------------------------- | -------------------------------------------- | ------------------------------------ | ------------------------------------------------------------------ |
| Mature 14-entity saved world, 5,000 native seconds         | 2,749 ms elapsed / 3,070 ms CPU; p95 0.93 ms | 2,328 ms / 2,732 ms CPU; p95 0.62 ms | Immutable attribute-definition reads; identical final-world digest |
| Same 514-entity/500-object snapshot, 180 warm native steps | 1,652 ms / 1,629 ms CPU; p95 14.80 ms        | 898 ms / 1,052 ms CPU; p95 6.54 ms   | Sight-cache capacity behavior; identical final-world digest        |
| 20 unchanged monthly usage reads, actual PostgreSQL ledger | 20 queries, mean 12.99 ms per read           | 1 query; warm mean 0.009 ms          | Repeatable-read snapshot, identical projected totals               |

The cold mixed fixture (10 added people, 20 added animals, 300 objects; 344 total entities) took 1,723 ms for 180 native seconds before the sight-cache fix, p95 12.26 ms and maximum 386.98 ms: only 0.58× required native throughput at 3×. A 164-entity crowded fixture (50 added people and 100 added animals) after the fixes took 2,298 ms for 180 seconds, p95 18.79 ms, maximum 39.56 ms, with 14,975 awareness records. Its 1.31× native headroom at 1× leaves little room for persistence and cognition. Neither is a capacity pass. Dense acquisition and moving-observer costs remain PF09/EPR work; no events, sensory fidelity, clock debt or authoritative persistence were dropped to improve a score.

Two browser sampling intervals of approximately ten seconds captured 583 and 601 frames: frame p50 16.7 ms and p95 18.5 ms, scene-update p50 0.1 ms and p95 0.2 ms. The first included a 100 ms outlier; the second maximum was 18.7 ms. Temporary frame logging was removed afterward. A separate 15-second stream sample delivered 336 updates/307 KB, gap p50 50.63 ms, p95 61.57 ms and maximum 103.20 ms, with no gap over 250 ms. This is limited runtime observation, not GPU qualification or proof that all jumps are eliminated.

Inspection of the latest 1,000 stored stages found 17 rejected human-gesture components from Hare, one response with both speech and action in a single operation, many pause/reload cancellations, and two provider-uncertain Jev outcomes. The fixes bind gestures/speech to actual capabilities, reject malformed envelopes in the parsing stage before optional interpretation, and make current speech history identify its speaker, perceived recipient and overheard status. Trigger source and subject visibility are separate. Redundant references, empty planning/withdrawal instructions and long floating-point meter strings were reduced without omitting required evidence. Read-only provider inspection confirmed both Jev runs failed with only `outcome: uncertain` in their run events; no finer cause or billing conclusion is supported, and neither was retried.

Two completed live-provider scenarios used 34 entities, eight added people, twelve added animals and five animals explicitly granted cognition. Eight directed responses and three autonomous responses completed; a twice-attempted out-of-hearing interaction was rejected before inference. A human correctly recalled the previous question; deer and hare used permitted speech/actions without rejected human gestures. Autonomous bystanders explicitly acknowledged overhearing when joining the exchange. A native private-thought marker appeared in its owner's first context and in none of the other actors' inspected contexts. These are limited behavioral observations, not a general privacy or model-quality guarantee. SQLite supplied the fixture repository, so these runs do not qualify PostgreSQL vector retrieval or reflection/dream execution.

Two conversation scenarios settled an estimated $0.034513 with zero remaining reservations. Three additional movement scenarios settled $0.010893: the first exposed mixed speech/action output, the second exposed coincident-destination target loss, and the final run selected and queued a native approach after both fixes. The provider accepted the single-kind `anyOf` schema. Native continuation moved Hare from x=16 to x=14.35 toward the bound x=14 destination after 15 game seconds; this demonstrates motion, not arrival or sustained following. Across 14 cognitive actors in that 34-entity scenario, 30 complete action-candidate scans had p50 about 39 ms and p95 66–81 ms; these are sequential scan timings, not per-tick work. Total estimated settlement for completed accounting is $0.045406. A separate no-provider native execution reached its offered deer-approach destination (20, 0, 18) and ended the movement action after 120 game seconds. Direct schema execution accepted a single speech operation and rejected a mixed speech/thought entry; a heard-impact fixture rendered as a world change, not speech. An earlier disposable run exited while waiting for a presence heartbeat; its $0.50 actor allowance is retained conservatively in the task's accounting, making the verification ceiling $0.545406 against the authorized $10. Later runs maintained presence. There were no automatic paid retries or worker allocations; fresh disposable scenarios after code changes were explicitly verified. Local zero-provider accounting execution also exercised reservation, uncertain settlement, late priced settlement and interrupted-attempt recovery; displayed totals followed each mutation without authorizing spending from the cache.

Generated configuration, formatting, whitespace and production TypeScript/Vite build checks passed; all 139 tracked Markdown documents had valid local links/anchors and no newly duplicated task bodies. The existing client-bundle size warning remains. Automated tests were neither added nor run. Deferred regression, scale, vector, reflection and recovery coverage remains in [TODO](maintainers/TODO.md#simulation-and-cognition-audit-regression-coverage), with implementation progress in PF03/PF04 and CR03.

## Spoken introduction learning

Read-only inspection of the running world found player knowledge with no observer names and retained speech saying “I’m Ada” while the speaker label remained “a person.” The original speech operation carried text/addressee only; private name operations changed the speaking actor’s own knowledge, leaving the player without a reception path.

Manual isolated runtime execution with the actual WorldService, response admission and public projection changed both entity and conversation labels from “a person” to Ada after an annotated introduction, retained the global name, and preserved the learned name across SQLite close/reopen. Repetition retained the same name revision; an announced alias updated only the observer label. Invalid nonliteral metadata preserved speech and prior knowledge. Distant listeners and a hearing-only observer did not learn the visible entity identity. Ordinary unannotated speech did not fabricate an introduction.

One live direct `gpt-5.6-luna` call answered a name question with “My name is Ada.” and `selfIntroduction: Ada`. The production bound schema and native response admission accepted it; the player’s projected name became Ada. Usage was 4,514 input and 73 output tokens; estimated cost $0.00121595, no uncertain completion or retry. This is one behavioral example, not broad language-quality qualification.

A synthetic speech-fan-out exercise (12 executions per mode/population) measured plain/introduction median 2.47/3.30 ms with 25 additional listeners and 13.79/19.52 ms with 200. The introduction maximum at 200 was 146.23 ms under concurrent workspace load. This isolates native event reception, not end-to-end simulation or provider throughput. The isolated browser walkthrough was unavailable: IPv6 loopback was blocked by the browser and a separate-browser connection timed out. Server projection and persistence were executed directly. Production TypeScript/Vite build, generated-config validation, focused formatting and documentation link checks passed; the existing bundle-size warning remains. No automated tests were authored or run; deferred cases are in TODO. Local evidence: `/tmp/ol-introductions-runtime.log` and `/tmp/ol-introductions-provider.log`.
