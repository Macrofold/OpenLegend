# Inventions and world evolution implementation tracker

## Spatial family dependencies

[SW03–SW05 and SW13](spatial-world.md) supply eligible geometry and placement adapters. Existing recipe authoring does not wait for flight, a GLB pipeline or full 3D completion. Keep creator versus actor authority, private invention evidence and actual activation/crafting separate. The first spatial branch installs native demo geometry, not an arbitrary generated-creature or building authoring capability.

This is the sole implementation tracker for declarations, invention, conjuring and world-evolution delivery. Start with the [invention foundation](../invention-foundation.md) and its [constitution](../world-constitution.md), [validation](../invention-validation.md), [runtime art](../invention-art-pipeline.md), and [budget](../invention-budgets.md) contracts. [Declarations, reusable mechanisms, and world evolution](../../archive/07-technical-architecture/declarations-and-evolution.md) remains the mechanical admission/activation owner. Product behavior for the creator workflow belongs to the [world-agent and workshop design](../../archive/03-design-proposals/world-agent-and-workshop.md), and ownership policy belongs to [invention governance](../../archive/03-design-proposals/invention-governance-and-ownership.md).

The first attribute family uses declaration admission for bounded definitions and presentation-only revision/removal, with god authority and restore-generation checks. This is a narrow EWF consumer; INV-1–INV-5 workflow, general family composition and natural-language authoring release gates remain open. See [Architecture](../architecture.md#extensible-attribute-foundation).

INV-9–INV-14 add the coordinated invention foundation. They extend the existing service and the unfinished responsibilities below; they do not replace INV-1–INV-8, reset their acceptance, or create a second task registry. All newly added tasks are uncompleted implementation/qualification work. EWF, AG, EPR, CR, NC, PF, SL and production-data retain their subsystem ownership.

### Destination and the smallest useful first release

The eventual experience is: a player or NPC expresses an intent; the system finds what already exists, identifies the missing capability, authors the smallest compatible addition, validates and activates it under world policy, and makes it available through ordinary interactions. Creator workshop changes can deliberately revise laws; ordinary invention must discover possibilities consistent with the world's premise and relevant established outcomes. An AI discussion, a stored description and a successful provider run are not an executable mechanic.

**The first release should let a right-click Invent request produce a usable, persisted recipe from an existing supported family, inside the world-agent conversation.** It need not wait for world-wide retrieval, generic material simulation, cross-world pack distribution, or executable algorithm generation. It must use the same authoritative invention workflow that later supports those capabilities. Subsequent releases expand what that workflow can admit rather than introducing a separate route for each UI or model.

### Inspected starting point

- [x] **BASE-1 — Existing finite admission:** `packages/domain/src/declarations.ts` validates generated sling/bow/arrow declarations, deduplicates content, commits definition/recipe records and records admission. This is a narrow family contract, not general mechanics generation.
- [x] **BASE-2 — Existing invention execution:** `apps/server/src/ai-director.ts` retrieves known recipes, routes reuse or one of three supported families, generates a typed draft and invokes domain admission. Its player-specific orchestration and family switch need extraction, not duplication.
- [x] **BASE-3 — Existing conversation entry:** `apps/client/src/world-agent.ts` opens a new conversation and submits `Invent this: …`. `apps/server/src/macrofold.ts` runs persistent chat with player-scoped context but explicitly denies mutation tools and only returns text. It does not submit chat inventions to admission.
- [ ] **BASE-4 — Complete live gameplay evidence:** a real world-agent request produces an admitted invention, which is crafted and used after reload. Basic provider/harness connectivity is insufficient evidence for this gate.

- [x] Qualify fresh live supported-sling generation/admission, native craft/equip/hunt after reopening, paraphrased semantic lookup, explicit reuse and derived modification; exercise the production browser history/choice/clarification/draft flows. [Evidence and limits](../verification.md#supported-invention-loop) retain the broader browser-originated generation/use gate and provider abstention explicitly.

### Scoped performance review delivery

- [x] Filter actor invention context and recent-method lookup by timeline in indexed SQL before applying the 3/50-row limit; retain public historical pagination across timelines.
- [x] Recover only unfinished job payloads in indexed batches of 50 while preserving interrupted/uncertain receipts and conservative reservation accounting.
- [x] Revalidate search knowledge membership in linear work and serialize only identity/revision pins in vector scope joins, without changing native admission or ranking semantics.

These narrow implementation changes do not complete INV-9–INV-14 or certify hosted scale. Current behavior belongs in [Architecture](../architecture.md#bounded-invention-history-and-recovery), observations in [Verification](../verification.md#invention-performance-review), and requested automated coverage in [TODO](TODO.md#invention-performance-review-regression-todos).

### Contracts to establish early, without building a universal framework

Keep these as small typed application/domain interfaces with in-process implementations first. SQLite can remain the repository; Macrofold remains one replaceable execution adapter. Exact field names are implementation decisions, not a new frozen public API.

| Contract                         | Owns                                                                                                                                              | Must remain separate from                                    |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| Invention request                | Stable request ID, world/epoch, principal, initiating player/NPC authority, actor if applicable, intent, target evidence, dependencies and budget | Provider run/session identity and browser-authored authority |
| Draft and candidate version      | Stable invention ID, schema/family version, immutable candidate digest, provenance, dependencies, assumptions and unresolved requirements         | World installation, character knowledge and item instances   |
| Authoring executor               | Bounded generation, clarification or abstention; evidence and typed proposal output                                                               | Rules, permissions and authoritative admission               |
| Capability family registry       | Trusted validators, applicability, supported effects, execution/action adapters and presentation defaults                                         | A growing central switch over named finished inventions      |
| Admission and activation         | Current policy, compatibility, deterministic checks, atomic world installation and receipt                                                        | Conversation prose and model confidence                      |
| Learning and action execution    | Which actor knows the result; ordinary prerequisites, resource consumption, progress and effects                                                  | Merely installing a definition or opening creator chat       |
| Conversation and creator queries | Durable conversation state, task references and audience-scoped reads                                                                             | Canonical world storage and omniscient NPC context           |

For initial compatible G1 additions, candidate approval and world activation may occur in **one database transaction**, but keep their identities/outcomes distinct. This permits later preview, revision, delayed activation and migration without changing the meaning of “invented.” A recipe's first version need not require a package manager or a general graph service.

### INV-1 — One shared, authorized invention workflow

**Depends on:** the existing finite admission path. **Unlocks:** a reusable backend service before changing UI behavior.

- [ ] **1.1 Extract the existing pipeline.** Introduce an application-owned invention service for resolve/reuse, author, validate and admit. Adapt the current endpoint and director to it. Carry actor identity explicitly instead of hard-coding `player`; keep typed execution free of game policy. Accept an explicit initiating actor/origin, actor-authored method and scoped continuation; remove reusable orchestration’s `controlledEntityId` assumptions.
  - [x] Extract the supported-family pipeline with explicit initiating scope and a small execution port; direct Invent, explicit world-agent Invent and NPC complete proposals/revisions share it. Player delegation remains pending.
- [ ] **1.2 Persist request and draft identity.** Store the initiating request, candidate/digest, status and authoritative receipt independently of the bounded event feed, provider result retention and browser localStorage. Reference these from conversations. Use a repository interface over the current database; no distributed infrastructure prerequisite. Include actor proposal revisions, neutral clarification state and private/public provenance without copying private goals into public metadata.
  - [x] Retain request scope, candidate/digest and admission receipt in the existing repository; checkpoint before admission and commit acceptance atomically. Indexed private history supports player and actor revisions independently of browser retention.
- [ ] **1.3 Introduce the common policy gate.** Persist independent player/NPC invention locks and a policy revision; check them before authoring spend and at activation. Preserve origin through delegation and every entry point. Implement the accepted [NPC default and enablement gate](../../archive/03-design-proposals/invention-governance-and-ownership.md#open-and-locked-invention); player invention is open by default for the prototype. Expose both independent settings when implemented. Owner edits follow the player gate until a separate administrative exception is explicitly adopted. Carry origin and lock checks through clarification/result continuations; goal edits and creator delegation confer no extra permission.
  - [x] Persist independent locks, revision and per-origin revocation markers; bind direct invention jobs/provenance to a server-established origin and inventor. Gate before paid work and at declaration admission; cancel revoked work without reviving it on unlock.
  - [x] Gate existing owner attribute/cognition/story definition edits, preserve ordinary actions/state edits, and expose saved settings with revision/restore fencing. Player default is open; NPC default is locked.
  - [x] Qualify and expose NPC unlock for complete supported-family proposals and private revisions; preserve the locked default.
  - [ ] Carry authority through explicit player delegation, imports and general workshop continuations; retain historical policy audit beyond the current saved record.
- [ ] **1.4 Make results truthful and recoverable.** Return typed outcomes for reused, needs clarification, draft ready, activated, unsupported capability, forbidden premise, stale/conflict, cancelled, unavailable and uncertain execution. A pending request can be read after restart without replaying generation. Retain stable mutation IDs and do not add automatic paid retries. Persist sanitized actor-visible results separately from technical validation records, including insufficient detail, unknown evidence and invalid-method reasons under the existing outcome vocabulary.
  - [x] Preserve distinct result codes and inspectable candidates; recover interrupted/uncertain work without replay. Project historical success separately from current installation. Private actor validation feedback supports later explicit revision.
- [ ] **1.5 Bind admission to relevant dependencies.** Recheck world identity, authority, applicable lock history, candidate digest and referenced definition versions. Unrelated simulation ticks should not invalidate a recipe; a revoked request must not become valid merely because a lock later reopens. Reserve authoring cost separately from eventual crafting resources. Recheck revocation at each continuation as well as activation.

**Completion gate:** direct requests and delegated requests reach the same admission policy and durable receipt. Duplicate delivery admits once; invalid proposals leave definitions, knowledge and resources unchanged. Existing crafting and learning still work while invention is locked.

- [x] Persist clarification/revision as linked jobs with root identity, prior method/feedback, original authority and an atomic one-child claim. Reject revoked/restored-away continuations; bound follow-ups and preserve spending. The private NPC slice uses these same boundaries.

### INV-2 — Make right-click Invent playable through world-agent chat

- [x] Qualify live bow/arrow generation and native craft/equip/fire after reopening; clarify feasibility without lowering confidence. Broader browser/adversarial coverage below remains open.

**Depends on:** INV-1. **This is the first user-facing release; do not wait for later stages.**

- [ ] **2.1 Preserve structured intent.** Send `invent` versus `ask`/`workshop` as an explicit request mode, plus optional clicked entity/location and observed revision. The server verifies references and binds authority. Do not infer authorization solely from an `Invent this:` string or silently turn quoted conversation into authoring.
  - [x] Route Invent entry points to the world-agent Invent view and send explicit `invent`/`discuss` mode. Bind actor, authority, world and timeline on the server. Clicked-object scope and workshop mode remain pending.
- [x] **2.1a Index and retrieve similar inventions.** Implement the [similar-invention contract](../../archive/07-technical-architecture/declarations-and-evolution.md#similar-inventions-before-authoring) with versioned text embeddings, vector search, authorized scope and applicability filters; refresh changed definitions and expose incomplete/unavailable search honestly. Run on explicit Invent intent before new candidate generation.
- [ ] Bound cold invention indexing per request/episode by missing sources, bytes, billable units and elapsed work; expose incomplete progress and resume without re-embedding settled sources or representing incomplete search as no matches. Reuse the existing vector store and INV-13 budget admission; qualify exact-search/reconcile work and repeated per-actor embedding before adding approximate/shared indexes. Preserve destination knowledge/rights and honest SQLite/provider-unavailable results.
- [x] **2.1b Add the Similar inventions modal.** Present ranked matches and differences, with Use existing, Modify existing, Invent new and Cancel. Preserve intent and selected version across continuation; recheck permissions and prerequisites. Modification creates a validated derived draft, never an in-place edit. No matches proceeds normally; unavailable search offers retry or explicit continuation. Do not generate while awaiting a choice or replay generation on reopening.

- [ ] **2.2 Add a bounded invention operation to the world-agent gateway.** It invokes INV-1 under the conversation's server-bound scope. An initial typed final-response envelope plus server dispatch is sufficient; MCP/tool streaming is optional. After the similar-invention search and any modal choice, an explicit Invent request can enter the authoring path without paying for a preliminary assistant turn just to repeat the request. If the agent already produced a valid candidate, submit that candidate rather than commissioning a second generation. Use a valid actor/player-supplied method directly, retaining conceptual fidelity and requiring acceptance of substantive substitutions under the declaration owner.
  - [x] Implement bounded supported-family authoring without preliminary chat or full harness; accept complete supplied candidates without rewriting. Broader freeform method investigation remains pending.
- [ ] **2.3 Render structured progress and receipts in the conversation.** Show reuse, relevant clarification, authoring, validation, blocked and installed outcomes. Give the admitted result an inspectable reference and a Craft/use affordance when appropriate. Assistant prose cannot claim installation before the committed receipt. Keep follow-up drafts attached to the same invention task.
  - [x] Show durable scoped status, saved proposal, cancellation and current-versus-historical recipe availability in the world-agent Invent view. Linked follow-ups retain the conversation/root identity, prior proposals and feedback. Results include native Craft actions and current material blockers; delayed responses preserve newer drafts and polling retains loaded older pages. These are structured Invent records, not a merged Discuss transcript.
- [x] **2.4 Define installation versus learning.** A player-character invention can grant knowledge through the existing discovery rule. A creator-only installation does not teach every character or spawn objects. Clicking Craft submits a separate ordinary action with fresh prerequisites; requesting an invention is not permission to consume inventory automatically. Learning a technique grants neither proficiency nor an item; no universal skill subsystem is a prerequisite.
- [x] **2.5 Close the delivery gaps.** Persist enough server-owned conversation/task linkage to recover pending work and receipts after reload, handle simultaneous tabs and deduplicate double submits. Reopening, switching tabs or reconnecting must not dispatch. A paused world may support discussion/drafting; activation waits for the allowed simulation boundary and revalidation.
- [ ] **2.6 Exercise the real loop separately from fixtures.** Right-click Invent → embedding/vector lookup → choose reuse, modification or new invention when matches exist → generate a supported recipe when needed → admit exactly once → display available crafting → gather missing ingredients → craft/use → reload and reuse without regeneration. Also cover paraphrased matches, no matches, inaccessible records, failed search, modal cancellation and duplicate continuation. Show an unsupported request honestly, with no invented item or fake success.

**Authorization policy for this slice:** the explicit Invent interaction can authorize automatic activation of a new compatible recipe within a declared low-impact G1 envelope. Do not add a confirmation dialog to every normal invention. Shared-law changes, destructive migrations or unclear scope require the distinct workshop policy and a concrete diff. Ask only consequential clarification; use bounded defaults for cosmetic details.

- [ ] Qualify similarity threshold across a larger set of paraphrases and materially different requests; the initial live examples are not broad retrieval-quality evidence. Broaden clear/ambiguous bow admissibility sampling beyond the successful feasibility-rubric correction; retain abstention when confidence is insufficient. Exercise the full 2.6 matrix; deferred automation stays in TODO.

### INV-3 — Expand beyond the three recipes through registered families

**Depends on:** INV-2's functioning loop. **Unlocks:** genuinely different inventions without rewriting chat/admission each time.

- [ ] **3.1 Register capabilities, not finished answers.** Move existing launcher/ammunition families behind versioned descriptors: input schema, material roles, applicability, permitted reads/effects, cost/work limits, validator, execution adapter and failure behavior. Reject incompatible development saves under the active save/load policy; preserve same-version definitions and the separate INV-5 active-definition/version semantics. Consume EWF01's common host/manifest metadata; retain ownership here of the mechanical family schema, validators and execution adapters. Do not create two registries with independently writable family versions. Contract extraction and adaptation of an already supported native family may be performed early for the first EWF consumer; complete INV-3 release still includes its existing playable-loop dependencies.
  - [x] Extract the three existing family descriptions/identity and finite contract to one domain module consumed by routing, search and admission. Native execution and the flat proposal shape remain unchanged; this does not complete the full family registry.
- [ ] **3.2 Generalize the proposal envelope.** Use stable kind/family/version references and bounded family-specific payloads, rather than a universal set of weapon fields. Allow a small dependency bundle such as recipe + output definition + action description. Keep G0 interpretation, G1 declarative changes and unsupported host requirements distinct. Reuse family validators for menu and freeform resolution. Bind the exact installed family/version under [runtime §3](../../archive/07-technical-architecture/world-module-runtime.md#3-definition-envelope-and-composition).
- [ ] **3.3 Make action discovery data-driven.** Families project parameter schemas, target rules, prerequisites and executable bindings into the complete action catalogue, crafting UI and NPC candidates. A newly admitted action becomes discoverable through the existing UI; rendering a new label alone must not make it executable. Visibility, knowledge and target availability remain authoritative filters. Expose the same bindings to AG05 prose resolution; do not introduce a central switch over named inventions. Use one applicability/execution source under [runtime §7](../../archive/07-technical-architecture/world-module-runtime.md#7-action-families-and-agency-integration).
- [ ] **3.4 Add common presentation requirements.** Each invention includes bounded item/action descriptions and a symbolic action representation, with appropriate common target/state variants. Publish custom icons/art asynchronously by artifact/version identity; use a trusted fallback immediately. Presentation failure cannot roll back valid mechanics or change collision, reach or effects.
- [x] **3.5 Prove extension with a non-weapon family.** Implement a carrying/container assembly or similarly small utility family with real capacity, containment and resource semantics. Generate a new instance of that family through the same conversation pipeline. No name-triggered canned recipe. Choose one useful family at a time; do not build the entire future material system first. Include an admitted output as a later supported input where feasible, retaining positive material/resource validation.
  - [x] Add a live-generated gathering tool with native crafting, bounded carried-tool yield and finite resource conservation; retain it across restart.
  - [ ] Add equipment selection, wear or invented-input chaining only with a concrete consumer; containers remain separate work.

**Completion gate:** adding a trusted family changes its module/registration and necessary engine support, not the conversation protocol or a switch over named inventions. Same-version recipes remain intact; incompatible development saves fail explicitly, and unsupported operations remain explicit gaps.

- [ ] **3.6 Parameterized reusable constructs and specializations.** Implement one bounded family-owned construct representation with explicit typed ports, supported binding phases, partial/closed specialization, exact dependencies and conservative effect/capability/work summaries under the [shared contract](../../archive/07-technical-architecture/world-module-runtime.md#reusable-constructs-and-specialization). Start with existing selector/effect/lifetime capabilities and EX04; do not require a universal language or graph editor. Reject incompatible effect substitution, unresolved required ports, invalid cycles and aggregate fan-out. Reuse INV-1 admission and INV-5 version/activation; EWF11 owns only cross-subsystem integration. Test independent invocation state, named resource semantics, template versus executable status, same-version restore, and pinned upstream versions.

- [ ] **3.7 Qualify positive composition and shared schema ownership.** With INV-3.1/3.2/3.6 and EWF11, derive authoritative dependency/read/effect summaries and schemas from actual registered family implementations. Positive consumer contracts must certify an invented output's structural/resource/role semantics before it can be reused as an input; simply deleting the native-only guard is not sufficient. Keep world-specific default consumers local and prove a materially different non-weapon/subsystem composition.

### INV-4 — Give the creator useful, scoped world investigation and workshop tools

**Depends on:** INV-1/2 identities and permissions. Can proceed alongside INV-3.

- [ ] **4.1 Separate creator and embodied audiences.** Introduce server-granted creator query capabilities over the authorized world. Cover entities, definitions, installations, character context and events through bounded typed reads. Never send the whole world snapshot, credentials or other actors' private memories to ordinary player/NPC jobs. Creator-only findings stay out of character knowledge unless learned through a valid in-world event.
- [ ] **4.2 Make history durable and navigable.** Persist the declared event coverage and technical invention artifacts beyond UI scrollback. Support exact lookup and paginated time/entity/version filters with evidence IDs, revisions and honest gaps. Add database indexes when needed; broad semantic search is not required for basic inspection.
- [ ] **4.3 Expose tool-shaped read and draft operations.** Resolve a definition, inspect dependencies, retrieve permitted evidence, get/update a draft and request validation through the same application services used by UI. Macrofold gets bounded capabilities or an equivalent server-mediated loop, never direct database writes or policy-setting authority. Include supported module/construct discovery and candidate validation/diff operations.
  - [x] Expose actor-scoped catalogue/material/recipe/used-system inspection and native dry-run tools through a bounded app tool dispatcher. A review-before-install loop can call them through ordinary inference. General creator-world queries, executable impact closure and additional definition-kind adapters remain pending.
- [ ] **4.4 Add durable multi-turn workshop state.** Record base version, intended scope, draft revisions, unresolved questions and validation reports. Present before/after mechanics and affected objects/processes. Keep conversation sessions replaceable; the canonical draft must survive provider-session loss. Keep requested behavior, assumptions, explicit unbound choices and source-to-binding traceability in this same versioned draft/session.
  - [x] Retain finite workshop candidates (including rejected drafts), native findings, selected base pins and exact-digest Apply through existing job/continuation records. Typed intent constraints, appearance selection and independent parallel branch coordination remain pending.
- [ ] **4.5 Enforce revocation and output audiences.** Recheck grants on each query, draft publication and activation. Separate creator explanation from public invention descriptions; private source evidence must not accidentally appear in a recipe, public event or exported artifact. Filter private dependencies in both friendly explanations and technical inspection.
- [ ] **4.6 Add confirmed god-mode conjuring.** Implement the [world-agent conjuring contract](../../archive/03-design-proposals/world-agent-and-workshop.md#confirmed-god-mode-conjuring): named or random candidate, reuse or guided definition of all relevant properties/dependencies, concrete quantity/location/effects/art-budget preview, explicit confirmation, and a separately authorized idempotent instance-creation operation. Automatically queue missing artwork after confirmation through INV-3.4 and the runtime art pipeline; distinguish valid creation with pending art from unsupported mechanics. Recheck scope, locks and revisions; preserve the confirmed random choice and receipts across restart. Verify cancellation before confirmation, duplicate submissions, revoked access, unsupported families, art failure and late art without duplicate spawning or paid retries. Existing families can ship first; expanded families/art depend on INV-3 and the asset pipeline, and shared-law changes still depend on INV-5.

- [ ] **4.7 Add bounded invention investigation.** After scoped tools in 4.3 and relevant INV-3 families, let a harness inspect materials, existing mechanisms and missing dependencies before producing a reusable candidate. Preserve the simple one-call path, reuse valid drafts and return unsupported host requirements honestly; do not add a mandatory preliminary call to explicit Invent requests.
  - [x] Add the four-turn/eight-tool ordinary-inference workshop over currently supported recipes; inspect→validate→prepare is implemented, with the zero-call supplied route preserved. Live investigation quality and larger family/dependency scope remain unqualified.
- [ ] **4.8 Add evidence-driven world investigation.** After 4.1–4.3, let the harness choose successive bounded queries over events, definitions and permitted character context. Demonstrate an explanation whose next query depends on prior evidence; cite records and gaps, enforce cumulative traversal budgets and keep god findings out of NPC knowledge.
- [ ] **4.9 Coordinate complex conjuring through tools.** Extend 4.6 once property/dependency and asset tools exist: inspect/reuse definitions, complete the reviewable candidate, then coordinate approved mechanics and art within the confirmed scope. End the run while waiting for user confirmation or long-running art; resume from durable task state without rerolling the candidate or duplicating paid generation.

**Completion gate:** the authorized creator can investigate an invention, cite its actual definition/history and prepare a scoped revision. The same tools called under ordinary player authority cannot reveal private NPC context or grant world-edit privileges.

- [ ] **4.10 Natural-language module authoring and refinement.** Deliver the [world-agent authoring workflow](../../archive/03-design-proposals/world-agent-and-workshop.md#natural-language-world-and-mechanic-authoring) using existing request/draft/tool/activation services, with EWF12 providing runtime support and explanation metadata. Demonstrate a supported new need, touch-only organism configuration, or reusable construct specialization without requiring hand-authored JSON. Ask consequential questions, propose reversible defaults, expose unsupported host behavior, and bind review to the exact current candidate/scope. Preserve low-impact invention versus consequential world-change/conjuring approval rules. Test concurrent chat/raw edits, stale confirmation, canceled work, permission/lock changes, provider failure, duplicate requests, and truthful installed-versus-draft reporting. Native fixtures establish contracts; separately capped live evaluation must demonstrate useful explanations and no silent intent substitution.

- [ ] **4.11 Extend scoped tools to a concrete creator-owned definition kind.** Under INV-4.1/4.3 and EWF12, implement discover/inspect/draft/native-preview/diff for one supported attribute or sense family. Bind god/creator scope in the application, not tool JSON; keep current player-lock and payer rules. Use existing native admission and INV-5 activation for consequential replacement. Do not promise a creator-wide query engine from actor-only metadata.

- [ ] **4.12 Integrate tools with the chosen conversation/actor surfaces.** Connect general Discuss or a dedicated creator mode to the same dispatcher only with explicit audience/mode and confirmation semantics; actor tool investigation belongs to AG08/CH01 and must not inherit a human Apply requirement or private creator evidence. Preserve the cheap ordinary Invent and supplied-NPC paths. Compare natural-language usability through capped live and browser evidence before declaring the feature seamless.

### INV-5 — Versioned workshop activation and existing-state migration

Coordinate module/save integration with [EWF07](extensible-world-foundation.md#ewf07--module-lifecycle-and-current-format-save-integration); this is the sole declaration/module activation mechanism. Analyze specialized templates, active effects, controllers/senses and retained-save pins. Removal detaches only its own ongoing contribution, never restores an old target snapshot; unsupported detach blocks retirement or requires explicit approved recovery.

**Depends on:** INV-3 version/family contract and INV-4 workshop state. **Unlocks:** changing existing inventions safely.

- [ ] **5.1 Separate candidate approval from installation.** Persist immutable versions, parent/fork lineage, pinned dependencies and per-world installation manifests. For a new compatible recipe, keep the simple transaction; for a revision, require an explicit family compatibility/migration result.
- [ ] **5.2 Rehearse affected-state changes.** Identify instances, learned references, pending work and owning systems. Preserve elapsed work, consumed materials, stored contents and pending contributions. Do not let an old owner and a replacement both update the same state.
- [ ] **5.3 Activate at a committed boundary.** Check expected base/profile/policy versions, migrate or quiesce affected processes, then atomically commit definitions, state and receipt. Preparation failure keeps the old installation. Recovery after a crash reads the receipt instead of repeating migration.
- [ ] **5.4 Handle conflicts and retirement.** Concurrent edits report conflict/rebase rather than last-write-wins; retain versions still referenced by instances, processes or the replay horizon. Quarantine affects declared scope and has a visible recovery policy. Rollback is a forward compatible change, not undoing unrelated history.

- [ ] **5.5 Add iterative workshop refinement.** After 4.3–4.4 and 5.1–5.3, give a bounded harness access to draft diffs, trusted validation reports and authorized scenario/migration previews. Use returned findings to revise within the admitted allowance, then present consequences before activation. Compare the roof-waterproofing or burn-rate case with one-call authoring; validator success is not permission to change the live world.

**Completion gate:** a workshop revision changes actual behavior while preserving existing resources and progress; rejected or interrupted activation never leaves mixed definitions/state.

### INV-6 — Composable materials, assemblies and passive world processes

**Depends on:** INV-3 and INV-5. **Unlocks:** reusable physical interactions rather than more isolated item recipes.

- [ ] **6.1 Add typed properties and ownership incrementally.** Register units, ranges, applicability, defaults, provenance and a single state owner. Distinguish unknown/conflicting/not-applicable; no arbitrary zero or immunity from missing data. Prefer component/material composition over attaching every conceivable field to every object.
- [ ] **6.2 Add bounded declarative composition.** Trusted operations supply mandatory reads, permitted writes, transfer accounting and evaluation limits. Declarative predicates/formulas may compose those operations within finite limits; new prose, property names or JSON keys cannot invent host effects.
- [ ] **6.3 Establish the fixed-step contribution protocol.** Read one start-state, aggregate competing resource claims, then atomically apply bounded contributions/transfers in stable order. Record time/random inputs. Enforce work and scheduled-descendant limits; no unbounded recursive updates or AI per tick.
- [ ] **6.4 Deliver one vertical physical example.** Build a modular shelter with rain/exposure/moisture behavior; then add drying/combustion as separate registered consumers/owners when supported. Cover material defaults, attachment changes, passive exposure during AI outage and version-preserving migration. Avoid implementing a full physics simulator as the first milestone.
- [ ] **6.5 Retain dormant influence notes.** Store capped anticipated consumers/causes with triggers, uncertainty and dependencies, but no executable effect or automatic recursive authoring. An unsupported immersion influence is neither implemented nor forever impossible.

**Completion gate:** two independently invented objects reuse the same admitted law, including unattended environmental effects. Resource accounting and behavior remain coherent without a model connection.

### INV-7 — Discover missing mechanics during play without endless generation

**Depends on:** INV-1 for the existing supported-family actor-method loop, coordinated with AG08 for actor continuation. Family expansion requires the relevant INV-3 validators/adapters; passive-system discovery additionally requires INV-6. The initial actor loop does not wait for the entire player modal, general family expansion or creator workshop.

- [ ] **7.1 Introduce a missing-capability request.** An unusual action/environmental interaction records the relevant family, missing contract, evidence, intent, current profile and bounded scope. Classify existing execution, semantic interpretation, supported new declaration, forbidden premise and unsupported host operation separately. Not every new encounter needs a permanent definition. Include insufficient actor-method detail, unknown evidence, invalid method and currently blocked execution with sanitized actor-scoped feedback; an unknown is not physically impossible.
- [ ] **7.2 Retrieve before authoring.** Reuse INV-2's text-embedding/vector retrieval alongside IDs, aliases, family applicability and lexical lookup behind a replaceable retrieval port. Apply actor-scoped discovery rules; autonomous NPC selection does not open the player modal. Jev can select among supplied eligible alternatives or abstain. Similarity never proves equivalent mechanics or changes unknown into impossible. Internal equivalent-definition reuse cannot disclose an unknown private design or erase independent discovery provenance.
- [ ] **7.3 Bound autonomous invention.** NPC goals/attention and meaningful blocked-action events may initiate requests under the agent lock. Coalesce compatible concurrent requests, use cooldowns/negative-result expiry and reserve cost before dispatch. Player delegation preserves player origin. Native survival does not wait on generation. Budget/deduplicate clarification and rejected-method episodes; actor memory, intentions and continuation remain [AG08](agent-agency.md#ag08--actor-side-invention-continuation-and-choice) work.
- [ ] **7.4 Revalidate resumption separately.** An activated capability can outlive its initiating request. Before resuming an action, recheck target, actor intent, prerequisites, authority and cancellation; activation alone does not perform it. Invalidate affected lookup/availability caches without regenerating every actor's context. Test abandoned/superseded goals, private result delivery, cancellation before result and invention without construction; no nearby actor receives the private discovery.
- [ ] **7.5 Validate consistency and measure value.** Check candidate effects against the profile and relevant past outcomes, without overgeneralizing one observation. Record reuse rate, deferrals, latency, spend and gameplay consequences separately. An error does not trigger an automatic paid repair loop. Record actor-proposal versus compiled-method fidelity, including explicitly accepted revisions; live evidence remains separately capped and authorized.

- [ ] **7.6 Connect character discovery across gameplay events.** When richer investigation needs CH01 and relevant INV-3/INV-6 support, connect a scoped planning harness to permitted missing-capability requests, actual action receipts and later observations. Exercise leaking shelter → inspect known materials → propose an improvement → construct through native actions → observe later rain → reconsider. Keep character hypotheses distinct from engine authoring, enforce the agent invention lock, and preserve progress across fresh jobs without waiting inside a paid run. CH01 is optional for richer investigation; a supported one-call method proposal uses the same service without that harness prerequisite.

**Completion gate:** an eligible unsupported interaction can become a reusable supported behavior, or remain an honest bounded deferral, without per-step inference, duplicate world laws or accidental action execution. The admitted method traces to the actor’s conceptual proposal or explicitly accepted revision. Verify private delivery near another actor and results after cancellation; reuse the [AG11 scenario matrix](agent-agency.md#ag11--integrated-deterministic-and-adversarial-acceptance) rather than duplicating it.

### INV-8 — Portable inventions and later algorithm extensions

**Depends on:** stable identity, activation and permissions. These do not block INV-2 through INV-7.

- [ ] **8.1 Add creator library and complete world-pack inventory.** Separate original authorship, learning, imports and installation; retain exact versions, rights and private-dependency blockers. Begin with a local library, not a marketplace service. Inventory distinguishes partial templates from runnable specializations and records destination bindings, derivation/credit and use/modification rights.
  - [x] Store server-derived creation attribution separately from deduplicated mechanics: world creators own NPC creations, player and world creators jointly own player creations; preserve independent discoveries and expose an NPC-created filter over known recipes.
  - [ ] Expand these local records into authorized account libraries and complete world-pack inventory; ownership does not grant character knowledge, publication or marketplace rights.
- [ ] **8.2 Export/import immutable manifests.** Pin authorized dependency closure, compatibility and lineage; preview conflicts and activate through INV-5 under the destination's policy. Export definitions, not private memories or a whole save. Partial distributable payloads must be labeled partial. Require explicit semantic bindings/adapters, exact dependency closure and exclusion of live invocations/private state; coordinate [EWF11](extensible-world-foundation.md#ewf11--reusable-construct-integration-and-local-portability-proof) and [EX10](../extensible-world-examples.md).
- [ ] **8.3 Resolve contribution terms before shared publication.** Implement consent, creator retention, free-use eligibility and co-authorship policy before promising complete freely reusable packs. Paid marketplace work follows proven free sharing/import.
- [ ] **8.4 Gate G2 separately if needed.** Only after a concrete mechanic cannot fit admitted G1 operations, evaluate a restricted deterministic algorithm runtime against a fixed query/effect interface. Require isolation, instruction/memory/output limits, replay, quarantine and migration evidence. No `eval`, generated host JavaScript, model-driven engine installation or Macrofold sandbox executing each simulation tick. Activating this future capability requires a separately approved engineering change; it is not available under current runtime rules. Use [runtime §12](../../archive/07-technical-architecture/world-module-runtime.md#12-restricted-algorithms-remain-conditional) and EWF10’s conditional consumer review.
- [ ] **8.5 Keep G3 an engineering release.** A new privileged operation, storage meaning or interpreter behavior requires reviewed host code, compatibility handling and migration. Preserve unsupported requests as design input; never claim the model has silently implemented them.

### INV-9 — Coordinated editable invention projects

**Design owner:** [Invention foundation](../invention-foundation.md). **Depends on:** the delivered INV-1/2 request lineage and relevant INV-4.4/4.10 workshop interfaces. Work can start with the four current families; it does not wait for all remaining INV-1–INV-8 or for a general interpreter.

- [ ] **9.1 Add the selected revision boundary.** Extend the current root/continuation records with one selected draft revision, immutable submitted candidate identity, intended change scope, pinned user constraints and references. Retain separate active definition/install/visual identities. Preserve the existing single-child claim and explicitly reconcile concurrent edits instead of silently replacing it with last-write-wins.
- [ ] **9.2 Unify editing views.** Make chat, forms, and authorized technical editing read/write the same expected revision; support appearance-only, function-only, and combined requests. Retain a selected silhouette/material when requested. Summaries and generated briefs must derive from the draft rather than become alternate state.
- [ ] **9.3 Coordinate independent work branches.** Persist mechanical readiness, visual readiness, active installation, and review grants separately. Record eligible dependent jobs durably, execute them outside the world mutation transaction, and fence publication by selected revision and authority. No full cross-product status enum or new workflow service is required.
- [ ] **9.4 Enforce scope-sensitive publishing.** Distinguish better depiction, new blueprint/variant, actual instance modification, and world-law replacement. Reuse INV-5 for active changes. Existing objects remain on their construction semantics unless a native modification or authorized migration applies; new law versions cannot give arbitrary player cohorts different physics.
- [ ] **9.5 Add structured repair findings and bounded re-entry.** Classify local output defects, upstream design contradictions, unknown evidence, unsupported host behavior, provider uncertainty, and current-runtime faults. Changed designs re-enter one validation planner while retaining reusable work; local art defects do not regenerate mechanics. Require acceptance of substantive substitutions and preserve failed findings/liked outputs.
- [ ] **9.6 Bind review to concrete content.** Reuse low-impact automatic activation and consequential workshop/conjure confirmation. Bind meaningful approvals to candidate, world, scope, material effects and allowance; independently approve art. Stale approvals cannot activate newer/stronger candidates. Do not create a confirmation step after every harmless tool operation.
  - [x] Apply a saved native-ready recipe by exact digest under the original world/actor/origin/base scope, with separate resumed-world admission and a single child receipt. Reuse neither a model approval assertion nor browser replacement bytes. Broader art/conjure/law confirmation remains pending.
- [ ] **9.7 Provide simple progress and continuation.** Show usable versus draft/blocked, required decisions, pending art and remaining budget in one invention view. Preserve private NPC feedback and separate native craft/use. Undo/reselect/fork must retain lineage and current authority rather than erase cost or revive old work.

**Completion gate:** a player can create, revise function while art is in flight, retain a preferred appearance, and later prepare a replacement without changing the currently working invention. Two-tab edits and late completions cannot silently publish the wrong revision. A complete supplied candidate still requires no additional search, Jev, or generation call.

### INV-10 — World constitution, freezes, and author refinement control

**Design owner:** [World constitution](../world-constitution.md). **Depends on:** current manifest/host bindings and INV-1.3; protected live replacement additionally uses INV-5. Shared EWF interfaces remain owned by EWF.

- [ ] **10.1 Resolve the constitution from existing authority.** Add a bounded, versioned view over world profile, exact module pins, validation/resource policies and protection records. Reference current grants/accounting rather than copy them. Do not add another writable manifest, permission store, or kernel ontology. Expose authorized plain-language inspection.
- [ ] **10.2 Separate stability controls.** Add immutable policy records for owner review/freeze and author no-automatic-refinement, independent of immutable artifact bytes, installation pins, origin locks and runtime quarantine. Enforce actual author/owner/operator/payer grants separately. Preserve current player/NPC defaults and the existing unresolved owner-authoring exception.
- [ ] **10.3 Implement explicit lock/unlock operations.** Resolve IDs/versions/lineages and protected bindings from user requests, show consequential scope, then commit expected-revision operations with audit receipts. Ambiguous names need a precise selection. A frozen rule still executes and its instances still change state; unlocking cannot revive previously revoked work.
- [ ] **10.4 Detect indirect freeze bypass.** With INV-11 impact analysis, reject unapproved replacement through aliases, changed dependencies, owner rebinding, or incoming interactions that violate protected invariants. Permit compatible compositions using a frozen law. Report unsupported protection breadth instead of claiming to freeze all emergent future consequences.
- [ ] **10.5 Govern constitutional amendments.** Evaluate proposed governing-policy/validation changes under the previously active authority and constraints. A candidate cannot exempt itself or grant new payer/security permission. Use INV-5 for coherent world-law installation; preserve relevant established history and explicit approximation/migration records.
- [ ] **10.6 Add optional deterministic freeze triggers.** After manual controls work, support an explicitly configured first-use/release trigger with committed-fact input, bounded scope and idempotent receipt. No model per tick or hidden auto-freeze on image completion. Default reversible owner freezes remain distinct from any out-of-scope irrevocable sealing feature.
- [ ] **10.7 Integrate safety and restore overlays.** Keep freeze orthogonal to quarantine; safety shutdown can stop frozen behavior without rewriting it. Old saves cannot restore revoked grants, restart cancelled work, or re-enable technically quarantined code. Coordinate same-version save pins and current external authority with SL and INV-5.

**Completion gate:** owner/author locks stop the intended direct and indirect changes, compatible use remains available, unrelated origin-lock changes do not revoke valid work, and frozen active mechanics can be quarantined/repaired without erasing history or bypassing permissions.

### INV-11 — Dependency-driven verification and reusable evidence

**Design owner:** [Invention validation](../invention-validation.md). **Depends on:** current family contracts, INV-1.5 and the applicable INV-3/EWF bindings. Begin with deterministic checks over the existing families; new invented-law proof requires its actual supported consumer under INV-6 or equivalent EWF work.

- [ ] **11.1 Build typed dependency projections.** Resolve exact definition references, invocation order, read/invalidation/query membership, resource conflicts, event subscriptions, rights, validation and presentation associations over existing identities. Use ordinary maps/tables and reverse indexes initially. Reject unsupported required capabilities and distinguish valid temporal feedback from unbounded expansion/zero-time cycles.
- [ ] **11.2 Derive conservative behavior summaries.** Trusted operations supply mandatory reads/effects, owner/resource identities, scope, lifecycle, work and disclosure limits. Derive candidate summaries from actual bindings; model-authored summaries cannot suppress checks. Preserve native phase/RNG order while extracting interfaces; changing numerical semantics is separate qualification.
- [ ] **11.3 Implement a bounded validation-plan builder.** Order cheap eligibility/schema/reference checks before impact analysis and paid work. Include referenced definitions, reverse consumers, shared resources, query membership, event/cognition consequences, freezes and active-state dependencies. Never truncate a mandatory closure to top-K semantic matches and call it complete; return a bounded pending/narrower-review outcome.
- [ ] **11.4 Add the invented-obligation hierarchy.** Support admitted world/family constraint and scenario references using reviewed predicate/test interfaces. Host integrity and active constitution obligations cannot be waived by a candidate. Test authoring a new supported law and its expectations under prior policy; the same model's passing tests are supplemental rather than self-certification.
- [ ] **11.5 Parameterize independent native scenarios.** Add shared idempotency/atomic-transfer/authority invariants; family boundary cases; competing consumers; cancellation/removal; seeded operation sequences and minimized counterexamples. Use differential/metamorphic checks only when their assumptions follow the contract. Prevent model fixtures from dispatching real providers or mutating live worlds.
- [ ] **11.6 Route focused semantic questions.** Implement a replaceable semantic-review port for unresolved intent/premise/relevance questions. Batch independent Jev questions sharing evidence; use task-specific calibrated outcomes including abstention. Do not require Jev before a known route or let its answer remove mandatory checks. Treat prototype thresholds as current tuning, not universal acceptance policy.
- [ ] **11.7 Add targeted generative investigation.** Give LLM/harness review compact candidate diffs, exact relevant contracts, counterexamples and scoped evidence. Require actionable referenced findings and explicit assumptions; preserve private actor projections. Use bounded repair and optional extra scenarios instead of mandatory model committees or repeated full-world critiques.
- [ ] **11.8 Persist verification records and safe reuse.** Key evidence by candidate, relevant host/family/dependency and validator/test versions, constitution inputs, query membership, assumptions and seeds. Distinguish native, semantic, isolated and live evidence. Current authority/action prerequisites remain fresh. Invalidate negative results when new consumers/capabilities appear; cross-world reuse rechecks destination policy and rights.
- [ ] **11.9 Qualify computational and interaction scope.** Exercise per-invocation and aggregate queries/targets/events/descendants, load/simulation-speed growth and relevant sequential exploits. A candidate within individual limits must not create forbidden combined resource cycles or unbounded downstream cognition. Coordinate runtime caps with INV-13 and PF rather than adding another scheduler.

**Completion gate:** a familiar variant reuses applicable evidence cheaply, a new incoming consumer invalidates the relevant cache, a novel supported world obligation can be admitted without hardcoded mechanic names, and an incomplete impact set/unknown requirement cannot be reported as fully verified. Missing host support remains an explicit blocker rather than a generated claim.

### INV-12 — Progressive in-game art and compatible publication

**Design owner:** [Runtime invention art](../invention-art-pipeline.md). **Depends on:** INV-3.4, INV-9 revision identity and the relevant INV-13 allowance/attempt boundary. Can deliver one current tool family before general world-law composition or full workshop activation.

- [ ] **12.1 Add presentation requirement identity.** Derive versioned function/observable/cosmetic constraints, pinned references, supported states, dimensions/camera/density, pivots/attachments, outputs and bounds from the candidate. Keep renderer details and live instance state separate. Classify meaningful appearance changes through INV-9/11 rather than trust a cosmetic label.
- [ ] **12.2 Prove an adequate immediate fallback.** Reuse assets, native family composition, and supported state effects without inference. Declare adequacy per family and state; a trap or doorway needs more than a generic icon. Use independent visual randomness. Missing required representation blocks the affected live surface, not unrelated valid mechanics.
- [ ] **12.3 Add one optional bounded image adapter.** Persist eligible requests and attempt receipts, limit outputs/references/pixels/work, and preserve a no-key/zero-budget fallback. Rough generation is asynchronous and skippable. Speculative draft art needs explicit allocation; confirmed conjuring still waits for its own confirmation before paid art.
- [ ] **12.4 Validate and stage durable artifacts.** Bound decode, file/pixel/frame/part counts, allowed formats/references, and resource usage; reject executable or unsafe content. Check alignment, identity, style, scale, silhouette and semantic cues with native checks plus qualified visual review. Persist bytes before manifest publication; provider URLs/workspaces cannot be the only retained copy.
- [ ] **12.5 Publish with independent compatibility receipts.** Atomically update a versioned preferred binding after review and fresh selected-revision/rights/contract checks. Clients fetch asynchronously and apply only to compatible current state. A late corpse/removed-object image must not recreate anything; stale candidate art can remain authorized reusable content without becoming live.
- [ ] **12.6 Support refinement and approval.** Separate better rendering from alternative redesign. Preserve chosen identity, pinned features and liked candidates; permit automatic low-risk publication or author/owner review under policy. Refined-art rejection retains adequate prior art and valid mechanics. A combined UI review never conflates art approval with mechanical activation.
- [ ] **12.7 Bound reuse and shared jobs.** Deduplicate by actual contract, references, style, state, density/camera and authorized scope; avoid per-frame/per-instance/per-wound-intensity generation. Cancelling one consumer does not cancel funded work for others; all consumers recheck independent publication rights. Cache reuse cannot leak private reference assets or transfer ownership.
- [ ] **12.8 Preserve assets through saves and releases.** Pin required immutable bytes/recipes/manifests, fence old-generation callbacks, and include only authorized dependency closure in INV-8 packs. Garbage collection respects other consumers and retained saves. Restore does not buy regeneration or reset real costs.
- [ ] **12.9 Measure one complete visual family.** Exercise icon/world/equipped alignment, relevant directions/states, pixel stability and frame/texture costs at normal camera scale. Compare immediate, rough and refined quality/latency/cost using separate fixture and bounded live evidence. Do not claim full creature rigs/building animation from one attractive still.

**Completion gate:** an invention becomes usable with truthful native art, acquires a reviewed replacement without changing mechanics, survives restart, and handles failure/cancellation/revision/restore without wrong-state publication, duplicate paid calls, or loss of the last adequate asset.

### INV-13 — Episode budgets and installed-cost enforcement

**Design owner:** [Invention budgets](../invention-budgets.md). **Depends on:** existing durable reservation/receipt records and INV-9 root linkage. Reuse billing/entitlement owners and PF instrumentation; no separate wallet or universal new job platform.

- [ ] **13.1 Attribute one episode across all work.** Bind payer, origin, actor, world, root/candidate and purpose separately; carry them through revisions, search, validation, art, harnesses and explicit follow-ups. Do not double-charge the actor decision that already authored a supplied NPC method. Forks/new roots cannot evade payer/world aggregate caps.
- [ ] Qualify and bound existing actor-month reservation/usage reads as attempt histories grow; full-month receipt materialization and unqualified provider cost bounds remain gaps despite the workshop-root cap; broader payer/runtime hierarchy is not complete. Reuse one authoritative accounting store, derive reserve from enforceable request/provider cost limits, and do not add a speculative second wallet. INV-13.2/13.3/13.7 own the broader ledger and reporting work.
- [ ] **13.2 Enforce hierarchical atomic reservations.** Apply payer/world/account/actor/episode/purpose/per-attempt constraints to the same cost facts. Serialize competing reservations; keep settled, unresolved-dispatched and undispatched holds disjoint. Preserve exact currency arithmetic, real dispatch timestamps and price/config provenance.
  - [x] Add an atomic indexed workshop-root cap over existing attempt cost facts, inherited by linked revisions/search/Apply without a second wallet; preserve uncertainty and membership through backup/restore. Full payer/world/runtime/entitlement hierarchy remains pending.
- [ ] **13.3 Reconcile uncertainty without replay.** Exercise failure before dispatch, lost response after execution, partial usage, cancellation, duplicate receipt, late settlement and provider lookup unavailable. Keep unknown charges conservative and visible; a new explicit paid attempt retains old exposure and remaining episode limits. Never infer free execution from invalid output.
- [ ] **13.4 Bound refinement and speculation.** Configure finite rounds/time/tokens/test/image work under the root cap. Pause usefully with retained drafts/findings on exhaustion. Material changes in scope/cost need current authorization; waiting for a human or art does not hold an idle paid harness. Validate that revisions/reload/unlock do not reset allowances.
- [ ] **13.5 Compile recurring cost envelopes.** With INV-11 and host contracts, bound queries/targets, rate/descendants, state growth, event/cognition/narration effects, optional semantic work and visual variants. Check aggregate world/instance capacity as well as one invocation. An indefinitely recurring law may have bounded per-window work; it need not have an arbitrary finite lifetime.
- [ ] **13.6 Enforce backpressure and scoped recovery.** Run costly tests/images off the mutation lane under bounded concurrency/CPU/memory. Defer optional enrichment before essential play, without dropping mandatory physical effects. Reject uncommitted bound violations and use declared quarantine/pause when a required owner cannot continue safely.
- [ ] **13.7 Add causal cost and readiness views.** Correlate native, Jev, LLM, image and downstream work with bounded diagnostics. Show cost status, reserved exposure, available allowance and meaningful stage/purpose totals; avoid counting shared jobs or included provider costs twice. Measure cost per usable invention and incremental installed cost, not just token price.
- [ ] **13.8 Integrate separate entitlement accounting.** Use the billing/entitlement contract for qualifying invention-unit reservations and consumption, separately from real costs. Test final-unit concurrency, rejected versus uncertain completion and policy-defined period handling before shared accounts launch. Existing-mechanic art/state variants must not debit a second invention unit.
- [ ] **13.9 Preserve non-rewindable authority.** Coordinate SL/production-data recovery so actual costs, uncertain attempts, current revocations and external receipts survive gameplay load. Lowered limits report encumbrance and stop new dispatch; they do not refund charges. Missing recovered accounting fails closed for paid work, not for unrelated native play.

**Completion gate:** no concurrent/revised/restored workflow exceeds its enforceable authorized allowance through double spending or forgotten uncertainty; known and unknown costs remain distinguishable; a cheap-to-author but unbounded recurring mechanic cannot be admitted as sustainable.

### INV-14 — Integrated qualification and staged release

**Design owner:** the [foundation packet](../invention-foundation.md). **Depends on:** the corresponding INV-9–INV-13 slices and their existing INV/AG/EWF/PF/SL consumers. These tests qualify implemented slices; they do not require building every future mechanic before shipping a supported tool.

- [ ] **14.1 Maintain a contract coverage matrix.** Map required behavior to its existing/new task and executable scenario, with native fixture, browser, provider and performance evidence separate. Preserve open parent gates until their full stated scope is demonstrated. Qualify current supplied-proposal and private NPC paths rather than treating them as unimplemented.
- [ ] **14.2 Exercise edits and backtracking end to end.** Create a tool, pin art, change function during generation, repair a rejected output, undo/reselect, and later revise the blueprint while an old instance remains in use. Verify no silent substitutions, loss of selected work, mistaken active-version replacement or stale approval.
- [ ] **14.3 Exercise law and validator evolution.** Using genuinely supported primitives, admit one world-authored rule and its scoped obligations, then compose another artifact. Attempt self-exemption, weakened candidate-only tests, incompatible shared-law versions, a new incoming consumer, a forbidden source cycle and a legitimate admitted source. No hardcoded fictional name may be the reason the generic planner succeeds.
- [ ] **14.4 Exercise authority and privacy boundaries.** Test author refinement lock, owner freeze/unfreeze, indirect override, independent origin locks, rejected delegation, art rights revocation, frozen-but-quarantined behavior and old-generation completion. Test NPC private invention near another actor, sanitized findings and no learning from copied-scene tests or private registry reuse.
- [ ] **14.5 Inject failures at durable boundaries.** Crash/restart before and after candidate checkpoint, reservation, dispatch, activation, byte staging, manifest publication and save installation. Repeat operations and deliver late results. Recover one coherent version and reconcile real spending without duplicate crafting/spawning/quota debits or art resurrection.
- [ ] **14.6 Qualify latency, cost and workload growth.** Measure native command responsiveness during testing/art; time to adequate and refined presentation; false rejection/escalation; repair counts; spend per usable result; and downstream native/cognitive growth at selected populations/speeds. Record assumptions and incomplete coverage rather than unsupported tail/capacity claims.
- [ ] **14.7 Evaluate live adapters deliberately.** Run only explicitly authorized, capped small examples after fixtures, preserving price/model/latency receipts. Compare a clear supported request, ambiguous method, substantive revision, art mismatch and insufficient evidence. Do not use the same tuning examples as general quality evidence or infer live quality from mocks.
- [ ] **14.8 Close documentation and enablement gates.** Update Architecture/implementation status only for delivered behavior, Verification only for observed evidence, and numeric unresolved policies only in open-decisions/configuration owners. Keep no-legacy-development-save policy, current lock defaults, unsupported G2 and no automatic paid retries. Enable new paid/background behavior only under explicit configured limits.

**Completion gate:** the selected playable slice satisfies the corresponding correctness, privacy, review, cost, performance and recovery contracts under actual evidence. Remaining expansion gates stay explicit; passing a small tool flow does not claim arbitrary invented physics, fully 3D simulation, hosted multiplayer scale, or complete autonomous invention quality.

## Unified World Agent delivery

The following work implements the accepted [runtime](../world-agent-runtime.md), [shared tools](../invention-workshop-tools.md), [MCP](../world-agent-mcp.md), [graph](../invention-graph.md), [composition](../invention-composition.md) and [scenario](../invention-scenarios.md) contracts. It extends INV-1–INV-14 rather than marking their unfinished scope complete. INV-11 still owns mandatory validation; INV-5 still owns live activation; EWF/AG/EPR/PF retain their subsystem responsibilities. Automated regression cases are recorded only in the maintainer TODO.

### INV-15 — Navigable relationship graph and visual inspection

**Depends on:** existing definition/pin identities and INV-11.1–11.3 where enforced behavior summaries are needed. **Owner:** [Relationship graph](../invention-graph.md).

- [ ] **15.1 Implement typed source adapters and exact node identity.** Project current recipes, materials, attribute/sense definitions and host interfaces without creating a second writable registry. Carry source/version/digest, relationship type, applicability and assertion basis; distinguish existing enforceable contracts from descriptive or hypothesized relationships. Expose unsupported kinds and missing relation coverage explicitly.
  - [x] Project exact current recipes/materials/base pins, native family metadata and installed attribute/sense bindings through source-specific readers. Historical bases are reference-only; hypothetical/read-effect coverage is not invented. [Current scope](../architecture.md#repertoire-relationship-and-mcp-foundation).
- [ ] **15.2 Build bounded reverse indexes and coherent generations.** Add source/target/type lookups over existing durable records; update on definition/manifest change, not ordinary ticks. Index publication must correspond to a complete source revision. Start in process/current SQL storage; add a graph database only after a measured need. Bound retained generations and caches.
  - [x] Add bounded immutable in-process incoming/outgoing adjacency, exact source-derived node/edge/snapshot IDs, a single frozen-definition cache and generation-bound paging. No per-tick graph rebuild or saved-world schema change. Persistent incremental indexes and impact invalidation remain pending.
- [ ] **15.3 Expose inspect, neighborhood, path and traversal queries.** Return stable references, provenance, relevant qualifiers, exact snapshot, scoped completeness, continuation and work limits. Separate exploratory partial results from the complete mandatory impact analysis in INV-11. Use a visited frontier; avoid enumerating every path or expanding live instances eagerly.
  - [x] Expose current-source inspection and one-hop in/out/both paging with source provenance, role/quantity qualifiers, explicit limitations and continuation even for empty filtered pages. Project selected live subject/evidence neighborhoods on demand. Server-side multi-hop/path/impact analysis remains pending; client navigation is not a proof of closure.
- [ ] **15.4 Include reverse consumers and negative-query membership.** Integrate INV-11's shared resources, incoming event subscriptions, state/query consumers and protection obligations. New consumers invalidate previous absence conclusions. Keep temporal cycles, definition cycles and runtime feedback as distinct semantics; an acyclic packaging graph is not proof of safe execution.
- [ ] **15.5 Add focused visual inspection.** Lazy-load a read-only React Flow view with grouped subsystems, expandable neighborhoods, relationship legends, evidence drill-down, revision comparison and an accessible list equivalent. Use Dagre for the first small layout; adopt ELK only for needed compound layouts. Keep positions non-authoritative and do not load the complete world graph into the initial client bundle.
  - [x] Add an owner-only relationship list in the World Agent panel with lexical search, exact-source detail, bidirectional links, current work and retained-evidence inspection; no paid calls, graph edits or new canvas dependency. Full graph layout, evidence links and browser/accessibility qualification remain pending.
- [ ] **15.6 Qualify projection parity and disclosure.** Demonstrate that UI and agent tools see the same permitted graph snapshot and can reach complete authorized artifact detail. Private edges/counts are not leaked as global totals, stale projections cannot authorize activation, and a hypothetical interaction cannot masquerade as an installed dependency.

**Completion gate:** the World Agent and player can inspect one known invention, navigate its exact requirements and reverse consumers, explain every displayed edge, and distinguish incomplete or hypothetical coverage. Adding a new source kind changes its adapter rather than the generic graph/UI.

### INV-16 — One application tool service and native MCP connector

**Depends on:** existing finite tools, authority, job and candidate services. **Owners:** [Shared tools](../invention-workshop-tools.md) and [MCP integration](../world-agent-mcp.md).

- [ ] **16.1 Extract request context from controlled-character assumptions.** Bind the out-of-world World Agent to authenticated world-level grants and recipient disclosure. Preserve the separate actor-scoped service profile for NPCs. Do not require recipe learning/perception for authorized administrative inspection; do not let a broad service principal disclose real-user/private content to an ungranted recipient.
  - [x] Add a separate world-granted read/preview profile shared by local owner inspection and dedicated read-only MCP credentials. It does not filter by the controlled character's learned recipes or confer broader scope on existing NPC/actor tools. Multi-user recipient grants and funded mutation contexts remain pending.
- [ ] **16.2 Register operations once.** Share validators, metadata, handlers, output projections and native mutation services between UI, structured-loop compatibility and MCP adapters. Add a stable operation catalogue for actual implemented kinds; unsupported tools/kinds are not advertised as executable. Retain zero-model native reads, supplied candidates and Apply.
  - [x] Register eight strict read/preview descriptors once for local HTTP and MCP: context, lexical definitions, exact inspection, relationships, item instances, retained evidence, current activity, native recipe preview. No independent registry/writer or new paid dispatch.
- [ ] **16.3 Implement the compatible SDK transport.** Pin audited official v2 server/Node packages and qualify the SDK-provided stateless 2025-11-25 compatibility profile against the inspected Macrofold v1 client. SDK major versions need not match. Qualify modern wire support separately; a temporary patched v1 server requires an observed blocker and upgrade task. Use stateless short JSON calls and application job handles. Do not implement a hand-written dual protocol, legacy SSE compatibility or a separate HTTP framework as a prerequisite.
  - [x] Implement the disabled-by-default official SDK 2.0.0 server/Node adapter with bounded requests/results, per-request server state and stateless compatibility. Local real-SDK HTTP initialization/discovery/calls are exercised; actual Macrofold/client forwarding and modern-profile qualification remain open. SDK compatibility replies may use short SSE framing; no custom codec was added.
- [ ] **16.4 Add authenticated world/context binding.** Configure a dedicated hashed OpenLegend bearer credential and encrypted Macrofold connection. Bind opaque context handles to the connection principal, world/generation, initiating account, audience, funding session, operation grants and expiry. Validate current authority on every call/commit; do not assume arbitrary headers or model-provided run IDs are trustworthy.
  - [x] Require an explicit credential hash, exact world ID, expiry and allowed Host list for the read-only server-to-server connector. Reject Origin/URL arguments, disabled/mismatched/expired scope and invalid credentials. Paid/mutating tools remain unavailable until durable session/context/approval grants are implemented.
- [ ] **16.5 Wire connector provisioning and a native harness.** Reuse current Macrofold clients/journals, Tools approval and workspace/preset Access rules. Configure an explicit connection/tool ceiling and deny unrelated shell/files/connectors. Provide a narrowly exposed authenticated HTTPS staging endpoint; do not disable private-network checks or expose all owner routes. Persist session/run mapping before remote dispatch and reconcile uncertain creation.
- [ ] **16.6 Verify structured results and durable actions.** Return the shared result envelope through actual MCP structuredContent/text forwarding, protocol errors and application findings. Mutations use separate durable operation IDs and exact candidate/plan refs; no agent-facing approve tool. Long jobs expose inspect/cancel handles without idle streams or paid polling loops. Unknown tool outcomes do not authorize new writes.
- [ ] **16.7 Establish real interoperability evidence.** Use local Inspector for protocol/auth negative cases, then the actual selected Macrofold harness for tools discovery, graph/read, draft/validate, human approval and one native Apply. Exercise restart/cancellation and reduced-scope fresh sessions under a recorded cap. Injected model envelopes cannot close this native integration gate.
- [ ] **16.8 Plan the compatibility upgrade.** Record exact SDK/protocol/client/harness versions and security-support horizon. Upgrade the client/server pair together with unchanged application identities/receipts. OAuth multi-user install, MCP resources/apps/subscriptions and new frameworks remain separately justified additions, not blockers for the trusted one-world integration.

**Completion gate:** a real Macrofold-native agent reaches the single OpenLegend tool service, has the authorized world view, can iterate and act under exact approvals, and cannot bypass domain, cost or disclosure checks. Existing finite UI and NPC paths keep their own scope and do not require MCP to execute natively.

### INV-17 — Kind adapters, composition, and live revision journeys

**Depends on:** the applicable INV-3/EWF host capabilities, INV-5 activation, INV-11 validation and INV-15/16 inspection. **Owner:** [Composition](../invention-composition.md).

- [ ] **17.1 Deliver one non-recipe creator adapter.** Expose exact read/schema/draft/diff/validation/activation for a supported reservoir/category or sense definition using its current owner. Include scoped context, native action/observation and same-version restoration. Unsupported behavior remains explicit; a descriptive tool result cannot install a new host operator.
- [ ] **17.2 Implement typed specialization through owning families.** Consume EWF11/INV-3.6 port identities and bindings; validate semantic interfaces, units, resource identity, target roles, disclosure, lifecycle and bounds. Preserve partially unbound reusable artifacts as non-executable. Do not remove native-input guards until positive consumer checks exist.
- [ ] **17.3 Connect composition to evidence and graph.** Derive enforced read/effect/resource/work summaries from actual admitted bindings; use INV-11 mandatory closure and independent scenarios. A world-authored obligation uses admitted predicate/test interfaces and cannot waive its prior governing policy. Trace the player's requested method to compiled fields and report conceptual substitutions.
- [ ] **17.4 Expose live-law change preparation.** Implement inventory of affected instances, active work, knowledge refs, owner bindings and presentation dependencies; classify preserve/migrate/quiesce/recover. Rehearse only in isolated state. Generate an exact plan for INV-5, require consequential review, and recheck current state before atomic installation rather than applying an old copied-world snapshot.
- [ ] **17.5 Preserve scope-sensitive editing.** Distinguish depiction improvement, new blueprint, physical-instance modification and shared-law replacement in the same conversation. Bind changed geometry/light/sense/appearance semantics to supported consumers and invalidate only affected evidence/art. No automatic transformation of all existing copies or arbitrary player-cohort physics.
- [ ] **17.6 Connect real NPC investigation.** Extend AG08/CH tooling with actor-scoped inspection, method drafting, actual native experiment receipts and later reconsideration. Keep World Agent records and copied-scene validation out of lived memory. An accepted method is not automatic construction; canceled goals and lock changes remain authoritative.
- [ ] **17.7 Qualify two unlike reusable examples.** Use a physical tripwire/bell or shelter family and a non-human charge/contact-sense example, with full inputs, execution, observation, action discovery and persistence. Add a world-authored law only once actual supported primitives exist. Wider magic, shared minds and G2 do not block the first proof.

**Completion gate:** the same application/agent/graph boundaries support substantially different subsystem-owned kinds and a real composition, including scope-aware modification and observed cognition consequences, without named-mechanic branches across generic infrastructure.

### INV-18 — Unified capable conversation and image-inclusive session funding

**Depends on:** current conversation/root-ledger persistence and INV-16; art actions require INV-12. **Owner:** [World Agent runtime](../world-agent-runtime.md).

- [x] **18.1 Raise the current workshop-root default to $5.** Update config and the example only, preserving explicit lower/zero configuration and existing admitted allocations. This completes the configuration default, not the target unified funding session, new turn limits or live native harness.
- [ ] **18.2 Unify conversation and progressive review.** Replace the tool-less versus tool-enabled UX split with one action-capable World Agent. Preserve quick native controls and explicit action mode/intent in backend requests. Show actual draft/validation/operation state and request approval only for consequential changes; quoting a request never grants mutation authority.
- [ ] **18.3 Separate session, project and run identities.** A deliberate funded session can revisit authorized projects; a project can span sessions with preserved lineage. Reopen/reconnect/compaction never starts a fresh allowance. Serialize conversation turns, retain pending questions/jobs/approvals, and rehydrate canonical selected constraints rather than trust model memory.
- [ ] **18.4 Enforce one $5 cap including image work.** Roll model/compute/Jev/embedding/art/validation charges into the same session ledger with visible purpose breakdown and no fixed art split. Reserve independent maximum exposure for native runs and tool jobs; avoid double-counting included charges. If a run holds funds needed by art, settle/reallocate through a supported operation or end/reconcile the run before spending; never release uncertain exposure to make room.
- [ ] **18.5 Enable a generous qualified agent profile.** Introduce explicit configurable turn/tool/context/output/deadline limits large enough for the target journeys, following the runtime's proposed starting profile. Inspect actual harness/provider enforcement; do not claim an advisory prompt is a hard cap. Pause with retained work on exhaustion, and keep all existing root/world/payer constraints authoritative.
- [ ] **18.6 Preserve reasoning and approvals over waits.** Retain constraints, evidence refs, rejected approaches, selected art and exact plans across provider compaction/restart. End/suspend idle runs for humans or long jobs; resume under the same funding session with fresh tool state. Reduced authority or restored timelines require fresh model context, not merely a rewritten system prompt.
- [ ] **18.7 Expose transparent cost and progress.** Show spent/reserved/uncertain/remaining amounts and job state without treating missing cost as zero. Publication of an already funded valid result needs no new generation allowance. Fresh session/top-up requires an explicit payer-authorized operation; ordinary mechanic/crafting fiction cannot increase funds.

**Completion gate:** one conversation investigates, explains, drafts, requests only meaningful approval, applies once, and optionally obtains art without exceeding the enforceable image-inclusive session allowance or losing intent across waits/reconnects.

### INV-19 — End-to-end capability gates and later optimization

**Depends on:** the implemented phase's INV/EWF/AG/EPR/PF/SL work; not every future phase. **Owner:** [Target scenarios](../invention-scenarios.md).

- [ ] **19.1 Maintain scenario-to-contract coverage.** Map S01–S11 to required implemented adapters, native checks, browser behavior, actual-harness evidence and retained gaps. Examples are capability targets, not implied launch commitments or fixture-proven live behavior. Keep phase A small enough to deliver first.
- [ ] **19.2 Qualify first real connected authoring.** Complete S01 with actual MCP/native harness, graph/read coverage for current kinds, retained preferred appearance intent, native draft/Apply/craft and a clear art-pending state. Require actual generated art only when INV-12 is enabled; a placeholder cannot be presented as final generation.
- [ ] **19.3 Qualify authoritative evolution and recovery.** Complete the applicable S04/S08/S10/S11 cases using supported law primitives: constraints that are themselves authored, freeze/use compatibility, coherent active-work changes, restore fencing, private evidence and budget uncertainty. Preserve old working versions on failed proposals.
- [ ] **19.4 Integrate encounter performance gates.** Use PF09's [encounter plan](../encounter-scaling.md) and EPR's private acquisition policy. Measure cold and warmed events/awareness/native-step work under simultaneous authoring; do not optimize scores by hiding semantic changes or lost witnesses.
- [ ] **19.5 Optimize cost after capability is qualified.** Measure cost per useful outcome and installed recurring cost, then evaluate smaller models, Jev routing, context/evidence reuse, selective review, queued bulk art and cache policy. Preserve the capable profile and honest quality comparisons; no mandatory router/model committee is introduced merely for nominal token savings.

**Completion gate:** each released phase has an actual user/actor journey with truthful status, precise remaining limits and measured resources. Optimization follows evidence rather than weakening the capabilities or integrity needed by the end goal.

### How to implement and track the next slice

Extend the delivered four-family invention, optional read/preview/Apply workshop, root-cap and actor-proposal paths. Complete remaining INV-1/2 acceptance and delegation work without reimplementing supplied-method validation, private feedback, or the existing gathering-tool proof. Begin the coordinated slice with INV-9 selected revisions, INV-11 native validation/evidence planning, and INV-13 episode accounting; then deliver INV-12 for one current tool family. Basic INV-10 protection records can proceed alongside that work; broad indirect-impact protection depends on the relevant graph contracts.

INV-4/5 remain the owners of general workshop tools and coherent live replacements. INV-6 supplies new physical consumers when a concrete gameplay case needs them, and EWF retains the common runtime seams. Do not wait for all advanced graph, pack or G2 work to make a useful supported invention playable. Do not enable a larger scope until its mandatory checks, resource limits and recovery are demonstrated.

For each task, record affected interfaces, same-version integrity and any active-definition activation implications (no old-development-save support), remaining limitations, deterministic evidence and, where needed, separately budgeted live evidence. A check mark means the task's stated outcome exists, not merely that an interface, prompt or design paragraph was added. Keep expensive evaluation deliberate and retain all existing task IDs and still-valid gates.

### INV-20 — Repertoire integration and cross-domain composition

**Design owner:** [Repertoire foundation](../repertoire-foundation.md). This is the integration work for the repertoire review, not a new action engine or family registry. AC owns invocation/activity/participation; INV-5 owns active changes; EWF/CR/EPR/NC/SW retain their semantic owners. Existing INV-15/16 entries above own the delivered graph/read transport.

- [x] **20.1 Establish source-backed views without changing world behavior.** Link exact definitions, current native arrangements/work and retained memory/commitment evidence through the same graph reader, local owner surface and read-only MCP service. Document coverage, role/source/assertion meaning and no accidental learning. No joint/contact/complete-law graph is asserted. Runtime evidence is in [Verification](../verification.md#repertoire-foundation-runtime-observations).
- [ ] **20.2 Bind World Agent action tools to AC.** Consume structured family invocations, typed results, method constraints and existing AG activity inspection/control; do not constrain agents to a pre-enumerated action shortlist. Distinguish use, retained method, reusable template and new law. Broader native action tools require their actual AC executor and session/approval grants under INV-16/18.
- [ ] **20.3 Add source adapters for actual n-ary relationships.** When an attachment, worksite, carrier or agreement owner ships, project its identity, role edges, pinned implementing definition, relevant state and disclosure. Include change/migration impact for the actual linked instances. Do not synthesize support/load/title from labels, pairwise proximity or `ownerId` alone.
- [ ] **20.4 Deliver information-bearing artifacts with a consumer.** Implement bounded immutable content revisions, one real carrier, native write/read/copy, source/claim uncertainty and CR/EPR learning projections under the technical design. Prove a false note remains an attributed claim and a copied map gains no hidden geography. Add compatible art binding without buying image work to create content semantics.
- [ ] **20.5 Deliver exact offers and agreement semantics.** Extend the existing commitment owner without conflating personal belief with mutual acceptance. Add role-specific actual acceptance, pinned terms, fulfillment/unknown/dispute, amendment/release and exact finite settlement for one family. Keep normative state, actor compliance and optional enforcement separate; preserve title/custody and anonymity/provenance distinctions.
- [ ] **20.6 Qualify operator-dependent composition with AC08.** Use one worksite owner and existing participant action owners for a hold/fasten family. Implement readiness, resource/channel claims, refusal/withdrawal, retained work, interruption and recovery. Do not grant machine autonomy to actor labor or build a general parallel scheduler before its consumer exists.
- [ ] **20.7 Integrate temporal method evidence with AC06/08.** Validate causal order and maintained predicates using the same typed constraints/results: brace before removal, preserve exact keepsake, sealed delivery, stop on perceived signal. Distinguish preparation evidence from current execution checks; require acceptance of unsupported guarantees rather than weakening them.
- [ ] **20.8 Implement observer-relative presentation with real appearance consumers.** Project admitted illusion/disguise/occlusion/recognition effects through EPR and presentation. Test hidden-trap fallback, illusory bird without physical bird, truthful pretend performance and authorized creator inspection. Share permitted asset bytes without leaking hidden source identities or generating per observer/frame.
- [ ] **20.9 Qualify a cross-domain ordinary-use scene.** Implement pot-as-bell -> actual sound -> learned convention -> independent response -> willing shared work using existing supported interfaces, with no hardcoded dinner-bell noun and no forced agreement. Compare to blueprint publication and law authoring; each stays on its correct route.
- [ ] **20.10 Preserve exotic-world extension contracts.** Before concrete clone/merge, local clocks, non-Euclidean/graph topology or deformable-body work, specify controller/body/identity lineage, resource/knowledge partition, cross-clock effects, location codecs, support/occupancy and recovery with the relevant owners. These are conditional host features, not blockers for ordinary graph or MCP reads and not latent toggles.

**Completion gate:** unlike physical, social and information consumers compose through the existing owners and appear in the same inspectable graph, with honest scope and native consequences. None is accepted solely by an added type, tool description or generated image. All actual model/harness, browser, persistence, privacy and performance qualification remains separately recorded.

### INV-21 — Reviewed MCP authoring and unified native execution

**Owner:** shared application service/MCP/runtime; uses INV-15/16/17/18, existing native kind owners and permanent command receipts. **Not a replacement for their broader acceptance gates.**

- [x] **21.1 Durable write core:** operational sessions, exact immutable revisions and base pins; native recipe/attribute/status/cognition/action adapters; same-writer permanent Apply receipt; exact human approval separate from MCP. No agent approve/raise-budget operation.
- [x] **21.2 Writable transport and discoverability:** opt-in MCP session context, schema guidance, draft/compare/native validation/change tools shared with local HTTP, bounded body/results/concurrency; status/cognition dependency projection. Protocol behavior exercised locally; hosted deployment remains gated.
- [x] **21.3 Native execution adapter:** parameterize the existing Macrofold backend/provisioner for a tool-only owner workspace, exact connection selection, session-bound model/compute reservations, managed billing, current configuration and context fencing. Actual provider interoperability/usefulness remains the separate release gate below.
- [ ] **21.4 Unified review UI:** explicitly fund/resume a conversation, show exact candidate and consequences before approval, apply through the same service, recover retained turn results, retain zero-cost native shortcuts.
- [ ] **21.5 Qualification:** run an isolated actual Macrofold native harness through discover/inspect/draft/validate/review/apply and cancellation/restart. Establish connector grants, supported model, protocol forwarding and actual usage before hosted enablement. Preserve the local/manual versus live distinction.
- [ ] **21.6 Extend only real owners:** add qualified family adapters, semantic constraint fidelity, no-copy material composition, general live law migration and low-impact approval policy as their independent owners deliver them. Keep missing operations undiscoverable rather than fake invocations.
- [ ] **21.7 Mature retention and transport:** qualify large session records, usage history and PostgreSQL; add asynchronous message delivery/streaming only with durable result resumption, never repeated paid runs. Manual/injected transport results do not complete broader load/privacy/concurrency acceptance.
