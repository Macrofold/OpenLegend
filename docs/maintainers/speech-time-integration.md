# Sound/speech and elapsed-time integration handoff

This is a coordination note, not a second hearing or simulation specification. [Simulation time](../simulation-time.md) owns clock/integration behavior. The sound branch owns its hearing/speech contract; preserve its canonical documentation when combining the branches. Work and validation remain with [PF13](simulation-time.md), SW, EPR and the existing speech tracker.

## Integration state

`feature/simulation-cadence` and `feat/hearing-speech-captions` both contain the new main agent guidance. Cadence now includes the inspected current-main physical-contact, content-limit and nonblocking-memory changes through a history-preserving connector merge. Recheck both tips before combining them; neither is authorization to overwrite the other's work. Sound was read, not merged or edited by the cadence task. Exact inspected revisions, main ancestry and observations belong in [cadence evidence](../verification/simulation-cadence.md#sound-branch-and-remaining-integration).

Sound implements graded dB hearing, whisper/normal/shout propagation, listener-specific stable fragments, scoped real-time captions, perceived-event history, listener-volume indexing and bounded geometry-transfer reuse. It also has conservative status participation, immutable static-object exposure reuse and episode-membership pruning. Cadence reuses the small status/exposure helpers; preserve one copy of each. Keep sound's latest dispatch/revocation/publication and embedding-batch guards, not only its visible captions.

The later sound review fixes bounded receiver/observer-cache thrashing under oversized crowds: retained entries remain useful and overflow is evaluated exactly, rather than repeatedly evicting a complete working set. Its independent speech-load producer records demand separately from native catch-up. An unpublished mutable-entity shortcut mentioned in its recovery note is not an implementation to adopt.

The sound tracker still describes one-second physical evolution and 480 mandatory steps per wall second at 8x. Those are superseded implementation assumptions, not current product requirements: 480 game seconds per wall second is requested elapsed time, not an integration-call count. Update HE05, reference/slice profiler assumptions and trace descriptions during integration. Preserve action, collision, resource and event semantics; do not restore the old inner loop merely to satisfy a historical digest or benchmark label.

## Recommended order

Use the reviewed elapsed-time branch as the time-integration base for the combined sound/cadence review. Sound also contains newer main physical-contact and content-limit changes, so do not replace its versions wholesale. If sound lands first, integrate cadence into that updated base instead, preserving the same responsibilities. Neither branch depends on a universal kinetic scheduler or volumetric acoustics. No main or sound merge is implied by this note.

Do not resolve shared files by selecting a complete side. Retain cadence's start/rate/end phases in `kernel.ts` and `world-service.ts`, while preserving sound's batched acquisitions, recipient-specific evidence, conversation eligibility and revocation-safe derived writes. Keep world ownership, nonblocking consolidation pressure, in-place saves and physical-body contact. Do not restore retired fixed content-count caps when replaying older spatial changes. No new database, save reset, scheduler or writable store is needed.

## Shared changes to reconcile

- `status-effects.ts`: retain one `mayAdvanceStatusEffects` helper and its participation/cache improvements, but preserve cadence's `reconcileStatusEffects`, `prepareStatusRates` and `integrateStatusRates`. Capture conditional rates at interval start for all subjects before applying endpoint effects. Do not replace these phases with the older serial transition-plus-rate loop. New automatic activation and its predicted boundary both use fresh self-bindings, not a retired episode's source/target; active instances retain their own bindings. Predicate pruning is an interval bound, not a replacement for actual admission.
- `object-exposure.ts`, `encounter-cache.ts` and `kernel.ts`: retain one ID-only exposure cache and unchanged episode-membership optimization with geometry/body/pose/range/capability invalidation. Batched acquisitions may combine only within one stable post-motion sensing phase. Immutable physical-phase finalization/unchanged-exposure proofs apply per accepted interval, not once after an arbitrary multi-interval call. Camera or unchanged visual exposure never suppresses speech admission or a contact change.
- `kernel.ts` and temporal boundaries: `advanceWorld` returns a bounded elapsed prefix; `maxIntervals` bounds work, not game seconds. Work created on arrival receives none of the preceding interval. Preserve sweeps and support seams. Reuse sound's native supply index where compatible, but not its mandatory one-second inner loop. Staggered flyer/status deadlines still force global work; regional scheduling remains PF13.11, not permission to delay events or truncate listeners.
- Physical contact in `perception.ts`, `world-modules.ts`, `kernel.ts`, `worlds/base/senses.ts` and the upgrader: retain main/sound's body-contact detector and in-place correction of retired proximity detectors. Conservative candidate discovery includes body extents. A zero-radius contact definition is not a zero-length interval. Endpoint contact and fleeting between-endpoint tangency are different guarantees; the latter needs a crossing bound or declared sampled policy, not an unsupported exactness claim.
- `world-service.ts`: subtract actual game-time progress, retain debt and navigation/pause/command fences, and preserve routine durability. Host wake frequency and real-time persistence are coordination choices, not biological ticks. Memory-consolidation pressure cannot block native time. The existing mutation owner serializes effects independently of how speech requests are scheduled.
- `events.ts`, `speech.ts`, `acoustics.ts` and perception/history: calculate heard evidence at committed emission state with listener-specific full/partial/no-word content and identity scope. Do not replace graded exposure with the older binary radius/0.65 gate; its threshold arithmetic is not automatically valid for dB policy. Retain stable cache admission, exact overflow and current acoustic broad-phase bounds.
- `draft.ts`, `experience.ts` and `status-capabilities.ts`: preserve append/index/capability optimizations without duplicate mutation owners. Recheck nested commands, immutable parent/fork isolation and invalidation. Compilation alone does not qualify those boundaries. Do not introduce the abandoned mutable-entity shortcut during conflict resolution.
- `scene.ts`, `main.tsx`, view/protocol: retain full camera-facing aspect and virtual depth, support-aware interpolation, shadows and authorized read-through alongside caption anchors. Caption anchors follow displayed current/recent-past poses, never another actor's private future route. Sparse state arrival does not change browser frame cadence.

## Independent event and presentation clocks

Speech is currently an instantaneous committed occurrence, not a sustained acoustic process. Sparse simulation must not poll speech at the next interval or batch emissions across movement, changed noise/geometry, sleep or permission boundaries. External commands interleave through the normal mutation owner. A future native timed utterance must end the interval at its emission time; sustained speech needs explicit start/end/interruption boundaries.

Conversation inactivity and physiological deadlines use game time. Caption reading lifetime, presentation expiry and render interpolation keep explicit real-time clocks and pause/hidden behavior. Overlapping captions do not imply physically competing noise. A future 20 Hz state-publication policy would not mean a 20 FPS rendering limit.

## Acoustic guardrails

Source SPL, 3D attenuation and listener floor determine candidate extents; shouting may exceed the former small radius. Preserve every legitimate listener and historical emission origin. Never truncate audiences, reroll stored fragments, expose full original words through recall/history fallbacks, or reinterpret past hearing at a listener's current position. Navmesh rays are not sight/sound rays. Geometry caches store transfer, not permissions or semantic evidence.

## Joint acceptance to retain

Cross-cutting coverage belongs in [TODO](TODO.md), not a duplicate test list here. The combined branch needs actual emission-before/after-motion, stacked floors, sleep/sense changes, same-world restore, partial-text persistence and 1x/8x caption lifetime observations. Record progressed time, pending navigation, debt and real event counts. Use sound's independently scheduled demand: a slowed producer must not appear successful by offering fewer utterances. Each historical passing or failing run retains its workload and host context; neither branch's separate evidence qualifies the combined application automatically.

## Message for the sound-branch implementer

Use `feature/simulation-cadence` as the time-integration side; its inspected main conflicts are resolved and recorded. Keep your graded acoustics, receiver index, transfer reuse, stable bounded caches, batched evidence, captions, physical contact, append/finalization work and asynchronous safety fixes. Preserve cadence's three-phase integration and fresh automatic-activation bindings. Replace mandatory-one-second/480-ticks assumptions in HE05 and stress tooling with actual game-time progress and interval counts. Exercise the joint system using the independent speech producer, not just the no-speech cadence workload. Caption animation stays at browser frame cadence. No hearing re-authoring, new renderer or duplicate scheduler is required.
