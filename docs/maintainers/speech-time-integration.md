# Sound/speech and elapsed-time integration handoff

This is a coordination note, not a second hearing or simulation specification. [Simulation time](../simulation-time.md) owns clock/integration behavior. The sound branch owns its hearing/speech contract; preserve its canonical documentation when combining the branches. Work and validation remain with [PF13](simulation-time.md), SW, EPR and the existing speech tracker.

## Integration state

`feature/simulation-cadence` and `feat/hearing-speech-captions` both contain the new main agent guidance. Recheck both tips before combining them; neither is authorization to overwrite the other's work. The cadence task reads the sound branch but does not merge or edit it. Exact inspected revisions and observations belong in [cadence evidence](../verification/simulation-cadence.md#sound-branch-and-remaining-integration), not a moving implementation claim here.

Sound already implements graded dB hearing, whisper/normal/shout propagation, listener-specific stable fragments, scoped real-time captions, perceived-event history, listener-volume indexing and bounded geometry-transfer reuse. It also has conservative status participation, immutable static-object exposure reuse and episode-membership pruning. Cadence reuses the small status/exposure helpers; preserve one copy of each. Keep sound's latest dispatch/revocation/publication and embedding-batch guards, not only its visible captions.

The later sound review additionally fixes bounded receiver/observer-cache thrashing under oversized crowds: retained entries remain useful and overflow is evaluated exactly, rather than repeatedly evicting a complete working set. Its independent speech-load producer records demand separately from native catch-up. Those changes are useful to integration; an unpublished mutable-entity shortcut mentioned in its recovery note is not an implementation to adopt.

The sound tracker still describes one-second physical evolution and 480 mandatory steps per wall second at 8x. Those are superseded implementation assumptions, not current product requirements: 480 game seconds per wall second is the requested elapsed-time rate, not the number of integration calls. Update HE05, reference/slice profiler assumptions and trace descriptions during integration. Preserve action, collision, resource and event semantics; do not restore the old inner loop merely to satisfy a historical digest or benchmark label.

## Recommended order

Finish the elapsed-time branch's current-main reconciliation first, then use it as the time-integration base for a combined sound/cadence review. Sound already has newer main physical-contact and content-limit changes, so do not replace its versions wholesale. If sound lands first, integrate cadence into that updated base instead, preserving the same responsibilities. Neither branch depends on a universal kinetic scheduler or volumetric acoustics. No main or sound merge is implied by this coordination note.

Do not resolve shared files by selecting a complete side. Retain cadence's start/rate/end phases in `kernel.ts` and `world-service.ts`, while preserving sound's batched acquisitions, recipient-specific evidence, conversation eligibility and revocation-safe derived writes. Keep current-main world ownership, nonblocking consolidation pressure, in-place saves and physical-body contact. Do not restore retired fixed content-count caps when replaying older spatial changes. No new database, save reset, scheduler or writable store is needed.

## Shared changes to reconcile

- `status-effects.ts`: retain one `mayAdvanceStatusEffects` helper and its current participation/cache improvements, but preserve cadence's `reconcileStatusEffects`, `prepareStatusRates` and `integrateStatusRates`. Capture rates and conditional predicates at interval start for all subjects before applying endpoint effects. Do not replace these phases with sound's older serial transition-plus-rate loop. New automatic activation and its predicted boundary both use fresh self-bindings, not a retired episode's `$source` or `$actionTarget`. Active instances retain their own bindings. Predicate-pruning is a valid bound for this interval, never a replacement for actual status admission.
- `object-exposure.ts`, `encounter-cache.ts` and `kernel.ts`: keep one ID-only exposure cache and unchanged episode-membership optimization, including geometry/body/pose/range and capability invalidators. Sound's acquisition batching may replace repeated acquisitions only within one stable post-motion sensing phase. Its immutable physical-phase finalization/unchanged-exposure proof applies per accepted elapsed interval, not once after an arbitrary multi-interval call. Camera or unchanged visual exposure never suppresses speech admission or a contact change.
- `kernel.ts` and temporal boundaries: `advanceWorld` returns a bounded elapsed prefix; `maxIntervals` bounds work, not game seconds. A newly arrived or newly created action receives none of the preceding interval's work. Preserve swept-body validation and explicit support seams. Sound's native supply indexing can be reused, but its one-second inner slice loop is not the cadence controller. Many staggered flyer/status deadlines currently force global work; regional scheduling remains PF13.11, not a reason to delay events or truncate listeners.
- Physical contact in `perception.ts`, `world-modules.ts`, `kernel.ts`, `worlds/base/senses.ts` and the world upgrader: retain main/sound's body-contact detector and in-place correction of retired proximity detectors. Include body extents in conservative candidate discovery. A zero-radius contact definition does not imply a zero-length integration interval. Endpoint contact and detection of a fleeting between-endpoint tangency are different guarantees; the latter needs an explicit crossing bound or declared sampled policy, not a hidden claim of exactness.
- `world-service.ts`: subtract actual `world.simTime` progress, retain debt, preserve navigation/pause/command fences and routine durability. The host wake frequency and real-time persistence policy are coordination choices, not biological ticks. Memory-consolidation pressure cannot block native time. Existing mutations still serialize effects, independent of how speech requests are scheduled.
- `events.ts`, `speech.ts`, `acoustics.ts` and perception/history: calculate heard evidence at committed emission state, retaining listener-specific full/partial/no-word content and identity scope. Do not replace graded exposure with the older binary radius/0.65 gate. Threshold arithmetic valid for that older gate is not automatically a dB-policy implementation. Retain stable cache admission, exact overflow and current acoustic broad-phase bounds.
- `draft.ts`, `experience.ts` and `status-capabilities.ts`: preserve sound's append/index/capability optimizations without duplicate mutation owners. Recheck snapshot identity, nested commands, parent/fork isolation and invalidation after integration; compilation alone does not establish these boundaries. Do not introduce the abandoned mutable-entity optimization under the guise of conflict resolution.
- `scene.ts`, `main.tsx`, view/protocol: retain full camera-facing aspect and virtual depth, support-aware interpolation, shadows and authorized read-through alongside caption anchors. Caption anchors follow displayed current/recent-past poses, never another actor's private future route. Sparse state arrival does not change the browser's frame callback cadence.

## Independent event and presentation clocks

Speech is currently an instantaneous committed occurrence, not a sustained acoustic process. Sparse simulation is no reason to poll speech at the next interval or batch emissions across movement, changed noise/geometry, sleep or permission boundaries. External commands interleave through the normal mutation owner. A future native timed utterance must end the interval at its emission time; a sustained utterance needs explicit start/end/interruption boundaries.

Conversation inactivity and physiological deadlines use game time. Caption reading lifetime, presentation expiry and render interpolation retain their explicit real-time clocks and pause/hidden behavior. Overlapping captions do not imply physically competing noise. State publication at 20 Hz, if later adopted, is not a 20 FPS rendering limit.

## Acoustic guardrails

Source SPL, three-dimensional attenuation and listener floor determine candidate extents; shouting may exceed the former small radius. Preserve every legitimate listener and historical emission origin. Do not truncate audiences, reroll stored fragments, expose full original words through recall/history fallbacks, or reinterpret past hearing at a listener's current position. Navmesh rays are not sight/sound rays. Geometry caches store transfer, not listener permissions or semantic evidence.

## Joint acceptance to retain

Cross-cutting coverage belongs in [TODO](TODO.md), not a duplicate test list here. The combined branch needs actual emission-before/after-motion, stacked floors, sleep/sense changes, same-world restore, partial-text persistence and 1x/8x caption lifetime observations. Record advanced time, pending navigation, debt and real event counts. Use sound's independently scheduled demand; a slowed producer must not make throughput look successful by offering fewer utterances. Neither branch's individual evidence qualifies the combined application automatically. A historical passing run and a later failing run must retain their own workload/host context.

## Message for the sound-branch implementer

Use `feature/simulation-cadence` as the time-integration side, after reconciling its newer-main conflicts. Keep your graded acoustics, receiver index, transfer reuse, stable bounded cache admission, batched evidence, real-time captions, physical contact, append/finalization work and asynchronous safety fixes. Preserve cadence's three-phase status/rate integration and fresh automatic-activation bindings. Replace the mandatory-one-second/480-ticks assumption in HE05 and stress tooling with actual game-time progress and measured interval counts. Exercise the joint system using your independent speech producer, not only the easier no-speech cadence workload. Caption animation remains at browser frame cadence. No hearing re-authoring, new renderer or duplicate scheduler is required.
