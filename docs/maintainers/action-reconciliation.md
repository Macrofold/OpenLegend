# Action branch reconciliation and recovery

**Status:** active integration subtracker of [AC05/AC08/AC10–AC12](action-capabilities.md) and [PF00/PF03/PF09/PF11](performance.md). This document owns branch-integration tasks, not a second action design or performance program. Parent acceptance gates remain unchanged.

## Resume here

Continue on `review/action-main-reconciled`. The latest verified runtime source is `d143461b85662b4ad7938968162e2674130b1cdb`; subsequent changes through `45ba48fb7a6022897ac76814b181a01eca87c56b` are documentation and removal of the temporary review workflow. Re-read the branch ref before writing. The preserved source branch is `review/action-cleanup-application` at `614764d77723955cfcf5972cd8f0bff8a70f3c12`; the replay used main `03105fed9209c126e4e69e9faeb4687f42d1e74a`. Inspect any newer main work separately. No force-push, main merge or world reset is authorized here.

Do not reconstruct the action replay again. Exact source comparison found the expected import, bounded-awareness-sequence, confirmation-fallback, contact, follow and death changes without an unintended large-file omission. The implementation commits `84bad771` and `1c797464` remain in this branch. Integration-only PR #20 was closed without merging because replay already superseded its old source-to-working-branch direction; both branches remain preserved.

The delivered continuation is `c6a924c` (body-sized contact grid), `565ec02f` and `cb5af992` (explicit disposable PostgreSQL profiling and owned startup cleanup). The temporary `.github/workflows/action-review.yml` was removed in `45ba48fb`; `.github/reconcile-action.py` is also absent. Ordinary CI was not modified. Run the reusable profiler directly using [profiling guidance](performance-profiling.md), rather than restoring a source-patching workflow.

Exact measurements, source hashes, runtime cases and limits are in [the focused Verification record](../verification/action-reconciliation.md). That record preserves both the failed original PostgreSQL setup and completed current-source runs without depending on expiring artifacts. In brief: production builds passed, contact/event/world digests matched in two isolated contact layouts, and PostgreSQL profiling cleaned up both successful and failed-startup fixture databases. Dense and accelerated throughput remain below target; ordinary CI remains blocked at formatting.

## Scope and canonical owners

- [Action capabilities](../action-capabilities.md) and [AC tracker](action-capabilities.md): parameterized native move/follow, explicit target and queue/replace authority, Jev-first grounding, partial-fulfillment disclosure, immutable revised-action approval, durable attempts and existing sequential plans. Preserve the mechanical-workflow reconciliation seam; invention authoring remains INV-owned and black-box here. No universal scripting, new scheduler or unsupported mechanical families.
- [Spatial world](../spatial-world.md#physical-contact), [knowledge](../knowledge.md#subject-binding) and [Architecture](../architecture.md): physical body contact rather than proximity; anonymous private evidence, body extent, detector changes, sleep/perception loss, death/revival and continuous encounter identity. A candidate-index optimization changes neither physical law nor sensory authority.
- [Performance](../performance.md), [PF tracker](performance.md) and [profiling guidance](performance-profiling.md): cooperative native work, database/timer/HTTP/SSE measurements and honest limits. Never drop legitimate evidence, weaken target scope or reduce requested simulation speed to improve reported results.
- [Save/load](../save-and-load.md#active-development-policy): identity-preserving upgrades, accounting and atomic publication. Retain main's content/save-count removals and context-budget rules; no parallel legacy runtime.
- [Verification](../verification.md) and its [focused record](../verification/action-reconciliation.md): actual evidence. Existing [TODO](TODO.md), including [physical contact coverage](TODO.md#physical-contact-correction), retains deferred cross-cutting regression work. No automated suites were authored or invoked in this delegated session. Static checks and isolated native/runtime exercises use zero paid calls; ordinary CI remains intact.

## Integration tasks

- [x] AR01.1 — Compare the committed replay with its generated candidate and inspect all differing production files. No unintended source omission found; production TypeScript/Vite builds passed locally and on GitHub. This is not full behavioral acceptance.
- [ ] AR01.2 — Repair in-scope `packages/domain/src/kernel.ts` import formatting. The two inherited failures in `scripts/check-agent-guidance.mjs` and `docs/maintainers/agent-guidance.md` remain separately owned. Report the actual ordinary CI gate without weakening checks or silently formatting unrelated files. Current-source runtime review success is not full CI success.
- [x] AR02.1 — Size the optional contact grid from actual body extents. Crowded/scattered candidate work decreased with identical exact ordered contacts, emitted events and final-world digests. Conservative vertical/horizontal discovery, source ordering, exact overlap and barriers were retained. Frame construction still samples world entities; this is not a persistent regional index.
- [ ] AR02.2 — Finish broader contact lifecycle qualification: sleep/waking, changed detector definitions, dead observers, physical boundary/height cases and unavailable memory capability. The small native walkthrough covered onset, stationary/reload stability, detector loss/regain, source removal and private views only. Existing physical-contact regressions in TODO remain open.
- [ ] AR03 — Finish follow/admission/replacement review: locomotion loss, target death, obsolete perception episodes, queued revisions, approval authority and durable restore. Native loss-of-sight continuation was exercised and stopped correctly. Do not treat that case as full AC or approval acceptance.
- [x] AR04.1 — Recover the stopped PostgreSQL run and complete current-source full-server diagnostics. Original missing-vector failure and later measured throughput/tail failures are retained under Verification, not silently converted into a pass.
- [x] AR04.2 — Add reusable explicit loopback PostgreSQL profiling with uniquely owned databases, no inherited application URL, sanitized diagnostics and startup-store cleanup. Real PostgreSQL/vector scenes and missing-vector initialization were exercised; all fixture databases were removed. Refused connection and default SQLite fallback were also exercised locally. General factory cleanup is not included.
- [ ] AR04.3 — Investigate remaining accelerated/dense throughput and command-tail failures under the existing PF00/PF11 program. The latest mixed requested-3x case achieved about 1.42x; dense requested-1x achieved about 0.288x with a 5.88 s maximum command. Repeat on a matched host and inspect native, history, persistence and clock-admission spans before selecting a fix. The earlier runner used a different CPU; contact-only changes cannot explain ordinary sight/hearing results. Long soaks, concurrent players and remote topology remain unqualified.
- [x] AR05 — Retain useful evidence and reproducible invocation, remove temporary transport/review scaffolding, and close superseded integration PR #20 without merging it. Ordinary CI and both source branches are preserved.
- [x] AR06.1 — Link the recovery tracker from the maintainer index and profiling guide; retain scoped evidence under `docs/verification/`; replace obsolete SQLite-only invocation guidance. The existing physical-contact law is unchanged, so no alternate spatial specification was introduced.
- [ ] AR06.2 — At final action-program closeout, reconcile remaining AC/PF parent status and any affected architecture/changelog entries with actual delivery. Do not duplicate existing TODO test matrices or close unmet parent gates merely because this integration slice built.
- [ ] AR07 — Fix general `createGameServer` startup cleanup for callers other than the profiler. A failure before the server handle returns can leave its internally allocated store/connection open. Preserve the original startup error, close only owned resources, and exercise unavailable extension/connection/writer-lock paths. The profiler's explicit cleanup does not mark the factory fixed.

## Next bounded sequence

Start with AR01.2 and the small AR07 ownership fix, then finish the remaining AR02.2/AR03 correctness review. For performance, use AR04.3 and the reusable PostgreSQL profiler; do not rebuild the replay, restore temporary workflows, or start a speculative general simulation worker. Commit new code and matching task state at least every five minutes. Each handoff must name the exact code revision, exercised behavior and open acceptance work, without relying on this conversation or transient artifacts.
