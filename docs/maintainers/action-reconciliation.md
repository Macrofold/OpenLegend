# Action branch reconciliation and recovery

**Status:** active integration subtracker of [AC05/AC08/AC10–AC12](action-capabilities.md) and [PF00/PF03/PF09/PF11](performance.md). This document owns branch-integration tasks, not a second action design or performance program. Existing parent IDs and acceptance gates remain unchanged.

## Resume here

Continue on `review/action-main-reconciled`; the inspected source checkpoint is `2f0e57bab7660413f5224ac52974ca7838c43ebb`. The preserved source branch is `review/action-cleanup-application` at `614764d77723955cfcf5972cd8f0bff8a70f3c12`. The replay used main `03105fed9209c126e4e69e9faeb4687f42d1e74a`. Re-read refs before writing; inspect newer main changes separately. Do not overwrite concurrent work, force-push, or merge into main.

The integrated commits `84bad771` and `1c797464` publish the action replay and follow/death lifecycle changes. Exact file comparison against the earlier generated candidate found only the intended kernel import cleanup, decision-context import/sequence-reduction/approval fallback changes, typed/cooperative contact updates and follow/death changes; no missing large-file implementation was found. Production TypeScript and Vite builds succeeded locally on Node 22.16.0 and in the current-source GitHub run on Node 22.23.2. Do not recreate the replay from memory or rerun an old reconciliation script over newer work.

The original PostgreSQL run `36105644998` built production source but initialized no gameplay phases: its plain PostgreSQL container lacked `vector`, raising SQLSTATE `0A000`, and the process remained open until timeout. Ordinary CI run `36105644927` stopped at formatting in `packages/domain/src/kernel.ts`, `scripts/check-agent-guidance.mjs` and `docs/maintainers/agent-guidance.md`; it did not reach typechecking or tests. The latter two files were inherited unchanged, not action implementation changes. Full CI is not green.

The corrected disposable PostgreSQL/vector run `36180280531` completed against `2f0e57`: mixed scenes achieved approximately 0.820x at requested 1x and 0.701x at requested 3x; the dense scene achieved 0.0748x at requested 1x. Storage errors were absent, but requested speeds and dense command latency were not acceptable. These short phases are failure/diagnostic evidence, not capacity qualification. Preserve a concise sanitized report under the Verification owner; raw artifacts/profiles remain outside version control.

## Scope and canonical owners

- [Action capabilities](../action-capabilities.md) and [AC tracker](action-capabilities.md): parameterized native move/follow, explicit target and queue/replace authority, Jev-first grounding, partial-fulfillment disclosure, immutable revised-action approval, durable attempts and existing sequential plans. Preserve the mechanical-workflow reconciliation seam; invention authoring remains INV-owned and black-box here. No universal scripting, new scheduler or unsupported mechanical families.
- [Spatial world](../spatial-world.md#physical-contact), [knowledge](../knowledge.md#subject-binding) and [Architecture](../architecture.md): physical body contact rather than proximity; unidentified private contact evidence; body extent, detector changes, sleep/perception loss, death/revival and continuous encounter identity. Visual range is not body radius. Preserve exact geometry and deterministic ordering.
- [Performance](../performance.md), [PF tracker](performance.md) and [profiling guidance](performance-profiling.md): shared perception snapshots, cooperative native work, database/timer/HTTP/SSE measurements and honest dense-world limits. Never drop legitimate evidence, weaken target scope or reduce requested simulation speed to improve reported results.
- [Save/load](../save-and-load.md#active-development-policy): identity-preserving development upgrades, accounting and atomic publication. Retain main's removal of arbitrary content/save-count ceilings and its current context-budget rules. No world reset or parallel legacy runtime.
- [Verification](../verification.md): actual commands, source revisions, measurements and limitations. Existing [TODO](TODO.md) owns deferred cross-cutting regression coverage. No automated suite authoring/execution for this delegated implementation; ordinary CI remains unchanged. Use static checks and isolated native/runtime/performance exercises with zero paid calls.

## Integration tasks

- [x] AR01.1 — Compare the committed replay with its generated candidate and review all differing production files. No unintended source omission found; local and GitHub production builds passed. This is not full behavioral acceptance.
- [ ] AR01.2 — Repair in-scope formatting, retain the two inherited formatting failures as separately owned work, and report the actual remaining ordinary CI gate. Do not weaken CI or silently format unrelated files.
- [ ] AR02 — Finish physical contact integration review. Current lookup uses the default sight-sized 28-unit grid although physical reach is body-sized. Measure a body-extent-sized candidate grid before retaining it, compare exact ordered contact/event outputs, and include dense and scattered layouts. Keep conservative vertical/horizontal discovery and exact overlap/occlusion; no sensory cap, changed body law or retained draft. Exercise detector replacement/loss, sleep/waking, source removal and privacy. Existing physical-contact regression coverage stays in TODO.
- [ ] AR03 — Review follow admission/continuation and replacement authority after integration: locomotion loss, lost sight, target death, obsolete perception episodes, queued revisions and same-version restore. Fix demonstrated defects through existing owners, without claiming all AC acceptance complete.
- [x] AR04.1 — Recover the stopped run and complete a current-source PostgreSQL/vector measurement. The measured throughput and dense tail failures remain open, not waived by a successful workflow exit.
- [ ] AR04.2 — Make the existing full-server profiler reproducibly select an explicitly disposable loopback PostgreSQL database, rather than editing its source inside a workflow. Never inherit a user's normal database URL. Create a uniquely named fixture database, retain resource ownership through startup failure, remove only successfully created fixture resources, sanitize diagnostics and disable paid dispatch. Keep SQLite as the default local fallback. Qualify normal teardown and failed startup with actual runtime evidence; a hard deadline remains a final safety net.
- [ ] AR05 — Remove temporary reconciliation transport/workflow scaffolding after retaining useful evidence and reproducible invocation. Keep ordinary CI intact. Check the integration-only PR before changing it; never merge the stale source branch back over the reconciled result or into main.
- [ ] AR06 — Reconcile implementation/evidence/task owners and navigation: appropriate subsystem contracts, Verification, AC/PF status, existing deferred TODO coverage and consequential history. Re-read touched files and publish a recoverable handoff without falsely closing parent gates.
- [ ] AR07 — Fix general server-factory startup cleanup separately from profiler ownership: `createGameServer` can fail before returning a handle, leaving its internally allocated store/connection open. Preserve the original startup error, close only owned resources, and exercise unavailable extension/connection/writer-lock failures. The profiler's explicit cleanup alone must not mark the general factory fixed.

## Next bounded sequence

Complete AR02's measured candidate-lookup change and AR04.2's reusable isolated profiler, committing code and matching task state at least every five minutes while new code exists. Then qualify current source, preserve concise evidence and remove temporary scaffolding. Do not start a speculative general simulation worker or change database/clock semantics based on these small samples. End each slice with its commit, exercised behavior and remaining acceptance work; recovery must not depend on this conversation or expiring CI artifacts.
