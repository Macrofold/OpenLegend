# Simulation boundary catalogue

This is a running design inventory for [simulation time](../simulation-time.md), not a second task tracker. PF13 owns the current delivery; PF/SW/EPR and feature owners retain future implementation tasks. Add a row when a mechanic gains a useful exact deadline, validity bound or unresolved coupling. A deadline predicts when work may matter; it never authorizes an effect or proves that an arbitrary larger interval is safe.

## How to extend the catalogue

For each scenario record: authoritative inputs; what remains constant; earliest deadline or conservative bound; invalidators; required boundary work; implementation/evidence status; and its existing owner. Use game-time deadlines for mechanics, monotonic real time for host/network/reading budgets, and rendering time for appearance. Never turn a timer unit into a universal update frequency. Inputs with unknown evolution need a bounded fallback rather than a speculative certificate.

**Status key:** integrated = current finite adapter contributes a bound or exact elapsed calculation; bounded = a deliberate approximation with known limits; candidate = not implemented. Current runtime evidence belongs in Verification; these entries are not universal mathematical or scale certification.

A bound is conditional on unchanged inputs, not an assertion that its event will actually occur. Recompute at an earlier command, geometry/status transition or changed participant. Do not cache an Infinity bound as permanently impossible: another committed mutation can change its premises. Predicate evaluation and the eventual effect must use the same subject/source/target bindings. Rate comparisons at equality may need one bounded strict-side departure; arbitrary clock nudges are not a remedy for a stuck state.

## Mechanical time and motion

| Scenario | Bound or expiration | Invalidators and required work | Status / owner |
| --- | --- | --- | --- |
| Idle unchanged world | Current base fallback horizon of one game minute; not one transition per game second | Any admitted command, policy, geometry or participant change invalidates predictions. More inert worlds can eventually use the earliest actual deadline without a periodic scan. | Bounded; base time policy / PF13 |
| Linear hunger/energy/reservoir | `(threshold - value) / rate`, considering direction, clamp and concern/recovery boundaries | Rate/need capability/status change, eating, replenishment, damage, editor mutation. Capture rate predicates at interval start and clamp at the boundary. | Integrated finite rates; status/attribute owners |
| Empty food or energy | Depletion first, then elapsed starvation/exhaustion damage; health exhaustion is another boundary | Food, recovery, life/capability and conditional-rate changes. Never charge the fed portion of an interval as starvation. | Integrated base physiology |
| Coupled/opposing clamped rates | Sum is not generally equivalent to serial clamps at saturation | Current fallback limits those unsupported combinations; future integrator must define composition, conservation and intermediate effects explicitly. | Bounded; status/PF13 |
| Finite work | Current action remainder; capacity or supply depletion can finish earlier | Interruption, target movement/removal, changed recipe/body/required tool, source exhausted by someone else. New work starts at its boundary and receives no previous interval. | Integrated native actions |
| Shared finite supply | Remaining supply / total currently active demand | Participant/action/rate changes, capacity completion or removal. Preserve deterministic ownership and debit/credit together; do not reuse a stale demand aggregate. | Integrated forecast plus native validation; richer coupled flows remain candidate |
| Ground locomotion | First relevant waypoint/support seam, arrival, or maximum permitted observation displacement | Geometry/profile/capability/target changes, unexpected collision. Sweep the body across movement; visual interpolation is not safety. | Integrated Recast/native movement |
| Known static route segment | A validated segment could remain valid until relevant geometry/body/traversal dependencies change | Dynamic occupants, support state and capabilities remain separate. A subset of a valid sweep may permit geometry work reuse. | Candidate; SW17 / PF12.6 |
| Small native wander | Each animal retains its timer remainder; due random impulses are ordered by deadline then ID | Death, fleeing, controlled action, capability or membership changes. Current 0.4 m non-emitting wander impulse is smaller than the default perception travel bound; the horizon is shorter than minimum recurrence. | Integrated local elapsed timer; bounded perception, not continuous crowd motion |
| Flight | Perch wait, route waypoint/landing, climb-limited speed and observation displacement | Route/profile/geometry changes, loss of flight, blocked landing. Avoid consuming pre-takeoff time as airborne work. | Integrated current corridor family; holding/fairness remains SW12 |
| Many independently staggered deadlines | Earliest deadline of each relevant dependency set, rather than every deadline forcing all entities to integrate | Local motion can become nonlocal when visibility, sound, collision, shared supply or a command couples participants. Materialize the relevant states at that actual occurrence before resolving it. Coalescing distinct deadlines into one later instant is not equivalent. | Candidate regional/subsystem work; PF13.11 / PF12.3. Current global earliest-deadline reduction is a measured scaling limit |
| Falling | Closed-form acceleration to terminal speed; solve time to support/displacement bound | Support geometry, velocity or capability change. Reconcile physical support; no interpolation through a floor. | Integrated finite vertical fall; general rigid-body/contact dynamics are separate |
| Moving platforms/vehicles | Relative transform/contact events, boarding and disembarking | Platform route, riders, acceleration, collisions, support ownership. A static support certificate is insufficient. | Candidate; SW |
| Teleport | Immediate admitted action, not a walk path | Destination/body/permission/cost validation at commit. Revoke all movement/sensory certificates. Portal itineraries need a higher-level planner; ordinary teleport execution does not. | Candidate traversal family; not required by cadence |

## Perception, sound and cognition

| Scenario | Bound or expiration | Invalidators and required work | Status / owner |
| --- | --- | --- | --- |
| Motion-driven sight/contact | Current finite displacement bound; smaller positive sense radii tighten it | Movement, observer availability, source life/body, geometry or senses. Sample at each accepted boundary; this does not prove every short peek or tangency is observed. | Bounded, not exact exposure crossing; EPR |
| Physical contact onset/exit | Solve relative-body contact/clearance along known motion or retain a declared sampled fallback | Shape, relative trajectory, support, collision, capability and contact policy. A zero-radius contact detector is not a zero time step; neither may proximity be substituted for actual body contact. | Candidate crossing certificate; reconcile main's body-contact implementation during integration |
| Stationary static-object exposure | Exact geometry result remains valid for unchanged immutable target poses/heights, observer pose/eye/range and map | Add/remove/move/change a target, geometry, observer/body/sense. Cache IDs only; resolve names/recognition fresh. No output truncation when cache capacity is reached. | Reused from sound branch; EPR/PF09 |
| Outside sensory range | Conservative earliest approach: gap / maximum relative speed | Teleport, speed/range/geometry/body changes; unknown speed invalidates. This proves only range exclusion, never unchanged LOS. | Candidate kinetic certificate; EPR/PF12 |
| LOS currently clear/blocked | Swept visibility corridor or stable occluder witness | Moving observer/target/occluder, door changes, profile/sense changes. A fixed time-to-live is not an exact proof. | Candidate; EPR/spatial |
| Instantaneous speech/sound | Resolve once at its committed emission state | Source/listener positions and availability at occurrence. Store permitted heard result; no per-tick/per-frame reroll. | Sound branch owns new dB model and listener indexing |
| Sustained sound | Source start/end or change, listener boundary, obstruction/noise change | Movement, amplitude, medium, geometry and listener state. Group only events sharing a valid sensing state. Do not treat caption duration as physical emission duration. | Candidate; EPR/audio |
| Cached acoustic transfer | Same geometric anchors and relevant geometry revision | Any changed path dependency. Apply each event's level/threshold/permission independently. Historical origin never follows the current source. | Sound branch has transfer reuse; general dependency certificates remain candidate; EPR |
| Room/portal exclusion | Conservative component/portal topology and influence bounds | Door/opening/material/topology change, new source or propagation family. Coarse rejection must be conservative; no hidden-topology leak. | Candidate; EPR/SW |
| Semantic reactions / attention | Committed stimulus, need-band crossing, configured deadline or plan outcome | Actor/knowledge/goal changes, stimulus revocation. Coalesce redundant opportunities, not distinct meaningful evidence. Native execution must not require model calls. | Existing cognition owner; generalized analytic trigger intake remains candidate |
| Goal, promise, conversation | Due game-time deadline, completion evidence or explicit inactivity expiry | New relevant action/speech, pause/resume policy, cancellation, participant change. Ties resolve at one explicit boundary. | Existing commitment/conversation deadlines integrated; general goal scheduling remains AG/CR |
| Sleep / status | Activation/deactivation comparison, daily window edge, reactivation delay, changeRate predicate | New command, source/target removal, capability/policy changes. Start-state predicates control the interval; no retroactive restoration/expenditure. | Integrated finite condition grammar; arbitrary predicates must supply a bound |
| Inactive automatic status with a retired episode | Reactivation delay followed by the automatic predicate under fresh subject/self bindings | A prior manual episode's source/target must not survive into the forecast for a new self-bound automatic instance. Reevaluate actual admission at the boundary. Manual-only inactive effects have no automatic deadline. | Integrated binding correction; PF13.16 / SR28 |
| Compound conditional rule | A false AND cannot become true before all currently false children could change; OR has the dual bound | The bound assumes captured linear rates and unchanged nonnumeric gates for this interval only. Earlier mutations/other boundaries invalidate it. A stable blocking gate can eliminate irrelevant comparisons without suppressing another rule's actual transition. | Integrated conservative predicate bound; PF13.16 / SR27–SR28 |

## World systems and infrastructure

| Scenario | Bound or expiration | Invalidators and required work | Status / owner |
| --- | --- | --- | --- |
| Fire fuel / finite emitter | Fuel / burn rate; current native heat uses remaining fuel seconds | Refuel/extinguish/rate or source change. Emit expiry once at the boundary. Heat transfer, spreading and secondary ignition need their own events. | Integrated depletion; thermal propagation candidate |
| Growth, decay, weather, crafting networks | Next stage/threshold, recipe completion, environmental change or scheduled transition | Inputs, environment, inhibitors, resource competition and feedback loops. Some permit closed forms; nonlinear/coupled systems require bounded numerical integration. | Candidate world families; not engine-wide assumptions |
| Status cascade at one timestamp | Bounded transition closure, explicit priority and cycle detection | Newly admitted mutually activating rules or action effects. No unbounded fixed-point loop. Current ordered reconciliation is finite, not a complete simultaneous equation solver. | Current bounded ordering; richer closure candidate |
| Native random choices | Next saved local deadline, ordered tie-break | Participant changes, external events. Retain countdown remainder; do not redraw because a callback/load occurred. Global interleaving with future random emitters needs explicit ordering. | Current wander ordering; counter-based/event-local RNG candidate |
| Navigation preparation | Geometry/profile/traversal versions, request cancellation, computation budget | World/load/action changes. Required technical wait is not game-time consumption. Reuse bounded workers and prepared meshes. | Existing SW17; per-tile rebuilds/regions candidate |
| Historical state / maintenance | Due consolidation/retention obligation, memory pressure, changed evidence | Revocation, pending commitments, paid-work uncertainty and storage failure. Do not discard necessary evidence to satisfy latency. | Existing PF08/CR; cold-store separation candidate |
| Network pose replacement | Real-time publication window, newest permitted state | Action acknowledgement, removal, authorization or reliable event bypass replacement coalescing. Never ship another actor's future route. | Existing host cadence; advanced delta batching candidate |
| Rendering | Device frame callback; visual interpolation deadline | Pause/reload, new state, camera, geometry or representation. 20 Hz state delivery does not mean 20 FPS. Recent-past support-aware paths can improve long gaps. | Existing independent renderer; richer motion buffering candidate |
| Captions / reading | Real-time reading duration, explicit hidden/pause policy | Event revision/revocation, settings, pause/visibility. Never accelerate reading by game-clock speed. | Sound branch presentation owner |
| Cache / worker lifetime | Input revision, resource budget, ownership epoch | World disposal, source mutation, shutdown, renderer loss. Retain bounded derived data only; stale work cannot commit effects. | Existing owners; long-session qualification remains open |

## Scaling direction, not a prerequisite framework

Start with the few implemented rate/deadline families. Next split dirty static/dynamic work and per-entity/local schedules so one fast flyer, many staggered independent timers or one short-range observer do not shorten unrelated regions' intervals. The [cadence evidence](../verification/simulation-cadence.md#failed-staggered-flight-scaling-probe) now triggers RP03: the global reduction is a demonstrated bottleneck, not merely a theoretical concern. Neither enlarging the displacement bound nor deleting the timers is an accepted fix.

Later consider a deadline heap, spatial dependency index, kinetic bounds and analytic dormant systems only when their inputs/invalidation are explicit and measurements justify them. A heap alone makes deadline selection faster; it does not eliminate integrating the whole world for each selected deadline. Local integration needs a dependency/occurrence boundary and stable ordering when participants interact. Preserve one authoritative ordering/commit owner. An exact mechanical integrator plus approximate sampled perception must be described as such, not advertised as exact continuous reality.
