# World Agent write continuation verification

This record owns the bounded observations for the current continuation of the reviewed MCP authoring implementation. It supplements historical observations in [Verification](../verification.md); no prior test, live-model or capacity claim is inherited.

## Delivered scope and evidence boundary

The resumed branch already contained the unified owner conversation, native Macrofold executor and session-bound MCP draft/review/Apply path. This continuation repairs dependency invalidation, portable session-history queries and committed-Apply recovery, correlates strict tool arguments with their names, and adds reviewed custom body-attribute attachment through the existing native owner. The source and tests on main have not been rebased into this continuation; current main guidance was inspected separately.

Production TypeScript compilation (`tsc --noEmit -p tsconfig.build.json`) and the Vite production build passed on the local integrated files. The existing main client chunk warning remains (approximately 2.661 MB minified / 720 KB gzip). No automated unit, integration or browser suite was written or run. No provider credential was read, no real model or image was requested, and paid provider spend is $0. Automatic repository CI is separate from these manual commands.

## Actual HTTP and MCP execution

A disposable SQLite world ran through `createGameServer`, with automatic ticking disabled for controlled execution, owner mode enabled and AI funding set to zero. A random fixture bearer exercised the actual official-SDK MCP endpoint with protocol 2025-11-25. Initialization and discovery returned the registered read/write tools. The client drafted a cognition-policy revision, validated it and prepared a review. Apply before approval returned `needs_approval`; the authenticated same-origin human decision route approved the exact digest; MCP Apply committed it and replay returned the same result.

A malformed JSON payload returned an expected bounded finding. Owner session discovery and saved review inspection returned their durable records. The native configuration gate reports missing worker-compute configuration without claiming the remote provider was reached.

The same path installed a custom charge reservoir, then drafted and approved a separate `attribute-bindings` change targeting one body. The initial value was 50. One manually advanced native game second drained it to 49 with no inference. Apply replay did not reset the reservoir. Session close revoked the handle; subsequent write-context access was refused. These are synthetic authored objects, not live natural-language interpretation or autonomous NPC acceptance.

## Committed receipt and restart recovery

The runtime exercise simulated loss of the operational review projection after a successful native Apply by restoring only that plan's previously approved status. It then saved a newer draft revision and retried the original plan. Apply returned the original permanent receipt and the native cognition-policy revision stayed at 2; the new draft was not applied. This is a targeted crash-window simulation around a real committed native result, not a process-kill matrix.

A new attempt to attach the same custom attribute returned `attribute-binding-rejected`; the existing value was not reset. After closing and reopening the actual server, the charge remained 49, the policy remained at revision 2, the session remained closed, and the spending-attempt table contained zero rows. The graph exposed the body's applicable native/custom attribute and effective sense bindings through their actual owners.

## Actual PostgreSQL execution

An isolated PostgreSQL 17 service ran the actual `PostgresDatabase` and `WorldAgentStore` from commit `11526e88736944afd863894c2251114539e64169`. Initialization succeeded twice. Session ordering returned `[100,10,2]`; turn ordering returned `[100,11,10,9,2]`; a cursor before 10 returned `[9,2]`. Active-session selection returned only the marked interrupted session. [The retained record](workshop-postgres.json) includes the exact workflow run and scope. This is actual small-dataset PostgreSQL execution, not merely SQL translation inspection; it is not full-game PostgreSQL recovery or production capacity.

## Mature synthetic SQLite history

The actual authoring repository read a disposable dataset with 10,000 sessions, 10,000 retained turns and eight active sessions. Twenty repeated reads returned bounded pages. Session-read median/max were 0.070/0.381 ms; turn-read median/max were 0.064/0.322 ms; active-session-read median/max were 0.037/0.134 ms. Both session and turn first pages started at sequence/time 9999 and their next pages at 9979. SQLite's active-session plan used `world_agent_active` with `world_id` selection.

Fixture insertion and initial index creation are excluded from these timings. These are in-process synthetic local read observations, not HTTP throughput, stable percentiles, total indexing cost or full application performance. Current history/source envelopes remain bounded; broader SQL and concurrency qualification stays open.

## Existing native stress workloads

The existing native stress runner executed the committed gems and mixed scenarios for 180 steps, no warmup, frozen snapshots and requested 3× speed. This continuation did not change native simulation effects; there is no matched optimization claim.

| Workload | Entities | Native loop | Largest step | Headroom at 3× |
| --- | --- | --- | --- | --- |
| Gems / 500 added ground objects | 514 | 1,619.30 ms | 118.64 ms | 0.618 |
| Mixed people/animals/resources | 344 | 1,495.01 ms | 279.97 ms | 0.669 |

Both workloads remain below the required headroom of 1. The mixed result retained 3,254 events and 3,474 awareness entries. Earlier review baselines used different native revisions/workloads or environments and cannot establish a speedup here. No physical effects, witnesses or simulation time were suppressed to improve the score. Current-main integration and PF/EPR/SW cold/warm, persistence and population qualification remain open.

## Remaining evidence limits

No live Macrofold connection, real native harness/model usefulness, generated image, browser accessibility, full-game PostgreSQL workload, general interaction closure, or multi-user hosted authority was qualified. The complete image-inclusive funding hierarchy remains unfinished even though native model/worker allocations use the shared session scope. The foundation exposes six concrete authoring kinds, not arbitrary laws or physical effects. [Focused remaining work](../maintainers/world-agent-writes.md) links existing INV/AC/PF owners and deferred automated coverage in maintainer TODO.
