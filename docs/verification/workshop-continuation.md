# World Agent write continuation verification

This record owns the bounded observations for the current continuation of the reviewed MCP authoring implementation. It supplements historical observations in [Verification](../verification.md); no prior test, live-model or capacity claim is inherited.

## Delivered scope and evidence boundary

The resumed branch already contained the unified owner conversation, native Macrofold executor and session-bound MCP draft/review/Apply path. This continuation repairs dependency invalidation, portable session-history queries and committed-Apply recovery, correlates strict tool arguments with their names, and adds reviewed custom body-attribute attachment through the existing native owner. The source and tests on main have not been rebased into this continuation; current main guidance was inspected separately.

Production TypeScript compilation (`tsc --noEmit -p tsconfig.build.json`) and the Vite production build passed on the local integrated files. The existing main client chunk warning remains (approximately 2.661 MB minified / 720 KB gzip). No automated unit, integration or browser suite was written or run. No provider credential was read, no real model or image was requested, and paid provider spend is $0. Automatic repository CI is separate from these manual commands.

## Actual HTTP and MCP execution

A disposable SQLite world ran through `createGameServer`, with automatic ticking disabled for controlled execution, owner mode enabled and AI funding set to zero. A random fixture bearer exercised the actual official-SDK MCP endpoint with protocol 2025-11-25. Initialization and discovery returned the registered read/write tools. The client drafted a cognition-policy revision, validated it and prepared a review. Apply before approval returned `needs_approval`; the authenticated same-origin human decision route approved the exact digest; MCP Apply committed it and replay returned the same result.

A malformed JSON payload returned an expected bounded finding. Owner session discovery and saved review inspection returned their durable records. The native configuration gate reports missing worker-compute configuration without claiming the remote provider was reached.

The same path installed a custom charge reservoir, then drafted and approved a separate `attribute-bindings` change targeting one body. The initial value was 50. One manually advanced native game second drained it to 49 with no inference. Apply replay did not reset the reservoir. Session close revoked the handle; subsequent write-context access was refused. These are synthetic authored objects, not live natural-language interpretation or autonomous NPC acceptance.

Further recovery and stress observations are recorded below as they are performed. PostgreSQL execution, browser accessibility, live Macrofold connector/harness forwarding, generated art, general law composition and multi-user authority remain separate acceptance gates. Query translation inspection is not PostgreSQL execution evidence.
