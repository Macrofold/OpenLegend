### Reconciliation notes from the implemented review

Grounding resolves provider-local entity tokens through the shared request decoder before native binding. Generated bindings, accepted alternatives and queued native steps preserve targeted actors' encounter episodes. A changed anonymous encounter invalidates delayed target authority; a creator-authored acquaintance follows the bundled knowledge owner's durable-identity policy. Missing legacy pins do not establish new recognition: preserve the saved intent and require a fresh decision for an unbound actor target. Exact installed-definition dependencies and general workflow execution remain separate unfinished integration work. No new registry, interpreter or schema reset is introduced.

The current adapter carries response-local operation identity, original request and manifest revision into native admission. Saved pending identity additionally includes explicit target and queue/replace mode; the exact alternative is never silently overwritten. These fields should map to the common invocation/authority contracts rather than become a competing definition registry. Current persistence follows the active in-place upgrade policy. Missing non-authoritative target/mode metadata is defaulted conservatively; existing alternative modes, commands, IDs and text remain unchanged.

