from pathlib import Path
import re

def edit(path,old,new,count=1):
 p=Path(path);s=p.read_text()
 if old not in s:
  if new in s:return
  raise RuntimeError(f'Missing source {path}: {old[:120]}')
 p.write_text(s.replace(old,new,count))
def prepend(path,text):
 p=Path(path);s=p.read_text()
 if text not in s:p.write_text(text+s)

# One provider codec, and total strict provider shape, with nested request-bound target IDs.
prepend('apps/server/src/cognition-contracts.ts',"import { navigationInvocationSchema } from './action-grounding.js';\n")
p=Path('apps/server/src/cognition-contracts.ts');s=p.read_text();a=s.index('        invocation: z');b=s.index("        mode: z.enum",a);s=s[:a]+"        invocation: navigationInvocationSchema.nullable().optional(),\n"+s[b:];s=s.replace(".extend({ targetEntityId: entityId.nullable(), actionId })", ".extend({ targetEntityId: entityId.nullable(), actionId, invocation: navigationInvocationSchema.extend({targetEntityId:entityId.nullable()}).nullable() })");s=s.replace("cognition-v10-private-invention","cognition-v11-action-invocations");p.write_text(s)
# Limit optional follow suggestions; parameterized discovery remains independent of this shortlist.
p=Path('apps/server/src/context.ts');s=p.read_text();s=s.replace('.filter((e) => e.actor?.alive && e.id !== actorId)\n    .slice(0, 16)', '.filter((e) => e.actor?.alive && e.id !== actorId)\n    .slice(0, 4)');p.write_text(s)
# Guard stale evidence inside the serialized commit too, not merely before it is queued.
p=Path('apps/server/src/ai-director.ts');s=p.read_text();s=s.replace("import { groundActionAttempts }", "import { groundActionAttempts, exactNavigation }")
needle='      this.maintenance.cancel();\n      const job: JobRecord = {'
if needle not in s:raise RuntimeError('Submit-action preflight anchor missing')
s=s.replace(needle,"""      const config=this.service.config;
      if(!exactNavigation(text,this.service.world,actorId,targetId) && (!config.budgetUsd || (!config.macrofoldKey && (!config.jevKey || !config.openaiKey))))
        return {ok:false,code:'ai-unavailable',message:'This wording needs configured Jev and language-model access with an allowance. Exact coordinate movement and unqualified visible-target following need no AI.'};
      this.maintenance.cancel();
      const job: JobRecord = {""",1)
p.write_text(s)
# The approval preview is native-derived, not solely the model's potentially misleading paraphrase.
p=Path('apps/server/src/action-grounding.ts');s=p.read_text();needle="    const fulfillment: ActionFulfillment = {\n      requested: text,\n      executableDescription: result.revised,"
if needle not in s:
 # New file may not have been formatted because it arrived in the staged tree.
 needle="    const fulfillment:ActionFulfillment = {requested:text,executableDescription:result.revised,"
if needle in s:
 prefix='''    const nativeDescription = boundCommands.map(command => {
      if(command.type==='move') return `Walk to x=${command.destination.x}, z=${command.destination.z} on ${command.destination.surfaceId}.`;
      if(command.type==='follow') return `Follow ${observed.visibleEntities.find(e=>e.id===command.targetId)?.name ?? 'the selected actor'} at ${command.distance ?? 3} world units until cancelled, interrupted or lost from sight. No stealth or sunset stop.`;
      return scoped.find(choice=>choice.commands[0]===command)?.description ?? `Perform ${command.type}.`;
    }).join(' Then: ');
    if(nativeDescription.length>1000) { await ports.record('Action explanation budget exceeded',{text},{steps:boundCommands.length}); continue; }
'''
 s=s.replace(needle,prefix+needle.replace('result.revised','nativeDescription'),1)
else:raise RuntimeError('Fulfillment native description anchor missing')
p.write_text(s)
# Indefinite holding must not look like 100% completed progress.
edit('apps/server/src/view.ts',"actor.action.stage === 'approaching'\n                ? 0", "actor.action.stage === 'approaching' || actor.action.type === 'follow'\n                ? 0")
# This change can persist new executable work/approval states: reject older development formats.
for root in ['packages/domain/src','apps/server/src']:
 for p in Path(root).glob('*.ts'):
  if '.test.' in p.name:continue
  s=p.read_text(); t=re.sub(r'(schemaVersion\s*(?:===|!==|:|<|>)\s*)9\b',r'\g<1>10',s)
  t=t.replace('schemaVersion: 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 | 9;', 'schemaVersion: 10;')
  t=t.replace("development-2026-09-22-spatial1", "development-2026-09-24-actions1")
  if t!=s:p.write_text(t)
p=Path('README.md');s=p.read_text().replace('development save format 9','development save format 10').replace('Schema 9 rejects','Schema 10 rejects');s=s.replace('## Explore the project','## Text actions and revised-action approval\n\nOpen **Character → Take an action** to submit a character intention separately from dialogue. Exact requests such as `go to x=12, z=14` and unqualified `follow Ada` can run without AI. Other wording uses configured Jev first, then bounded interpretation when needed. Disclosed tolerable omissions can run as partial fulfillment; uncertain revisions wait for **Accept revised action** or **Decline / withdraw**. Follow means remaining near a visible living target, not stealth, scent tracking, or a sunset deadline. Use the explicit support identifier when floors overlap. Choose Queue or Replace deliberately. Invention does not need to be unlocked.\n\nThis branch uses development format 10; select a fresh data directory for earlier worlds. See [action capabilities](docs/action-capabilities.md), its [workflow reconciliation boundary](docs/action-capabilities.md#mechanical-workflow-reconciliation), and [runtime evidence](docs/verification.md#action-capability-native-slice).\n\n## Explore the project',1);p.write_text(s)
# Remove the old unused interpreter only when no other source depends on it.
p=Path('apps/server/src/attempt-interpretation.ts')
refs=[str(q) for root in ['apps','packages','scripts'] for q in Path(root).rglob('*.ts') if q!=p and ('prepareAttemptInterpretation' in q.read_text() or "./attempt-interpretation.js" in q.read_text())]
if not refs and p.exists():p.unlink()
else:print('Retained interpreter references',refs)
# Current facts, target revision and reconciliation are deliberately separate.
with Path('docs/action-capabilities.md').open('a') as f:f.write('''\n\n## Partial fulfillment and initiator review\n\nThis section refines the earlier strict fidelity wording: **never silently change intent**, but allow an explicit useful subset when its omissions are tolerable. Missing criteria do not automatically require invention or rejection. The grounding result accounts for supported and omitted clauses, why they were omitted, the concrete revised behavior, and whether the initiating player/actor must decide. Approximation is not fulfillment of an exact guarantee.\n\nUse exact complete binding without inference first. Otherwise use Jev for finite full-match selection/classification before generative parameterization. A clear matching handle needs no generative translation. A bounded generative stage supplies required parameters and the omission report; Jev can classify whether a proposed omission is tolerable, needs the initiator's decision, or invalidates the method. Initial classification confidence is 0.8; uncertainty chooses interpretation or confirmation, not automatic execution. A model's semantic judgment never overrides native validation, scope, costs, or authority.\n\nFor a tolerable subset, retain the original request, supported/omitted clauses and rationale in Intelligence. Show the actual native-derived behavior and missing criteria to the player. An uncertain revision is stored in the existing actor-private pending-intent owner with exact commands, mode and relevant revisions; it starts no action and changes no current work. The player gets Accept/Decline controls. An autonomous actor receives accept/withdraw handles on its next eligible ordinary decision; no extra always-running reviewer or fabricated in-world conversation is created. Acceptance queues only that stored revision and rechecks live prerequisites.\n\nRemoving `until sunset` can extend the activity rather than narrow it: disclose that no sunset termination is enforced and show the real cancellation/interruption/lost-target rules. `Without being noticed` may be central; uncertain removal must return to the initiator. Skipping a prerequisite must not count it as completed. Dropped requirements are not promised for later and do not automatically dispatch invention or paid retries. Partial fulfillment and later physical success/failure remain different records.\n\n## Mechanical workflow reconciliation\n\n**Reserved integration owner:** reconcile this section with the separately developed invention composition/runtime contracts before adding a second general mechanics interpreter. Invention authoring (draft, validate, revise, activate) remains a black-box application workflow. An invented mechanical workflow (select targets, consume resources, work, wait, effect) belongs to simulation execution and must share the relevant native invocation/predicate/output/lifecycle semantics with actor activities.\n\n| This branch uses now | Future shared contract to reconcile | Explicit non-goal here |\n| --- | --- | --- |\n| Domain `NavigationInvocation` move/follow codec and native binding | Versioned invocation-facing descriptor in INV/EWF's common registry | No second mechanics registry or universal verb enum |\n| Existing concrete `Command` adapters and single AG action lane | Family admission, work/resource claims and executor interfaces | No generated effects, raw state setters or host code |\n| Existing bounded sequential plan and item receipts | Shared typed result ports, predicates, wait/branch/repetition semantics | No new general workflow DSL in this slice |\n| `Action.follow` path/hold cursor through the kernel movement owner | Per-invocation activity state, interruption and definition pins | No parallel movement/controller writer |\n| Existing actor-private attempts with an exact stored alternative | Common invocation identity, accepted scope and revision lifetime | No alternate goal store or independent approval service |\n| World manifest revision plus existing world/load epochs | Exact installed definition dependencies and invalidation | No silent upgrade of running work |\n| Jev-first classification and fulfillment reports | Scoped discovery, native summaries and interpreter evidence | No private invention registry leakage or automatic authoring |\n\nBuilt-in and invented families should eventually provide the same discoverable parameter/target schema, eligibility and knowledge policy, authoritative validation, resource/work contract, output ports, progress/completion/cancellation semantics and actor-safe projections. The action layer should not care whether an eligible installed implementation originated in code or an admitted definition. Method installation, learning, possession, action permission and execution remain distinct.\n\nContinue point movement, narrow follow and fulfillment review independently of invention's authoring pipeline. Before introducing general branching/repetition, compound mechanical effects or richer persistent processes, reconcile their execution representation with the invention branch. A temporary actor plan and a reusable definition need not be stored identically, but must not acquire contradictory semantics for costs, waits, results and cancellation. This section does not mark that integration complete.\n''')
with Path('docs/architecture.md').open('a') as f:f.write('''\n\nThe action slice advances current development saves to format 10 (`development-2026-09-24-actions1`): earlier development worlds are rejected without mutation or migration. The new provider response view requires nullable invocation fields and binds nested entity references to the current permitted set. Other actors cannot inspect a follower's private path/last-observation cursor. Partial/confirmation descriptions are derived from the actual native command bindings; the model supplies its semantic coverage explanation, not a substitute physical preview.\n''')
with Path('docs/maintainers/TODO.md').open('a') as f:f.write('''\n- Action follow-up: general parameter clarification (including choosing an ambiguous support) currently returns an unresolved intention and diagnostic, not a generated parameter form. Full dependency-specific retry invalidation, multi-target reference roles, richer workflow nodes, and exact definition pins remain AC/INV integration work. Add coverage for strict provider invocation schemas, private follow-cursor projection, fresh development format rejection, and native-derived revision previews.\n- Keep full repository typecheck repair separate: older test fixtures contain pre-existing async API, authority-provenance and removed goal-field errors. Production build/typecheck is exercised in this slice; no unit/browser suites or test-file changes are included.\n''')
with Path('docs/maintainers/action-capabilities.md').open('a') as f:f.write('''\n\n## Current delivered slice\n\nNative move/follow invocation binding, scoped target-bearing proposals, stored revised-action approval, Jev-first text grounding, player Character action controls and actor accept/withdraw handles are implemented narrowly. The actual scope and run evidence are in [Architecture](../architecture.md#jev-first-action-grounding) and [Verification](../verification.md#action-capability-native-slice). Existing task checkboxes above retain their broader acceptance requirements; no automatic completion of AC01–AC12 or AX tests is implied. Requested automated tests are collected in [TODO](TODO.md#action-capability-slice-deferred-automated-coverage).\n''')
with Path('archive/05-project/implementation-status.md').open('a') as f:f.write('''\n\n## Native action capability slice\n\nParameterized move/follow, Jev-first text grounding and explicit approval of uncertain revised actions now use the existing response/plan/action owners. Player input is in Character → Take an action; actor proposals share the resolver. See [current architecture](../../docs/architecture.md#jev-first-action-grounding), [evidence](../../docs/verification.md#action-capability-native-slice) and [mechanical-workflow reconciliation](../../docs/action-capabilities.md#mechanical-workflow-reconciliation). Broader capability registration, authoring integration, conditional workflow nodes and alternate topology support remain future work.\n''')
Path('/tmp/action-message').write_text('[skip ci] fix: harden action previews, scoped schemas and same-version persistence')
