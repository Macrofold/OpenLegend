diff --git a/apps/client/src/scene.ts b/apps/client/src/scene.ts
index 80b3c10..5bfc063 100644
--- a/apps/client/src/scene.ts
+++ b/apps/client/src/scene.ts
@@ -0,0 +1 @@
+import { configureBillboard, billboardBodyPoint, cameraDepthFraction } from './billboard';
@@ -27 +28 @@ import type { SceneCallbacks, WorldRenderer } from './world-renderer';
-import { birdArt, surfaceMesh } from './spatial-art';
+import { birdArt, surfaceMesh, surfaceSeamsMesh } from './spatial-art';
@@ -57 +57,0 @@ interface RenderedEntity {
-  shadowGeometry?: string;
@@ -475,0 +476 @@ export class WildernessScene implements WorldRenderer {
+    configureBillboard(material);
@@ -485,3 +486,10 @@ export class WildernessScene implements WorldRenderer {
-    // Upright cards keep world depth/foot anchoring independent of camera pitch.
-    sprite.setEulerAngles(90, (this.cameraSettings.yaw * 180) / Math.PI, 0);
-    sprite.setLocalPosition(binding.x, binding.y + binding.height / 2 + bob, binding.z);
+    // Full camera-facing cards retain their silhouette at every pitch. Body depth is
+    // supplied by the shared billboard shader; collision and shadow proxies never rotate.
+    sprite.setRotation(this.camera.getRotation());
+    sprite.rotateLocal(90, 0, 0);
+    const up = this.camera.up;
+    sprite.setLocalPosition(
+      binding.x + up.x * (binding.height / 2 + bob),
+      binding.y + up.y * (binding.height / 2 + bob),
+      binding.z + up.z * (binding.height / 2 + bob),
+    );
@@ -604,0 +613 @@ export class WildernessScene implements WorldRenderer {
+            configureBillboard(m);
@@ -625 +634 @@ export class WildernessScene implements WorldRenderer {
-    shadow.enabled = !crate && view.kind !== 'station';
+    this.presentation.setShadowVisible(shadow, !crate && view.kind !== 'station');
@@ -805,13 +814,9 @@ export class WildernessScene implements WorldRenderer {
-        for (let x = surface.minX + 0.4; x < surface.maxX; x += 0.5)
-          this.primitive(
-            'Deck plank seam',
-            'box',
-            timber,
-            node,
-            x,
-            surfaceHeight(surface, x, surface.minZ) + 0.008,
-            (surface.minZ + surface.maxZ) / 2,
-            0.018,
-            0.012,
-            surface.maxZ - surface.minZ,
-          );
+        const seams = surfaceSeamsMesh(this.app.graphicsDevice, surface);
+        this.geometryMeshes.push(seams);
+        const detail = new pc.Entity('Deck plank seams', this.app);
+        detail.addComponent('render', {
+          meshInstances: [new pc.MeshInstance(seams, timber)],
+          castShadows: false,
+          receiveShadows: true,
+        });
+        node.addChild(detail);
@@ -894,0 +900,4 @@ export class WildernessScene implements WorldRenderer {
+    const right = this.camera.right,
+      up = this.camera.up;
+    this.app.graphicsDevice.scope.resolve('ol_cameraRight').setValue([right.x, right.y, right.z]);
+    this.app.graphicsDevice.scope.resolve('ol_cameraUp').setValue([up.x, up.y, up.z]);
@@ -913 +922 @@ export class WildernessScene implements WorldRenderer {
-        entry.shadow.enabled = false;
+        this.presentation.setShadowVisible(entry.shadow, false);
@@ -933 +942,5 @@ export class WildernessScene implements WorldRenderer {
-      if (this.view) {
+      if (
+        this.view &&
+        (Math.abs(dx) + Math.abs(dy) + Math.abs(dz) > 1e-6 ||
+          entry.renderedSupport !== entry.view.supportSurfaceId)
+      ) {
@@ -947,2 +960,6 @@ export class WildernessScene implements WorldRenderer {
-      entry.shadow.enabled =
-        entry.observed && entry.view.appearance !== 'crate-mesh' && entry.view.kind !== 'station';
+      this.presentation.setShadowVisible(
+        entry.shadow,
+        entry.root.enabled &&
+          entry.view.appearance !== 'crate-mesh' &&
+          entry.view.kind !== 'station',
+      );
@@ -1085 +1102 @@ export class WildernessScene implements WorldRenderer {
-  ): number | null {
+  ): { fraction: number; point: pc.Vec3 } | null {
@@ -1095,5 +1112,18 @@ export class WildernessScene implements WorldRenderer {
-    return image.data[
-      (Math.floor(v * image.height) * image.width + Math.floor(u * image.width)) * 4 + 3
-    ]! >= 24
-      ? t
-      : null;
+    if (
+      image.data[
+        (Math.floor(v * image.height) * image.width + Math.floor(u * image.width)) * 4 + 3
+      ]! < 24
+    )
+      return null;
+    const binding = this.cards.get(card)!;
+    const foot = card
+      .parent!.getPosition()
+      .clone()
+      .add(new pc.Vec3(binding.x, binding.y, binding.z));
+    const point = billboardBodyPoint(
+      new pc.Vec3().lerp(ray.from, ray.to, t),
+      foot,
+      this.camera.right,
+      this.camera.up,
+    );
+    return { point, fraction: cameraDepthFraction(point, ray.from, ray.to, this.camera.forward) };
@@ -1119,2 +1149,2 @@ export class WildernessScene implements WorldRenderer {
-      const t = this.cardHit(card, image, ray);
-      if (t !== null) obstruction = Math.min(obstruction, t);
+      const hit = this.cardHit(card, image, ray);
+      if (hit) obstruction = Math.min(obstruction, hit.fraction);
@@ -1128 +1158,2 @@ export class WildernessScene implements WorldRenderer {
-      let fraction: number | null = null;
+      let fraction: number | null = null,
+        bodyPoint: pc.Vec3 | undefined;
@@ -1137 +1168,3 @@ export class WildernessScene implements WorldRenderer {
-        fraction = this.cardHit(entry.sprite, image, ray);
+        const hit = this.cardHit(entry.sprite, image, ray);
+        fraction = hit?.fraction ?? null;
+        bodyPoint = hit?.point;
@@ -1148 +1181 @@ export class WildernessScene implements WorldRenderer {
-        const hit = ray.from.clone().lerp(ray.from, ray.to, fraction),
+        const hit = bodyPoint ?? ray.from.clone().lerp(ray.from, ray.to, fraction),
@@ -1197 +1229,0 @@ export class WildernessScene implements WorldRenderer {
-    const local = this.local(event);
@@ -1222,2 +1253,0 @@ export class WildernessScene implements WorldRenderer {
-    } else {
-      this.publishHover(this.pick(local.x, local.y), { x: event.clientX, y: event.clientY });
@@ -1224,0 +1255 @@ export class WildernessScene implements WorldRenderer {
+    // Hover is sampled by update() at 20 Hz; clicks still resolve immediately.
diff --git a/apps/client/src/spatial-art.ts b/apps/client/src/spatial-art.ts
index fb9103d..9d5065f 100644
--- a/apps/client/src/spatial-art.ts
+++ b/apps/client/src/spatial-art.ts
@@ -58,0 +59,27 @@ export function surfaceMesh(device: pc.GraphicsDevice, surface: WalkableSurface)
+/** Painted-size plank seams share one receiver mesh instead of one shadow-casting box each.
+ * These are surface detail, not independent geometry or physical obstacles. */
+export function surfaceSeamsMesh(device: pc.GraphicsDevice, surface: WalkableSurface): pc.Mesh {
+  const positions: number[] = [],
+    normals: number[] = [],
+    indices: number[] = [];
+  const length = Math.hypot(surface.slopeX, 1, surface.slopeZ);
+  for (let x = surface.minX + 0.4; x < surface.maxX; x += 0.5) {
+    const first = positions.length / 3;
+    for (const [px, pz] of [
+      [x - 0.009, surface.minZ],
+      [x - 0.009, surface.maxZ],
+      [Math.min(x + 0.009, surface.maxX), surface.maxZ],
+      [Math.min(x + 0.009, surface.maxX), surface.minZ],
+    ]) {
+      positions.push(px!, surfaceHeight(surface, px!, pz!) + 0.014, pz!);
+      normals.push(-surface.slopeX / length, 1 / length, -surface.slopeZ / length);
+    }
+    indices.push(first, first + 1, first + 2, first, first + 2, first + 3);
+  }
+  const mesh = new pc.Mesh(device);
+  mesh.setPositions(positions);
+  mesh.setNormals(normals);
+  mesh.setIndices(indices);
+  mesh.update();
+  return mesh;
+}
diff --git a/apps/client/src/world-presentation.ts b/apps/client/src/world-presentation.ts
index c15315f..072099b 100644
--- a/apps/client/src/world-presentation.ts
+++ b/apps/client/src/world-presentation.ts
@@ -41,0 +42 @@ export interface RevealBinding {
+  sourceMesh?: pc.MeshInstance;
@@ -44,0 +46,5 @@ export interface RevealBinding {
+  center: Float32Array;
+  size: Float32Array;
+  foot: Float32Array;
+  spriteSize: Float32Array;
+  lastStrength: number;
@@ -55,0 +62 @@ export class WorldPresentation {
+  private readonly worldLayer: pc.Layer;
@@ -58 +65 @@ export class WorldPresentation {
-  private readonly hiddenMaterials = new WeakMap<pc.MeshInstance, pc.Material>();
+  private readonly hiddenMeshes = new WeakSet<pc.MeshInstance>();
@@ -63,0 +71 @@ export class WorldPresentation {
+    this.worldLayer = app.scene.layers.getLayerById(pc.LAYERID_WORLD)!;
@@ -130 +138,19 @@ export class WorldPresentation {
-    return { meshes, strength: 0, frame: material };
+    const binding: RevealBinding = {
+      meshes,
+      sourceMesh: sprite.render?.meshInstances[0],
+      strength: 0,
+      frame: material,
+      lastStrength: 0,
+      center: new Float32Array(3),
+      size: new Float32Array(3),
+      foot: new Float32Array(3),
+      spriteSize: new Float32Array(2),
+    };
+    for (const mi of meshes) {
+      mi.setParameter('ol_revealCenter', binding.center);
+      mi.setParameter('ol_revealSize', binding.size);
+      mi.setParameter('ol_spriteFoot', binding.foot);
+      mi.setParameter('ol_spriteSize', binding.spriteSize);
+      mi.setParameter('ol_revealStrength', 0);
+    }
+    return binding;
@@ -142 +168,4 @@ export class WorldPresentation {
-    // Revoked knowledge disappears immediately; only still-authorized presentation eases out.
+    // Most targets are outside the local reveal. Skip both allocations and uniform writes
+    // once hidden, but always process revocation of a previously visible target immediately.
+    const target = authorized ? strength : 0;
+    if (target === 0 && binding.strength === 0) return;
@@ -144 +173 @@ export class WorldPresentation {
-      ? binding.strength + (strength - binding.strength) * (1 - Math.exp(-dt * 12))
+      ? binding.strength + (target - binding.strength) * (1 - Math.exp(-Math.min(dt, 0.1) * 12))
@@ -145,0 +175,14 @@ export class WorldPresentation {
+    if (Math.abs(binding.strength - target) < 0.001) binding.strength = target;
+    binding.center[0] = foot.x;
+    binding.center[1] = foot.y + height / 2;
+    binding.center[2] = foot.z;
+    binding.size[0] = binding.size[2] = Math.max(width * 0.7, 0.1);
+    binding.size[1] = Math.max(height * 0.65, 0.1);
+    const spriteFoot = (
+      binding.sourceMesh?.getParameter('ol_spriteFoot') as { data?: ArrayLike<number> } | undefined
+    )?.data;
+    binding.foot[0] = spriteFoot?.[0] ?? foot.x;
+    binding.foot[1] = spriteFoot?.[1] ?? foot.y;
+    binding.foot[2] = spriteFoot?.[2] ?? foot.z;
+    binding.spriteSize[0] = width;
+    binding.spriteSize[1] = height;
@@ -149,9 +192,2 @@ export class WorldPresentation {
-      mi.setParameter('ol_revealStrength', binding.strength);
-      mi.setParameter('ol_revealCenter', [foot.x, foot.y + height / 2, foot.z]);
-      mi.setParameter('ol_revealSize', [
-        Math.max(width * 0.7, 0.1),
-        Math.max(height * 0.65, 0.1),
-        Math.max(width * 0.7, 0.1),
-      ]);
-      mi.setParameter('ol_spriteFoot', [foot.x, foot.y, foot.z]);
-      mi.setParameter('ol_spriteSize', [width, height]);
+      if (binding.lastStrength !== binding.strength)
+        mi.setParameter('ol_revealStrength', binding.strength);
@@ -158,0 +195 @@ export class WorldPresentation {
+    binding.lastStrength = binding.strength;
@@ -173,7 +210,7 @@ export class WorldPresentation {
-        const source = this.hiddenMaterials.get(mi);
-        if (hidden && !source) {
-          this.hiddenMaterials.set(mi, mi.material);
-          mi.material = this.proxyMaterial;
-        } else if (!hidden && source) {
-          mi.material = source;
-          this.hiddenMaterials.delete(mi);
+        const wasHidden = this.hiddenMeshes.has(mi);
+        if (hidden && !wasHidden) {
+          this.hiddenMeshes.add(mi);
+          this.worldLayer.removeMeshInstances([mi], true);
+        } else if (!hidden && wasHidden) {
+          this.hiddenMeshes.delete(mi);
+          this.worldLayer.addMeshInstances([mi], true);
@@ -189,0 +227 @@ export class WorldPresentation {
+      layers: [],
@@ -190,0 +229,3 @@ export class WorldPresentation {
+    const meshes = [...proxy.render!.meshInstances];
+    this.worldLayer.addShadowCasters(meshes);
+    proxy.on('destroy', () => this.worldLayer.removeShadowCasters(meshes));
@@ -195,0 +237,5 @@ export class WorldPresentation {
+  setShadowVisible(proxy: pc.Entity, visible: boolean): void {
+    // layers:[] means component hierarchy changes cannot reinsert invisible color draws.
+    // The caller supplies current authorization/floor visibility, not the last-seen ghost.
+    for (const mi of proxy.render!.meshInstances) mi.castShadow = visible;
+  }
