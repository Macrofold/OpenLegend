name: Workshop continuation source
on:
  push:
    branches: [feature/invention-repertoire-foundation]
    paths: [.github/workflows/workshop-continuation-source.yml]
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install --frozen-lockfile --ignore-scripts
      - name: Export sources and dependencies without executing tests
        run: tar --exclude=.git -czf /tmp/workshop-continuation.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: workshop-continuation-source
          path: /tmp/workshop-continuation.tar.gz
          retention-days: 1
