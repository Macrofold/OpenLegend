name: Spatial review reconciliation source
on:
  push:
    branches: [feature/spatial-world-3d]
    paths: [.github/workflows/spatial-review-export.yml]
permissions:
  contents: read
jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - run: git archive --format=zip --output=/tmp/spatial-reconcile-source.zip HEAD
      - uses: actions/upload-artifact@v4
        with:
          name: spatial-reconcile-source
          path: /tmp/spatial-reconcile-source.zip
          retention-days: 1
